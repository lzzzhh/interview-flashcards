import type { QACard } from '../types';

const SM2 = { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() };

export const machineLearningCards: QACard[] = [
  // ============================================================
  // 监督学习 (20 questions: ml-1 ~ ml-20)
  // ============================================================
  {
    id: 'ml-1',
    category: 'machine-learning',
    question: '逻辑回归的损失函数是什么？请推导其梯度。',
        answer: '逻辑回归使用交叉熵损失函数（Cross-Entropy Loss），也称为对数损失（Log Loss）。对于二分类问题，假设 y ∈ {0, 1}，模型的预测值为 ŷ = σ(wᵀx) = 1/(1+e^{-wᵀx})。单个样本的损失为 L = -[y·log(ŷ) + (1-y)·log(1-ŷ)]，本质上是极大似然估计的负对数形式。对整个数据集求平均得到代价函数 J(w) = -(1/m)·Σ[y⁽ⁱ⁾log(ŷ⁽ⁱ⁾) + (1-y⁽ⁱ⁾)log(1-ŷ⁽ⁱ⁾)]。梯度推导利用 sigmoid 函数的性质 σ\\\'(z) = σ(z)(1-σ(z))，对 J(w) 求导得 ∂J/∂w = (1/m)·Σ(ŷ⁽ⁱ⁾ - y⁽ⁱ⁾)·x⁽ⁱ⁾，形式与线性回归梯度极其相似，这正是使用交叉熵而非 MSE 的优势——避免梯度消失问题。',
    tags: ['逻辑回归', '损失函数', '梯度下降'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-2',
    category: 'machine-learning',
    question: 'SVM 中如何选择合适的核函数？',
    answer: 'SVM 核函数将数据映射到高维空间以实现线性可分，选择原则如下：(1) 线性核（K(x,z)=xᵀz）：特征维度远大于样本数，或数据近似线性可分时首选，训练速度快且不容易过拟合；(2) 高斯/RBF 核（K(x,z)=exp(-γ‖x-z‖²)）：特征少样本多时最常用，通过 γ 控制单个样本的影响半径，γ 越大模型越复杂越容易过拟合；(3) 多项式核（K(x,z)=(xᵀz+c)^d）：适用于图像处理等场景，但参数 d 较多需要调参。实际工程中先用线性核作为 baseline，效果不好再试 RBF 核并通过交叉验证调优 C 和 γ 参数。',
    tags: ['SVM', '核函数', 'RBF', '特征映射'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-3',
    category: 'machine-learning',
    question: '决策树中的信息增益和基尼系数（Gini Index）分别是什么？它们如何用于分裂节点？',
    answer: '信息增益（Information Gain）基于信息熵，熵衡量数据集的纯度：H(D) = -Σᵢ pᵢ·log₂(pᵢ)。对特征 A 分裂后，信息增益 IG(D,A) = H(D) - Σᵥ (|Dᵥ|/|D|)·H(Dᵥ)，选择 IG 最大的特征进行分裂，ID3 算法即基于此。基尼系数（Gini Index）是 CART 树使用的分裂标准：Gini(D) = 1 - Σᵢ pᵢ²，取值范围 [0, 1-1/k]，越小数据越纯。分裂时计算加权基尼指数 Σ(|Dᵥ|/|D|)·Gini(Dᵥ)，选择使其最小的特征和切分点。二者本质都是衡量不纯度的指标：信息增益偏好多值特征（需用增益率修正），Gini 计算更快（无对数运算），通常两者效果相近。',
    tags: ['决策树', '信息增益', '基尼系数', 'CART'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-4',
    category: 'machine-learning',
    question: 'KNN（K近邻）算法的原理是什么？有哪些优缺点？',
    answer: 'KNN 是一种惰性学习（lazy learning）算法，没有显式的训练过程，核心思想是"近朱者赤"：对一个新样本，在特征空间中找距离最近的 K 个训练样本，通过投票（分类）或平均（回归）进行预测。距离度量常用欧氏距离 d = √Σ(xᵢ-yᵢ)²，也可用曼哈顿距离或余弦相似度。K 值选择关键：K 太小 → 模型复杂、过拟合、对噪声敏感；K 太大 → 模型简单、欠拟合。通常用交叉验证选 K。优点：(1) 简单直观无需训练；(2) 天然支持多分类；(3) 无假设前提。缺点：(1) 预测慢 O(nd)，大数据集不可用；(2) 维数灾难（高维空间距离失效）；(3) 对缺失值和异常值敏感；(4) 特征尺度必须统一。',
    tags: ['KNN', '惰性学习', '距离度量', 'K值选择'],
    subTopic: '监督学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-5',
    category: 'machine-learning',
    question: '朴素贝叶斯"朴素"在哪里？推导其分类原理。',
    answer: '"朴素"指特征条件独立假设：给定类别 y，各特征 x₁,...,xₙ 相互独立，即 P(x₁,...,xₙ|y) = Πᵢ P(xᵢ|y)。这个假设在现实中几乎不成立，但实际分类效果往往很好。分类决策基于贝叶斯定理：P(y|x₁,...,xₙ) ∝ P(y)·Πᵢ P(xᵢ|y)，选择后验概率最大的类别 ŷ = argmax_y P(y)·Πᵢ P(xᵢ|y)。根据特征类型不同分为三种：(1) 高斯朴素贝叶斯——连续特征建模为高斯分布 P(xᵢ|y)=N(μᵧ,σ²ᵧ)；(2) 多项式朴素贝叶斯——离散计数特征，适用于文本分类（TF-IDF）；(3) 伯努利朴素贝叶斯——二值特征（词出现/不出现）。优点是参数少速度快，维度独立可并行；缺点是无法捕捉特征交互。',
    tags: ['朴素贝叶斯', '贝叶斯定理', '条件独立', '文本分类'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-6',
    category: 'machine-learning',
    question: '线性回归和逻辑回归的本质区别是什么？为什么逻辑回归叫"回归"却用于分类？',
    answer: '核心区别：(1) 线性回归预测连续值 ŷ=wᵀx+b，输出可以是 (-∞,+∞)，损失函数为 MSE；(2) 逻辑回归在线性组合之上套了 sigmoid 函数 ŷ=σ(wᵀx+b)=1/(1+e^{-(wᵀx+b)})，将输出压缩到 (0,1) 表示概率，损失函数为交叉熵。之所以叫"回归"是因为它本质上是对对数几率进行线性回归：log(P/(1-P)) = wᵀx+b，即逻辑回归是在对"概率"这一连续量做回归建模，只是最终通过阈值（如 0.5）将概率转化为类别。逻辑回归的优势：(1) 直接输出概率，可解释性强；(2) 系数 w 可理解为特征对 log-odds 的贡献；(3) 可通过正则化防止过拟合。',
    tags: ['线性回归', '逻辑回归', 'Sigmoid', '损失函数'],
    subTopic: '监督学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-7',
    category: 'machine-learning',
    question: '什么是过拟合和欠拟合？如何识别和解决？',
    answer: '过拟合（Overfitting）：模型在训练集上表现极好但测试集表现差，即"记住"了训练数据而非学习到通用模式。表现为训练误差持续降低而验证误差上升。解决方案：(1) 增加训练数据；(2) 正则化（L1/L2）；(3) 早停（Early Stopping）；(4) Dropout（神经网络）；(5) 降低模型复杂度（减少参数、减枝等）；(6) 数据增强。欠拟合（Underfitting）：模型连训练集都拟合不好，即"太过简单"。表现为训练误差和验证误差都高。解决方案：(1) 增加模型复杂度；(2) 增加更多/更好的特征；(3) 减少正则化强度；(4) 训练更长时间。机器学习本质是在偏差（欠拟合）和方差（过拟合）之间找平衡。',
    tags: ['过拟合', '欠拟合', '模型诊断', '正则化'],
    subTopic: '监督学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-8',
    category: 'machine-learning',
    question: '请解释偏差-方差权衡（Bias-Variance Tradeoff）。',
    answer: '偏差（Bias）是模型预测值期望与真实值之间的差距，反映模型拟合能力；方差（Variance）是不同训练集上模型预测值的波动程度，反映模型稳定性。期望泛化误差可分解为：Error = Bias² + Variance + Irreducible Error（不可约噪声）。简单模型（如线性模型）：高偏差低方差（欠拟合）；复杂模型（如深度树）：低偏差高方差（过拟合）。总误差在两者交点处最小，这就是偏差-方差权衡。现代深度学习有时呈现"双降"（Double Descent）现象——参数多到一定程度后测试误差会再次下降，但这并不否定传统权衡理论。实战策略：集成方法（Bagging 降方差，Boosting 降偏差）、正则化（降低方差可能增加偏差）、交叉验证选最优复杂度。',
    tags: ['偏差', '方差', '权衡', '泛化误差'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-9',
    category: 'machine-learning',
    question: 'K 折交叉验证（K-Fold Cross-Validation）是什么？为什么比简单划分训练/测试集更好？',
    answer: 'K 折交叉验证将数据均分为 K 份，每次用 K-1 份训练、1 份验证，轮流 K 次取平均作为模型性能估计。常用 K=5 或 10。优点：(1) 所有数据既参与训练也参与验证，数据利用率高，特别适合小数据集；(2) 降低单次随机划分带来的方差，评估结果更稳定可靠；(3) 可以估计模型性能的方差（K 次结果的标准差）判断模型稳定性。注意事项：(1) 分类任务建议用分层 K 折（StratifiedKFold）保持每折类别比例一致；(2) 时间序列数据不宜随机划分，应用时序交叉验证；(3) 对大数据集计算开销大，可用简单划分替代。交叉验证主要用于模型选择和超参数调优，最终模型在全部数据上重新训练。',
    tags: ['交叉验证', 'K折', '模型评估', '过拟合'],
    subTopic: '监督学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-10',
    category: 'machine-learning',
    question: 'L1 正则化和 L2 正则化有什么区别？各自有什么数学性质？',
    answer: 'L1 正则化（Lasso）：在损失函数后加 λ·Σ|wᵢ|（权重绝对值之和）。从贝叶斯角度看等价于权重服从拉普拉斯先验分布。核心特性是产生稀疏解——使部分权重严格为零，天然具有特征选择功能。梯度为常数 ±λ（符号函数），不随权重减小而减小。L2 正则化（Ridge）：在损失函数后加 λ·Σwᵢ²（权重平方和）。等价于高斯先验分布。使所有权重收缩但不为零，平滑且数学性质好，对共线性特征各维度均匀收缩。几何理解：L1 约束域是菱形，目标函数等高线容易在尖角处（坐标轴）相交得到稀疏解；L2 约束域是圆形，等高线在切点处相交权重非零但较小。实践中常用 Elastic Net 结合两者：λ₁·‖w‖₁ + λ₂·‖w‖₂²。',
    tags: ['L1正则化', 'L2正则化', 'Lasso', 'Ridge', '稀疏性'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-11',
    category: 'machine-learning',
    question: '什么是梯度下降？如何直观理解其优化过程？',
    answer: '梯度下降是一阶迭代优化算法，通过沿负梯度方向更新参数来最小化目标函数。参数更新公式：w := w - α·∇J(w)，其中 α 为学习率。直观理解：假设你站在山腰要下山，梯度指明当前位置"最陡上升"的方向，负梯度就是"最陡下降"方向。你每次沿该方向走一小步（学习率控制步长），不断重复直到到达谷底（局部最小值）。梯度是一个向量，每个分量 ∂J/∂wᵢ 表示损失函数对该参数的敏感度——该参数微调能带来多大损失变化。对于凸函数（如线性回归的 MSE），梯度下降保证收敛到全局最优；对于非凸函数（如神经网络），可能陷入局部最优或鞍点。',
    tags: ['梯度下降', '优化', '学习率', '凸优化'],
    subTopic: '监督学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-12',
    category: 'machine-learning',
    question: '学习率（Learning Rate）如何选择？太大或太小有什么后果？',
    answer: '学习率 α 控制参数更新的步长。太大：(1) 损失震荡不收敛甚至发散，在最小值附近来回跳动；(2) 可能导致梯度爆炸。太小：(1) 收敛极慢，训练时间过长；(2) 可能卡在局部最优或鞍点出不来。选择策略：(1) 在 [0.1, 0.0001] 对数尺度上试 (0.1, 0.01, 0.001, 0.0001)；(2) 绘制损失曲线观察：稳定下降则合适，震荡则减小，平坦则增大；(3) 使用自动学习率方法如 Adam 可省去手动调参。实践可采用学习率衰减策略：阶梯衰减（每 N 轮减半）、指数衰减（α = α₀·e^{-kt}）、余弦退火等，训练初期大步快走，后期小步精调。',
    tags: ['学习率', '超参数', '优化', '收敛'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-13',
    category: 'machine-learning',
    question: '特征缩放（Feature Scaling）为什么重要？什么算法必须做特征缩放？',
    answer: '特征缩放将不同量纲的特征统一到相近范围，常用方法：(1) 标准化 Z-score = (x-μ)/σ → 均值 0 方差 1；(2) 归一化 Min-Max = (x-min)/(max-min) → [0,1]。必须缩放的算法：(1) 基于距离的：KNN、K-Means —— 值域大的特征主导距离计算；(2) 基于梯度的：神经网络、逻辑回归——缩放能加速梯度下降收敛，避免"狭长山谷"导致之字形震荡；(3) SVM——RBF 核对尺度敏感；(4) PCA——方差大的特征主导主成分方向。不需强制缩放的算法：(1) 树模型（RF、GBDT、XGBoost）——基于阈值分裂对单调变换不敏感；(2) 朴素贝叶斯——各自独立建模。对 SVM 和神经网络，特征不缩放到同一量级，模型效果会极差。',
    tags: ['特征缩放', '归一化', '标准化', '数据预处理'],
    subTopic: '监督学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-14',
    category: 'machine-learning',
    question: '如何处理类别特征（Categorical Features）？One-Hot 编码有什么缺陷？',
    answer: '类别特征处理方法：(1) One-Hot 编码：每个类别变成一个二值列，适合无序低基数类别（如性别、城市≤10个），缺陷是对高基数特征会产生维数爆炸和高稀疏矩阵；(2) Label 编码：将类别映射为整数 0,1,2...，适合有序类别（如学历：高中<本科<硕士），但树模型对无序 Label 编码会误解距离关系；(3) Target Encoding：用类别对应的目标变量均值替换，如 mean(City)=0.3，优点是不增加维度，缺点是有标签泄露风险需用交叉验证套；(4) Embedding：神经网络中用低维稠密向量表示类别，适合极高基数场景（如用户 ID）自动学习语义关系。对树模型（XGBoost/LightGBM），直接传 Label 编码即可，树能自动找到最佳分裂点，不需要 One-Hot。',
    tags: ['类别特征', 'One-Hot', '编码', 'Embedding'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-15',
    category: 'machine-learning',
    question: '缺失值如何处理？常见插补方法有哪些？',
    answer: '处理策略分三步：(1) 分析缺失模式——MCAR（完全随机）、MAR（条件随机）、MNAR（非随机），判断可否删除；(2) 删除法——缺失率 <5% 可直接删样本，缺失率 >60% 可删特征，但信息损失大；(3) 插补法——均值/中位数插补（简单但降低方差）；众数插补（分类变量）；用相关变量做线性回归插补；KNN 插补（找相似样本的该特征值填空）；迭代插补（MICE/IterativeImputer）将每个含缺失变量轮流做目标训练模型预测缺失值；(4) 建模法——将缺失作为独立类别（树模型天然支持 XGBoost 的 missing 参数）；添加"是否缺失"指示列保留缺失信息。实战：LightGBM 可自动处理缺失值无需手动插补。',
    tags: ['缺失值', '数据清洗', '插补', '预处理'],
    subTopic: '监督学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-16',
    category: 'machine-learning',
    question: '类别不均衡问题有哪些解决方案？SMOTE 的原理是什么？',
    answer: '类别不均衡指正负样本比例悬殊（如欺诈检测 1:999），模型会倾向预测多数类。解决方案分层：(1) 数据层面——欠采样：随机删除多数类样本（可能丢失信息），NearMiss 选择"有用"多数类；过采样：随机复制少数类（容易过拟合），SMOTE：在少数类样本间插值生成新样本（KNN 选最近邻，随机线性插值 x_new = x + rand(0,1)·(x_nn - x)）；混合：SMOTE + ENN/Tomek Links 先过采样再清洗边界；(2) 算法层面——类别权重：在损失函数中给少数类更大惩罚权重，样本权重 ∝ 1/频率；Focal Loss：减少易分类样本的权重，聚焦困难样本；(3) 评估层面——不用 Accuracy 用 Precision/Recall/F1/AUC。SMOTE 不适用于超高维稀疏数据（如文本），此时用类别权重更合适。',
    tags: ['类别不均衡', 'SMOTE', '采样', '样本权重'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-17',
    category: 'machine-learning',
    question: '决策树如何进行剪枝（Pruning）？预剪枝和后剪枝有什么区别？',
    answer: '剪枝是防止决策树过拟合的核心手段。预剪枝（Pre-pruning）：在树生长过程中提前停止分裂，条件包括——最大深度（max_depth）、最小节点样本数（min_samples_split）、最小叶节点样本数（min_samples_leaf）、信息增益低于阈值、节点纯度足够高等。优点是训练快，缺点是可能"短视"过早停止（当前无增益但之后再分可能有增益）。后剪枝（Post-pruning）：先生成一棵完整树再自底向上修剪——(1) 代价复杂度剪枝（CCP，CART 使用）：定义损失 L(T)=误分类率+α·|T|，|T| 为叶节点数，α 越大树越小，对每个 α 找最优子树序列再用交叉验证选 α；(2) 减少错误剪枝（REP）：用验证集评估，若删除某子树后根节点替换为叶节点能降低验证误差则剪枝；(3) 悲观错误剪枝（PEP，C4.5 使用）：不用验证集，基于训练集误差加上连续性校正评估。后剪枝通常效果更好但计算量更大，实践中 XGBoost/LightGBM 使用预剪枝+正则化效果就很好。',
    tags: ['决策树', '剪枝', '预剪枝', '后剪枝', 'CCP'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-18',
    category: 'machine-learning',
    question: '随机森林中如何计算特征重要性？有哪些方法？',
    answer: '特征重要性衡量每个特征对模型预测的贡献，常用三种方法：(1) 基于不纯度减少（MDI / Gini Importance）：计算每个特征在所有树的每次分裂中基尼系数减少量的加权平均（权重为分裂节点的样本占比），缺点是偏向高基数特征和连续特征，不能反映泛化能力；(2) 基于精确度下降（Permutation Importance / MDA）：打乱某个特征的取值后，观察模型预测误差的增量（比如用 OOB 数据），误差增加越多说明该特征越重要。优点是无偏、适用任何模型，缺点是高相关特征会互相稀释重要性；(3) SHAP 值：基于博弈论 Shapley 值，计算每个特征对每个样本预测的边际贡献，是当前最权威的方法，可解释单样本预测且满足一致性。实战中排列重要性和 SHAP 比 MDI 更可靠。',
    tags: ['随机森林', '特征重要性', 'SHAP', '排列重要性'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-19',
    category: 'machine-learning',
    question: 'SVM 中的软间隔（Soft Margin）和硬间隔（Hard Margin）有什么区别？参数 C 的作用是什么？',
    answer: '硬间隔 SVM 要求所有样本严格在决策边界两侧，要求数据完全线性可分，对异常点极为敏感——一个离群点可能完全改变决策面。现实中数据基本都不是完全线性可分的。软间隔 SVM 引入松弛变量 ξᵢ≥0 允许部分样本越界，约束条件变为 yᵢ(wᵀxᵢ+b) ≥ 1-ξᵢ，目标函数变为 min (1/2)‖w‖² + C·Σξᵢ。参数 C 是正则化系数（理论上应为惩罚系数），权衡间隔最大化和误分类容忍度：C 大 → 惩罚重 → 硬间隔效果 → 低偏差高方差（过拟合风险）；C 小 → 容忍误分类 → 更大间隔 → 高偏差低方差（欠拟合风险）。C 通过交叉验证选择 (0.01, 0.1, 1, 10, 100)。软间隔 SVM 的数学优势在于原问题始终有可行解，且支持向量是 ξᵢ>0 或恰好 ξᵢ=0 且 yᵢ(wᵀxᵢ+b)=1 的样本。',
    tags: ['SVM', '软间隔', '硬间隔', '支持向量'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-20',
    category: 'machine-learning',
    question: '为什么要构造多项式特征？使用多项式特征有什么注意事项？',
    answer: '多项式特征通过原始特征的幂次和交叉组合生成非线性特征，使线性模型能拟合非线性关系。例如原始特征 x₁,x₂ → 多项式 d=2 生成 [1, x₁, x₂, x₁², x₁x₂, x₂²]。好处：线性回归 + 多项式特征 = 多项式回归，保持了系数可解释性的同时大幅提升拟合能力。注意事项：(1) 维数爆炸——n 个特征 d 次多项式生成 C(n+d,d) 个特征，如 100 特征 d=2 → 5151 个，极易过拟合，必须配合 L1/L2 正则化；(2) 特征尺度——多项式特征需要先标准化，否则 x² 和 x 量级差异悬殊；(3) 外推不可靠——多项式在训练数据范围外表现极不稳定；(4) 高次项易过拟合，通常 d≤3 配合交叉验证选 d；(5) 多项式核 SVM 等价于隐式多项式特征映射，通过核技巧避免了显式构造高维特征。',
    tags: ['多项式特征', '特征工程', '非线性', '维度灾难'],
    subTopic: '监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 无监督学习 (10 questions: ml-21 ~ ml-30)
  // ============================================================
  {
    id: 'ml-21',
    category: 'machine-learning',
    question: 'K-Means 聚类中如何选择 K 值？肘部法则（Elbow Method）和轮廓系数（Silhouette Score）分别怎么用？',
    answer: 'K 值选择是 K-Means 的核心难题，常用两种方法：(1) 肘部法则：计算不同 K 下的 SSE（误差平方和 SSE=Σ‖x-μ‖²），画出 K-SSE 曲线，找到"肘部"即下降速度骤减的转折点。但现实数据常无明显肘部，需要经验判断；(2) 轮廓系数：对每个样本计算 sᵢ=(bᵢ-aᵢ)/max(aᵢ,bᵢ)，其中 aᵢ 是样本到同簇其他样本的平均距离（簇内紧凑度），bᵢ 是到最近其他簇的平均距离（簇间分离度）。sᵢ 范围 [-1,1]，越接近 1 聚类越好。对每个 K 计算所有样本的平均轮廓系数，选最大的 K。肘部法则关注"组内差异"，轮廓系数兼顾"组内+组间"，后者更客观。其他方法还有 Gap Statistic（比较随机数据下的簇改善量）和业务先验知识。',
    tags: ['K-Means', '肘部法则', '轮廓系数', '聚类评估'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-22',
    category: 'machine-learning',
    question: '请推导 PCA（主成分分析）的数学原理。',
    answer: 'PCA 的核心目标是找到投影方向，使数据投影后的方差最大化。推导过程：(1) 数据中心化：x←x-μ，使均值为 0；(2) 计算协方差矩阵 Σ = (1/n)·XᵀX；(3) 对 Σ 做特征值分解 Σ = VΛVᵀ，特征向量 vᵢ 即为第 i 主成分方向，对应特征值 λᵢ 为该方向的方差。等价推导：PCA 也可从最小化重建误差角度导出——PCA 是最优线性自编码器（线性激活+MSE损失）。降维时选前 d 个最大特征值对应的特征向量 W=[v₁,...,v_d]，投影 Z = XW，重建 X̂ = ZWᵀ。保留方差比例 = Σᵢ₌₁^d λᵢ / Σᵢ₌₁^p λᵢ，通常选 >90% 或 95% 确定 d。PCA 是无监督线性方法，优点是降维去噪，缺点是丢失可解释性（主成分是特征线性组合）且只能捕捉线性关系。',
    tags: ['PCA', '主成分分析', '特征值分解', '降维'],
    subTopic: '无监督学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-23',
    category: 'machine-learning',
    question: '聚类有哪些常用的评估指标？无标签数据如何评价聚类质量？',
    answer: '聚类评估分两大类：(1) 内部指标（无标签）：轮廓系数衡量紧凑度和分离度的平衡；Davies-Bouldin 指数（DBI）：计算簇间相似度（簇内距离之和/簇心距离）的平均值，越小越好；Calinski-Harabasz 指数（CHI / 方差比准则）：簇间离散度与簇内离散度的比值，越大越好。三者各有偏好，DBI 对圆形簇好，CHI 对大样本好；(2) 外部指标（有标签时验证）：调整兰德指数（ARI）：衡量聚类划分与真实标签的一致性，校正了随机标签的期望，[-1,1] 越接近 1 越好；归一化互信息（NMI）：衡量两个分配的互信息归一化到 [0,1]；同质性/完整性/V-measure：同质性=每簇只含单一类别，完整性=每类别分在同一簇，V-measure 是两者的调和平均。',
    tags: ['聚类评估', '轮廓系数', 'ARI', 'NMI', '无监督评估'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-24',
    category: 'machine-learning',
    question: 'DBSCAN 和 K-Means 有什么区别？各自适用于什么场景？',
    answer: '核心区别：(1) K-Means 需要预设 K 值，DBSCAN 只需指定 eps（邻域半径）和 MinPts（最少点数），能自动发现簇数；(2) K-Means 只能生成凸形/球形簇，DBSCAN 可发现任意形状簇（如环状、S 形）；(3) DBSCAN 天然能识别噪声点（不属于任何簇的离群点），K-Means 强制每个点归属一簇；(4) K-Means 对异常值敏感，DBSCAN 有抗噪能力；(5) 时间复杂度：K-Means O(nkt)，DBSCAN 用 KD-tree 可达 O(n·log n)，但高维数据会退化为 O(n²)；(6) DBSCAN 对 eps 参数敏感，且密度不均数据（不同簇密度差异大）表现差。选择原则：数据维度低、簇可能非球形、有噪声 → DBSCAN；数据量大维度高、已知 K、簇呈球形 → K-Means。',
    tags: ['DBSCAN', 'K-Means', '密度聚类', '聚类对比'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-25',
    category: 'machine-learning',
    question: '高斯混合模型（GMM）与 K-Means 有什么区别？EM 算法在 GMM 中如何迭代？',
    answer: 'GMM 假设数据来自 K 个高斯分布的混合，每个样本以概率 πₖ 属于第 k 个高斯 N(μₖ, Σₖ)。与 K-Means 的核心区别：(1) K-Means 每个样本硬分配给一个簇，GMM 输出每个样本属于各簇的软概率（后验概率 γ(zₙₖ)）；(2) K-Means 假定簇是等方差球形，GMM 通过协方差矩阵 Σₖ 支持椭球形（full/tied/diagonal/spherical 四种模式），表达能力强；(3) K-Means 是 GMM 的特例（各簇协方差 σ²I 且 σ→0 时退化为 K-Means）。EM 算法求解：(E步) 计算后验概率 γ(zₙₖ) = πₖ·N(xₙ|μₖ,Σₖ) / Σⱼ πⱼ·N(xₙ|μⱼ,Σⱼ)；(M步) 用 γ 加权更新参数 πₖ = Nₖ/N，μₖ = (1/Nₖ)Σγ(zₙₖ)xₙ，Σₖ = (1/Nₖ)Σγ(zₙₖ)(xₙ-μₖ)(xₙ-μₖ)ᵀ。交替迭代至收敛。GMM 的缺点：需要选 K，对初始化敏感，计算协方差矩阵逆可能数值不稳定。',
    tags: ['GMM', 'EM算法', '高斯分布', '软聚类'],
    subTopic: '无监督学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-26',
    category: 'machine-learning',
    question: 't-SNE 和 PCA 有什么区别？t-SNE 为什么适合可视化？',
    answer: '核心区别：(1) PCA 是确定性的线性降维（矩阵分解），t-SNE 是随机的非线性降维（梯度优化）；(2) PCA 保留全局结构（方差最大方向），t-SNE 保留局部结构（相似邻居关系优先）；(3) PCA 可反向重建数据，t-SNE 不可逆。t-SNE 原理：(a) 高维用高斯分布计算样本间相似度 pⱼ|ᵢ ∝ exp(-‖xᵢ-xⱼ‖²/2σᵢ²)，对称化 pᵢⱼ=(pᵢ|ⱼ+pⱼ|ᵢ)/2n；(b) 低维用 t 分布（自由度=1）算相似度 qᵢⱼ∝(1+‖yᵢ-yⱼ‖²)⁻¹；(c) 最小化 KL 散度 KL(P‖Q)=Σpᵢⱼ·log(pᵢⱼ/qᵢⱼ)。t 分布尾部更重，高维拥挤的远距离点在低维被拉开形成更大间距，解决了"拥挤问题"。t-SNE 特别适合 2D/3D 可视化（可看到明显簇结构），但计算慢 O(n²)，不同 perplexity 结果差异大，不适合高维→高维降维和特征提取。',
    tags: ['t-SNE', 'PCA', '降维', '可视化', 'KL散度'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-27',
    category: 'machine-learning',
    question: '层次聚类（Hierarchical Clustering）分为哪两种？请描述其合并/分裂策略。',
    answer: '层次聚类分为凝聚法（Agglomerative，自底向上）和分裂法（Divisive，自顶向下），凝聚法更常用。凝聚法流程：初始每个样本各为一簇 → 每步合并最近的两簇 → 重复至只剩一簇形成树状图（Dendrogram），最后切树得到所需簇数。簇间距离定义是关键：(1) Single Linkage（单链接）：d(A,B)=min_{a∈A,b∈B} d(a,b)，易产生链状"长簇"，对异常值敏感；(2) Complete Linkage（全链接）：d(A,B)=max_{a∈A,b∈B} d(a,b)，倾向生成紧凑等径簇，异常值使大簇不易合并；(3) Average Linkage（平均链接）：d(A,B)=mean_{a∈A,b∈B} d(a,b)，折中方案最常用；(4) Ward 法：合并后使得 SSE 增加最小的两个簇，Δ=SSE(A∪B)-SSE(A)-SSE(B)，倾向生成等方差球状簇。优点是无需预设 K，树状图可直观展示聚类层次，缺点是 O(n²·log n) 复杂度，大数据不适用。',
    tags: ['层次聚类', '凝聚法', '树状图', 'Ward'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-28',
    category: 'machine-learning',
    question: '自动编码器（Autoencoder）的基本原理是什么？有哪些变种？',
    answer: '自动编码器是一种无监督神经网络，目标是学习恒等映射 h(f(x))≈x，结构为编码器 f: x→z（压缩到低维潜在空间）和解码器 h: z→x̂（重建原始输入）。训练损失为重建误差 L=‖x-x̂‖² 或交叉熵。核心思想：通过瓶颈层迫使网络学到数据的最优压缩表示，瓶颈层即降维后的特征。常见变种：(1) 降噪自编码器（DAE）——输入加噪，输出干净信号，强迫学鲁棒特征；(2) 稀疏自编码器——在隐藏层加 L1 约束或 KL 散度约束，限制神经元激活数量；(3) 变分自编码器（VAE）——在潜在空间加 KL 正则使之服从标准正态分布，具有生成能力 z~N(0,I)→x̂；(4) 收缩自编码器——在损失中加编码器 Jacobian 矩阵 Frobenius 范数，使得学到的表示对输入微小变化不敏感。AE 在异常检测（高重建误差=异常）、降维、去噪等方面有广泛应用。',
    tags: ['自编码器', '降维', 'VAE', '重建误差'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-29',
    category: 'machine-learning',
    question: '常见的异常检测方法有哪些？适用于什么场景？',
    answer: '传统统计方法：(1) 3σ 法则（单变量高斯）：x 超出 [μ-3σ, μ+3σ] 判为异常，仅适合单变量正态分布；(2) 箱线图（IQR）：Q1-1.5·IQR 和 Q3+1.5·IQR 之外。密度/距离方法：(3) Isolation Forest——随机选特征和切分点建树，异常点路径短（容易被孤立），对高维数据高效；(4) LOF（局部异常因子）——比较样本局部密度与邻居的局部密度之比，适合密度不均匀的聚类型异常；(5) One-Class SVM——用 SVM 学习正常数据边界，边界外判异常。深度学习：(6) Autoencoder 重建——正常数据重建误差小，异常数据重建误差大；(7) GAN 异常检测——用无异常的生成器无法很好重建异常样本。选择原则：高维大数据 → Isolation Forest；需要有簇的局部异常 → LOF/DBSCAN；时序数据 → LSTM+重建误差。',
    tags: ['异常检测', 'Isolation Forest', 'LOF', 'One-Class SVM'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-30',
    category: 'machine-learning',
    question: 'LDA（线性判别分析）和 PCA 的区别是什么？LDA 是监督还是无监督方法？',
    answer: 'LDA 是一种监督降维方法，与 PCA 的核心区别：(1) PCA 无监督→最大化投影方差；LDA 有监督→最大化类间散度/类内散度的比值（Fisher 准则），目标函数 J(W) = |WᵀS_B W| / |WᵀS_W W|，其中 S_B 是类间散度矩阵，S_W 是类内散度矩阵；(2) PCA 最多保留 min(n-1,p) 个成分，LDA 最多保留 C-1 个判别方向（C=类别数），因此 LDA 输出维度受限于类别数；(3) LDA 假设各类协方差相同（同方差性），若各类分布差异大则效果差；(4) PCA 降维=数据压缩，LDA 降维=找最佳分类方向。LDA 优缺点：优点是融入标签信息能提升分类准确率，降维后同类点聚拢异类远离；缺点是要求数据符合正态分布+同方差，且需要标签，对小样本高维（p>>n）场景类内散度矩阵不可逆。常先用 PCA 降维再用 LDA。',
    tags: ['LDA', 'PCA', '降维', 'Fisher准则', '监督学习'],
    subTopic: '无监督学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 集成学习 (10 questions: ml-31 ~ ml-40)
  // ============================================================
  {
    id: 'ml-31',
    category: 'machine-learning',
    question: '请对比随机森林（Random Forest）、GBDT 和 XGBoost 的区别。',
    answer: '三者都是树模型集成，但核心机制不同。随机森林（RF）：Bagging 并行训练多棵决策树，每棵树的训练数据 Bootstrap 采样+特征随机子集，预测时投票/平均。降低方差不降偏差，树深可以很高。GBDT（梯度提升决策树）：Boosting 串行，每棵树拟合上一轮的负梯度（残差），步长小（shrinkage）慢慢逼近。同时降低偏差和方差。XGBoost：GBDT 的工程优化版——(1) 正则化目标函数含叶子节点数和叶子权重 L2 正则项，天然防止过拟合；(2) 二阶泰勒展开利用 Hessian 信息，收敛更快；(3) 支持列抽样和行抽样；(4) 处理缺失值（学习默认分裂方向）；(5) 支持近似分裂算法；(6) 并行化特征分裂点搜索。总结：RF→防过拟合稳定好；GBDT→拟合能力强但慢；XGBoost→又快又强。',
    tags: ['随机森林', 'GBDT', 'XGBoost', '集成学习', '对比'],
    subTopic: '集成学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-32',
    category: 'machine-learning',
    question: 'Bagging 和 Boosting 的本质区别是什么？为什么 Bagging 降方差而 Boosting 降偏差？',
    answer: 'Bagging（Bootstrap Aggregating）：并行、独立训练多个弱学习器，通过 Bootstrap 采样制造数据差异性，最终投票/平均。因为各模型之间独立性假设（实际有重叠数据），方差 Var(平均)=ρσ²+(1-ρ)σ²/B，B 越大方差越小，而偏差不变（学习器相同复杂度），所以 Bagging 降低方差。Boosting：串行、自适应训练，每一步聚焦上一步的错分样本（AdaBoost 调整权重）或残差（GBDT 拟合负梯度）。每步用一个弱学习器（如短决策树）逐步修正前序模型的错误，相当于做广义加性模型的逐段逼近 F_M(x)=Σαₘ·fₘ(x)。因为每一步都在减少当前模型与真实值的差距，综合模型逐步降低偏差，但串行过程对数据扰动敏感，方差可能略增。',
    tags: ['Bagging', 'Boosting', '偏差', '方差', '集成学习'],
    subTopic: '集成学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-33',
    category: 'machine-learning',
    question: 'XGBoost 为什么比传统 GBDT 快？有哪些工程优化？',
    answer: 'XGBoost 的加速来源：(1) 块结构（Block Structure）/ 预排序：构建时对每列特征预排序后存入内存块，后续每轮分裂可复用排序结果，只需 O(k·n·log n) 一次排序；(2) 近似分裂算法：对连续特征分桶（候选分裂点），用加权分位数算法（Weighted Quantile Sketch）保证稀疏候选集近似精确最优，大幅减少分裂点搜索次数；(3) 缓存优化（Cache-Aware Access）：预排序数据按块存储，提升 CPU 缓存命中率，减少内存随机访问；(4) 核外计算（Out-of-Core）：利用磁盘存放压缩块（列块压缩），用独立线程预取和进解，突破内存限制可处理百亿级数据；(5) 列抽样：类似随机森林，既降内存也加速计算；(6) 并行化：特征内并行（在每层分裂时各特征独立计算）。此外自动处理缺失值、自带正则化防止过拟合也让工程调参更容易。',
    tags: ['XGBoost', 'GBDT', '工程优化', '加速'],
    subTopic: '集成学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-34',
    category: 'machine-learning',
    question: 'LightGBM 的直方图算法（Histogram）是什么？有什么优缺点？',
    answer: 'LightGBM 的核心优化是将连续特征离散化为 K 个桶（bins，如 255 个），构建直方图。分裂时不需要遍历所有样本，只需扫描 K 个桶找到最佳分裂点。优势：(1) 内存大幅降低——每个特征只需存 K 个整数的直方图而非所有值的梯度/二阶梯度；(2) 计算快——分裂增益计算从 O(n) 降到 O(K)，K 通常 256（用 uint8 存）；(3) 父节点直方图减兄弟节点直方图 = 当前节点直方图（做差加速），进一步减少计算。缺点：分桶相当于内置了轻微正则化效果，但理论上可能错过最优精确分裂点。LightGBM 配合 GOSS（梯度单边采样）和 EFB（互斥特征绑定）进一步优化。实践中 LightGBM 比 XGBoost 训练快 3-5 倍，尤其大数据高维度场景优势明显。但小数据或要求精确分裂时 XGBoost 的预排序可能更好。',
    tags: ['LightGBM', '直方图', '分桶', '加速'],
    subTopic: '集成学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-35',
    category: 'machine-learning',
    question: 'AdaBoost 的原理是什么？它和 GBDT 有什么不同？',
    answer: 'AdaBoost（自适应增强）是 Boosting 族先驱算法，原理：初始各样本权重相等，每轮训练弱分类器，根据分类错误率 εₘ 计算该分类器权重 αₘ = (1/2)·ln((1-εₘ)/εₘ)，然后更新样本权重——被错分的样本权重乘以 exp(αₘ) 放大，正确分类的乘以 exp(-αₘ) 缩小，下一轮分类器更关注之前被分错的样本。最终分类器 H(x) = sign(Σαₘ·hₘ(x)) 加权投票。与 GBDT 的关键区别：(1) AdaBoost 通过样本权重引导下一轮学习，GBDT 通过拟合残差（负梯度）引导；(2) AdaBoost 的 αₘ 由 εₘ 解析确定，GBDT 的学习率需手动调参；(3) AdaBoost 损失函数是指数损失 L(y,F)=e^{-yF}（数学上等价于用加权重采样的前向分步加法模型），GBDT 可自定义损失；(4) AdaBoost 对噪声和异常值特别敏感（错分样本权重指数增长），GBDT 通过步长shrinkage更鲁棒。实践中 AdaBoost 已较少使用，大多场景被 GBDT/XGBoost 取代。',
    tags: ['AdaBoost', '样本权重', 'Boosting', '指数损失'],
    subTopic: '集成学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-36',
    category: 'machine-learning',
    question: '什么是 Stacking 和 Blending 集成？它们的区别是什么？',
    answer: 'Stacking（堆叠泛化）是一种两层集成架构：第一层用多个不同类型的基模型（如 SVM + RF + XGBoost + LR）做 K 折交叉验证，每折输出验证集的预测值作为第二层训练数据，第二层元模型根据这些预测做最终预测。核心要点：第一层的 K 折交叉目的不是评估而是生成无泄漏的训练特征。Blending 是 Stacking 的简化版：Holdout 方法——划分训练集和验证集，基模型用训练集训练、用验证集生成预测作为元模型输入。区别：(1) Stacking 用 K 折交叉，数据利用率更高但慢；Blending 简单快速但有数据浪费和过拟合风险；(2) Stacking 基模型需统一 K 折划分，实现复杂；Blending 各模型独立训练。元模型常用简单模型（逻辑回归或带约束的线性回归）避免二次过拟合。Stacking 在 Kaggle 竞赛中极常见，是 Top 方案标配技术。',
    tags: ['Stacking', 'Blending', '集成学习', '元模型'],
    subTopic: '集成学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-37',
    category: 'machine-learning',
    question: 'CatBoost 相比 XGBoost/LightGBM 有哪些独特优势？',
    answer: 'CatBoost 由 Yandex 开发，三大亮点：(1) 原生类别特征处理——用 Ordered Target Statistics 对类别特征编码（按时间顺序递增计算目标统计量避免数据泄露），无需手动预处理，且用一种特殊组合方法（类别特征交叉）自动学习交互特征；(2) Ordered Boosting——解决传统 Boosting 的预测偏移（Prediction Shift）问题：先用随机排列对每个样本只使用"历史"数据训练模型，得到该样本的无偏残差估计作为目标，避免梯度偏差积累，类似在线学习的思路；(3) 对称树（Oblivious Decision Trees）——每层所有节点用同一个特征和同一分裂点，树是平衡的。优点：推理极快（可用 SIMD 加速）、模型小、适合生产部署和低延迟场景。缺点：表达能力比非对称树弱，需要更多棵树。CatBoost 调参简单（多使用默认参数）即可获得好效果，在类别特征占主导的比赛中常是黑马。',
    tags: ['CatBoost', '类别特征', 'Ordered Boosting', '对称树'],
    subTopic: '集成学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-38',
    category: 'machine-learning',
    question: '随机森林中的特征随机采样（Feature Subsampling / Feature Bagging）有什么作用？',
    answer: '特征随机采样指每棵决策树在每次分裂时只随机考虑 m 个特征而非全部 p 个特征。典型 m=√p（分类）或 p/3（回归）。作用：(1) 降低树之间的相关性——如果每棵树都用全部特征，强特征每次都被选为分裂点，所有树长得相似，Bagging 的方差降低效果被抵消。特征采样打破这种一致性，使树更多样化；(2) 减少过拟合——单棵树复杂度降低，集成后泛化能力提升；(3) 加速训练——每次分裂只需评估 m 个特征而非 p 个；(4) 给弱特征机会——某些中等特征在部分树中得以被选中，可能发现其他树遗漏的模式。极端情况 m=1 即为完全随机决策树（Extremely Randomized Trees），泛化能力进一步强但偏差也增大。特征采样是随机森林区别于 Bagging + 决策树的关键创新。',
    tags: ['随机森林', '特征采样', '相关性', '多样性'],
    subTopic: '集成学习',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-39',
    category: 'machine-learning',
    question: '梯度提升（Gradient Boosting）的数学原理是什么？为什么拟合负梯度？',
    answer: 'Gradient Boosting 是函数空间上的梯度下降。设我们希望构建强模型 F(x) 最小化损失 ΣL(yᵢ, F(xᵢ))。在第 m 轮，我们有了当前模型 F_{m-1}(x)，梯度的函数空间版本是 func. gradient：r_im = -[∂L(yᵢ, F(xᵢ))/∂F(xᵢ)]|_{F=F_{m-1}}，即损失函数对当前模型输出 F 的负偏导。在函数空间中，梯度本身是一个"方向函数" r_m(x)——它告诉我们模型输出应往哪个方向调整才能最快降低损失。因此我们用一个新的弱学习器 fₘ(x) 去拟合这个负梯度方向（即让 fₘ(x)≈r_m），然后沿该方向更新 Fₘ(x) = F_{m-1}(x) + ν·fₘ(x)（ν 为学习率/步长）。以平方损失 L=(y-F)² 为例，负梯度 r = y-F（残差），所以 GBDT 用树拟合残差是梯度提升的特例。该框架统一了任意可微损失函数下的 Boosting。',
    tags: ['梯度提升', '函数空间', '负梯度', '数学原理'],
    subTopic: '集成学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-40',
    category: 'machine-learning',
    question: '为什么集成的多样性（Diversity）很重要？如何提高集成的多样性？',
    answer: '集成学习效果好有两个关键条件：(1) 每个基学习器准确率至少略优于随机猜测；(2) 基学习器之间"犯不同的错误"即有多样性（Diversity）。多样性越高，投票/平均时错误互相抵消的余地越大，集成的泛化误差 Gang 越低。提高多样性的方法：(1) 数据层面——Bagging 的 Bootstrap 重采样（不同训练集）、Boosting 的序列性样本权重调整、交叉验证折的不同划分；(2) 特征层面——随机子空间法：每棵树用不同特征子集（随机森林本质就是特征采样+数据采样双随机）；(3) 算法层面——混合异质性模型（Stacking 的核心理念，如树+SVM+LR+NN，不同归纳偏置导致不同错误模式）；(4) 参数层面——同模型用不同超参数（不同深度、学习率的 XGBoost 混合）；(5) 随机种子初始化——神经网络不同随机权重初始化。但多样性不是越高越好，极端随机会导致 ACC 下降到不可接受水平，需在多样性-准确性间权衡。',
    tags: ['集成学习', '多样性', '泛化', '基学习器'],
    subTopic: '集成学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 特征工程 (8 questions: ml-41 ~ ml-48)
  // ============================================================
  {
    id: 'ml-41',
    category: 'machine-learning',
    question: '特征选择有哪些方法？比较 Filter、Wrapper、Embedded 三类方法。',
    answer: 'Filter（过滤式）：基于特征自身统计特性独立于模型筛选。方法：(a) 方差阈值——低方差特征信息量少可剔除；(b) 相关性分析——Pearson 相关系数或互信息，选与目标相关性强的；(c) 卡方检验——分类问题衡量特征与类别独立性；(d) 信息增益/基尼重要性。优点是计算快、可规模化，缺点是不考虑特征组合效应和模型适配性。Wrapper（包裹式）：以模型性能为标准搜索最优特征子集——前向搜索（每次加一个最好特征）、后向搜索（每次删一个最弱特征）、递归特征消除（RFE：训练模型→删最不重要→重训练）。优点是效果好考虑特征交互，缺点是计算量大 O(2^n)，小数据集可用。Embedded（嵌入式）：特征选择融入模型训练——Lasso 的 L1 正则化自动产生稀疏解；树模型的 MDI 重要性；带稀疏约束的 XGBoost(reg_lambda)。嵌入式的效率和效果兼顾，是工业界主流。实战推荐：先用 Filter 粗筛降维，再用树模型重要性/正则化自动选。',
    tags: ['特征选择', 'Filter', 'Wrapper', 'Embedded', 'RFE'],
    subTopic: '特征工程',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-42',
    category: 'machine-learning',
    question: '类别特征编码的方法有哪些？One-Hot、Label 和 Target Encoding 各适用于什么场景？',
    answer: 'One-Hot 编码：创建 k 个二值列（k=类别数），每行只有一个 1。适用：无序低基数（k≤10）类别如性别、国家。不适用：高基数（城市/品牌有上千类）导致维数爆炸、稀疏矩阵训练慢。Label 编码：将类别映射为递增整数 0,1,...,k-1。适用：有序类别（教育程度、评级）和树模型（XGBoost/LightGBM 对数值编码天然可找分裂点）。不适用：线性模型和神经网络（会人为引入类别间的虚假顺序和等距假设）。Target Encoding：将每个类别替换为其目标变量的某种统计量——mean(target)、smoothing 修正后的 target 均值：encoded = (n·mean(target)+global_mean·m)/(n+m)。适用：高基数类别且目标与类别有关系。风险：标签泄漏（用目标信息编码目标本身是数据泄露），必须用交叉验证或者内置正则化。实践中 Kaggle 选手常对高基数特征先用 Target Encoding 降维，再送 XGBoost。',
    tags: ['类别编码', 'One-Hot', 'Target Encoding', '数据预处理'],
    subTopic: '特征工程',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-43',
    category: 'machine-learning',
    question: '连续特征离散化（分箱/分桶）有什么好处？有哪些分箱方法？',
    answer: '分箱（Binning）将连续变量划分为离散区间，好处：(1) 处理异常值——极端值被归入边界桶，不像原始值那样扭曲模型；(2) 引入非线性——每个桶变成类别特征，线性模型可学习阶梯函数关系；(3) 提高稳定性——微小波动仅改变桶内值不影响桶标签；(4) 简化特征——原始连续分布复杂（如长尾收入分布），分箱后各桶更均衡。方法：(1) 等频分箱——每个桶样本数相等，对长尾分布好；(2) 等距分箱——每个桶区间宽度相同，适合均匀分布；(3) 基于决策树分箱——以目标为监督信号找最佳分裂点（类似 CART），效果最好但计算量大需防过拟合；(4) ChiMerge——从每值一个桶开始自底向上合并相邻卡方统计量（类分布相似）的桶。注意事项：分箱本质是信息损失（丢失了连续值的精细差异），树模型不需要人工分箱，逻辑回归/朴素贝叶斯适合。',
    tags: ['分箱', '离散化', '特征工程', 'ChiMerge'],
    subTopic: '特征工程',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-44',
    category: 'machine-learning',
    question: '什么是特征交叉（Feature Crossing）？为什么要手动构造交叉特征？',
    answer: '特征交叉是将两个及以上特征组合形成新特征，例如：年龄 × 性别、城市 × 品牌。核心动机是捕捉特征之间的交互效应（Interaction Effect），即一个特征的效应依赖于另一个特征的取值。线性模型 y=w₁x₁+w₂x₂+w₁₂x₁x₂ 中，x₁x₂ 的系数 w₁₂ 衡量的就是交互效应。不引入交叉特征时，线性模型只能学习线性可加关系，遗漏大量信息。深层原因：决策边界在特征空间可能是非轴对齐的（diagonal boundary），单特征不对齐的方向只有通过交叉特征才能表达。深度模型的隐式交叉——神经网络通过多层非线性变换自动学交叉，但宽度模型（LR/FM 等）需要显式构造。代价也是维数爆炸：n 特征两两交叉→C(n,2)个再配合类别特征→极高维稀疏特征矩阵，需要配合正则化（FTRL/L1）处理。推荐系统中 FM/FFM/DeepFM 系列模型就是对特征交叉的建模。',
    tags: ['特征交叉', '交互效应', '线性模型', '特征工程'],
    subTopic: '特征工程',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-45',
    category: 'machine-learning',
    question: '归一化（Min-Max Normalization）和标准化（Z-Score Standardization）有什么区别？什么时候用哪个？',
        answer: '归一化（Min-Max）公式：x\\\'=(x-min)/(max-min)，值域 [0,1]，无负数且不对分布做假设。优点是保留原始数据分布形状，适合需要非负输入的场景（如图像像素、神经网络用 sigmoid/tanh 激活时归一化到 [0,1] 或 [-1,1]）。缺点是极度依赖 min/max，有异常值时压缩大部分正常值到极小范围。标准化（Z-Score）公式：x\\\'=(x-μ)/σ，均值为 0 标准差为 1，不限制上下界。优点是对异常值相对鲁棒（超出 [-3,3] 仍有区分度），适用于大多数 ML 场景。选择原则：(1) 算法需要计算距离或梯度 → 标准化（PCA、KNN、SVM、神经网络、逻辑回归）；(2) 数据有明显上下界且需要保持分布 → 归一化；(3) 数据含异常值 → 可考虑 RobustScaler：x\\\'=(x-median)/IQR，用中位数和四分位距替代均值和标准差；(4) 树模型 → 两者都不需要。不确定时优先用标准化。',
    tags: ['归一化', '标准化', '特征缩放', '数据预处理'],
    subTopic: '特征工程',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-46',
    category: 'machine-learning',
    question: '如何处理高基数（High Cardinality）类别特征？',
    answer: '高基数指类别数量很大（如用户 ID 百万级、IP 地址千万级、产品 SKU）。One-Hot 不可行——维数爆炸 + 极度稀疏。处理策略：(1) Target Encoding（均值编码）——用目标变量均值编码，需交叉验证防泄漏，对回归/分类二值目标都适用，Kaggle 竞赛中几乎必用；(2) Count Encoding / Frequency Encoding——用类别出现频次或频率替代，信息量低但简单有效，尤其对低频类别的团状效应好；(3) Embedding 层——神经网络中将高基数类别映射到低维稠密向量（如 user_id → 16 维向量），自动学习语义相似关系，类似 Word2Vec 思路；(4) 分箱法——按频次或目标均值对类别聚类归并，如长尾类别统一标为"Other"；(5) CatBoost 的 Ordered TS——用时间序列递增的 target 统计量，天然防泄漏；(6) 特征哈希 (Hashing Trick) —— Hash(category)%D 映射到固定 D 维，简单但有哈希冲突，适合内存和实时推理要求高场景。',
    tags: ['高基数', '类别编码', 'Embedding', '特征哈希'],
    subTopic: '特征工程',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-47',
    category: 'machine-learning',
    question: '如何基于树模型计算特征重要性？这些重要性的局限性是什么？',
    answer: 'GBDT/XGBoost/LightGBM 均提供特征重要性：(1) Gain（平均增益）——特征在所有分裂中带来的平均损失减少量（增益）的加权和，反映特征的"贡献"；(2) Cover（平均覆盖率）——特征所分裂节点覆盖的样本总数或二阶梯度之和，反映特征的"使用范围"；(3) Weight（频次）——特征被选为分裂点的总次数，简单但偏向高基数特征；(4) SHAP values——基于 Shapley 值，在 GBDT 中可高效计算（TreeSHAP 算法）。主要局限性：(a) 自相关特征互相稀释——两个高度相关的特征 A 和 B，树随机选 A 或 B 分裂，A 和 B 的重要性会各自减半，而实际上缺任一个都不碍事；(b) Gain 高 ≠ 预测依赖——某些特征可能在节点分裂中被频繁用于分裂低深度节点，但实际对最终预测贡献不大；(c) 不能反映特征间的交互效应和方向。建议用排列重要性或 SHAP 作为补充验证。',
    tags: ['树模型', '特征重要性', 'SHAP', 'XGBoost'],
    subTopic: '特征工程',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-48',
    category: 'machine-learning',
    question: 'PCA 用于特征降维时有哪些注意事项和局限性？',
    answer: 'PCA 用于特征降维的核心缺陷：(1) 线性假设——PCA 只能提取线性结构，无法捕捉非线性流形（应用 Kernel PCA 或用自编码器替代）；(2) 方差 ≠ 信息——PCA 最大化投影方差，但某些高方差方向可能是噪声（光照变化）而低方差方向反而是关键信息，这是 PCA 在分类问题中可能丢掉判别信息的主因；(3) 可解释性丧失——新特征(PC1,PC2...)是原始特征的线性组合，无法说清每个 PC 的实际物理含义；(4) 对尺度敏感——不同量纲特征需先标准化，否则大方差特征主导第一主成分；(5) 无监督——不考虑目标变量，降维后对分类不一定最优，可改用 Partial Least Squares（PLS）替代；(6) 对异常值敏感——异常值会扭曲协方差矩阵估计，可用 Robust PCA。工程建议：(a) 保留方差≥90-95% 的主成分；(b) 将 PCA 作为数据预处理步骤联合交叉验证而非分离操作；(c) 对数据本质非线性的场景优先试 t-SNE 可视化或 Autoencoder 降维。',
    tags: ['PCA', '降维', '局限性', '特征工程'],
    subTopic: '特征工程',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 评估指标 (8 questions: ml-49 ~ ml-56)
  // ============================================================
  {
    id: 'ml-49',
    category: 'machine-learning',
    question: '准确率（Accuracy）、精确率（Precision）、召回率（Recall）和 F1 Score 分别是什么？如何计算？',
    answer: '四个指标由混淆矩阵的 TP/FP/FN/TN 定义：(1) Accuracy = (TP+TN)/(TP+FP+FN+TN)：预测正确的比例，最直观但类别不均衡时失效（99% 负样本全猜负 Accuracy=99%）；(2) Precision = TP/(TP+FP)：预测为正的样本中真正正的比例，"宁缺毋滥"——高精确率意味少误报（如在垃圾邮件检测中不要误删正常邮件）、侧重质量；(3) Recall = TP/(TP+FN)：真正正样本中被预测出的比例，"宁抓错不放"——高召回意味着少漏报（如癌症筛查不能漏诊）、侧重覆盖；(4) F1 Score = 2·(Precision·Recall)/(Precision+Recall)：P 和 R 的调和平均，均衡考虑两者。为何用调和平均不用算术平均？调和平均对小值更敏感，只有 P 和 R 都高时 F1 才高，杜绝一面极端高另一面很低。根据业务场景可调整 β 参数为 F_β = (1+β²)·(P·R)/(β²·P+R)，β>1 更重召回，β<1 更重精确率。',
    tags: ['准确率', '精确率', '召回率', 'F1', '混淆矩阵'],
    subTopic: '评估指标',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-50',
    category: 'machine-learning',
    question: 'ROC AUC 是什么？如何理解和计算？为什么它对类别不均衡不敏感？',
    answer: 'ROC（Receiver Operating Characteristic）曲线以 FPR（假阳性率）=FP/(FP+TN) 为横轴、TPR（真正率/召回率）=TP/(TP+FN) 为纵轴。通过逐步调低分类阈值（从严格→宽松），正样本越抓越多但负样本误判也增加，画出单调递增曲线。AUC（Area Under Curve）是 ROC 下面积：(0-1 之间)，随机分类=0.5，完美分类=1.0。AUC 的统计解释：随机选一个正样本和一个负样本，模型给正样本的打分高于负样本的概率。AUC = 1 则完全可分离；AUC=0.5 等同于随机。AUC 对类别不均衡不敏感的原因：FPR 和 TPR 的计算都除以各自类别的总数（FP/(FP+TN) 仅涉及负类，TP/(TP+FN) 仅涉及正类），即使正负样本比例从 1:1 变为 1:100，只要模型对两个类的排序能力不变，FPR/TPR 不会变，曲线位置不变。因此 AUC 是排序能力的纯净度量，适合不均衡问题。',
    tags: ['ROC', 'AUC', '排序能力', '不均衡'],
    subTopic: '评估指标',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-51',
    category: 'machine-learning',
    question: '混淆矩阵（Confusion Matrix）如何解读？从混淆矩阵能推导出哪些指标？',
    answer: '混淆矩阵是分类模型结果的二维表格，行=真实类别、列=预测类别，二分类有四个单元格：TP（真正例）——模型预测为正实际为正；FP（假正例）——预测为正实际为负（误报/第一类错误）；FN（假负例）——预测为负实际为正（漏报/第二类错误）；TN（真负例）——预测为负实际为负。从这四个值可导出几乎所有分类指标：(1) 准确率=(TP+TN)/总；(2) 精确率=TP/(TP+FP)；(3) 召回率/灵敏度=TP/(TP+FN)；(4) 特异度=TN/(TN+FP)，衡量对负类的识别能力；(5) F1=2TP/(2TP+FP+FN)；(6) MCC（马修斯相关系数）= (TP×TN-FP×FN)/√((TP+FP)(TP+FN)(TN+FP)(TN+FN))，[-1,1] 综合度量最全面，类别不均衡时比 F1 更可靠。多分类混淆矩阵扩展到 k×k 矩阵，对角线是各分类正确预测数，可通过宏平均/微平均计算多分类版本的 Precision/Recall/F1。混淆矩阵可视化（热力图）能快速定位哪些类别容易混淆。',
    tags: ['混淆矩阵', 'TP', 'FP', '分类指标'],
    subTopic: '评估指标',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-52',
    category: 'machine-learning',
    question: '多分类问题的宏平均（Macro）和微平均（Micro）有什么区别？',
    answer: '多分类问题中需将一个全局指标计算方式分两类。微平均（Micro）：先汇总所有类别的 TP/FP/FN/TN，再计算总体的 Precision/Recall/F1。公式：Micro-P = ΣTPᵢ/(ΣTPᵢ+ΣFPᵢ), Micro-R = ΣTPᵢ/(ΣTPᵢ+ΣFNᵢ)。因为每个样本贡献相同权重，受大类主导——某类样本越多，在指标中占比越大。当类别不均衡时微平均反映的是大类的性能。宏平均（Macro）：先独立计算每个类别的 Precisionᵢ/Recallᵢ/F1ᵢ，再取算术平均。即 Macro-P = (1/k)·ΣPᵢ。每个类别权重相等——小类与大类的指标影响力相同。当关注小类性能时用宏平均，当关注整体所有样本时用微平均。如果宏平均和微平均差距大（如 Macro-F1=0.3，Micro-F1=0.85），说明模型在小类上表现远差于大类。多分类 F1 也分 Macro-F1 和 Micro-F1，还有 Weighted-F1 = ΣwᵢF1ᵢ (wᵢ=各类样本占比)，是大类和微平均的折中。',
    tags: ['多分类', '宏平均', '微平均', 'Macro-F1', 'Micro-F1'],
    subTopic: '评估指标',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-53',
    category: 'machine-learning',
    question: '对数损失（Log Loss / Cross-Entropy Loss）是什么？它在什么场景下使用？',
    answer: 'Log Loss 是评估分类模型概率输出的标准指标，计算公式：LogLoss = -(1/N) Σ [yᵢ·log(pᵢ) + (1-yᵢ)·log(1-pᵢ)]，其中 yᵢ∈{0,1} 是真实标签，pᵢ 是模型预测为类别 1 的概率。Log Loss 不仅看你是否分类正确，还看你给的置信度有多准：如果真实是 1，你预测 0.99 的损失远小于预测 0.51；反之预测 0.01 非常自信地犯错，惩罚极大。多分类扩展：LogLoss = -(1/N) Σᵢ Σⱼ yᵢⱼ·log(pᵢⱼ)，j 遍历所有类别。使用场景：(1) Kaggle 竞赛中最常被要求的分类评估指标；(2) 需要概率校准的场景（如风险评估需精准概率而非仅类别）；(3) 用作分类模型的训练损失函数（交叉熵=Log Loss 训练时的叫法）。Log Loss 对极端概率值（p 接近 0 或 1）极其敏感——一个几乎等于 0 但实际为正的样本可以爆掉整体指标。能驱动模型做好的概率校准（Platt Scaling / Isotonic Regression）。',
    tags: ['对数损失', '交叉熵', 'Log Loss', '概率校准'],
    subTopic: '评估指标',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-54',
    category: 'machine-learning',
    question: 'RMSE（均方根误差）和 MAE（平均绝对误差）有什么区别？各自什么时候更适用？',
    answer: 'RMSE = √[(1/n)·Σ(ŷᵢ-yᵢ)²]，MAE = (1/n)·Σ|ŷᵢ-yᵢ|。核心差异：(1) RMSE 对大误差惩罚更重（平方放大），MAE 对所有误差等权。极端误差（如预测1000实际0）在 RMSE 中贡献 10⁶ 而 MAE 仅 1000，所以 RMSE 对异常值极为敏感，异常值多时 MAE 更鲁棒；(2) 数学性质：RMSE 可导（L2），在梯度下降优化中更友好（MAE 在 0 处不可导）；(3) RMSE≥MAE 且 RMSE=MAE 当且仅当所有误差相等。两者差距越大说明误差方差（或异常值）越大，通过 RMSE-MAE 差值可以评估误差分布的异质性。使用建议：(a) 希望预测值尽可能接近真实值（中值/无偏）且异常值已有控制 → RMSE；(b) 数据有大量无法去除的异常值 → MAE；(c) 优化过程需要用可微损失函数 → RMSE/MSE；(d) Huber Loss 做折中——误差小于 δ 时用 L2，大于 δ 时用 L1，远近通吃。',
    tags: ['RMSE', 'MAE', '回归评估', '鲁棒性'],
    subTopic: '评估指标',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-55',
    category: 'machine-learning',
    question: 'R²（决定系数）是什么？如何解读它的值？',
    answer: 'R² 衡量回归模型的拟合优度（goodness-of-fit），公式：R² = 1 - SS_res / SS_tot = 1 - Σ(yᵢ-ŷᵢ)² / Σ(yᵢ-ȳ)²。通俗讲：SS_tot（总平方和）衡量数据本身波动，SS_res（残差平方和）衡量模型未能解释的波动，二者比值说明"模型解释了多少比例的数据变异"。R² 范围 (-∞, 1]：(1) R²=1：完美拟合，所有残差=0；(2) R²=0.8：模型解释 80% 的方差；(3) R²=0：模型等同水平线 ȳ 预测，无解释力；(4) R²<0：模型比直接取均值预测还差（非线性模型或直接在不适当测试集评估时易出现）；(5) R²=负值也可能在带截距线性回归的测试集上出现（训练 R² 非负但测试可能负）。R² 并不完全等于 "好模型"——高 R² 不意味没有过拟合、低 R² 不意味模型无用（金融预测 R²=0.1 也是有价值的 alpha）。调整 R²（R²_adj）进一步以自由度修正，惩罚过多无关特征，更适用于模型选择。',
    tags: ['R²', '决定系数', '回归评估', '拟合优度'],
    subTopic: '评估指标',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-56',
    category: 'machine-learning',
    question: '精确率-召回率曲线（PR Curve）和 ROC 曲线有什么区别？什么时候用 PR 曲线？',
    answer: 'ROC 曲线画 TPR vs FPR（真正率对假正率），PR 曲线画 Precision vs Recall。关键区别：(1) ROC 对类别不均衡不敏感——FPR=FP/(FP+TN)，真正/假正率的分母都是各自类别的总数，即使正类从 10% 降到 1%，ROC 曲线位置不变但 PR 曲线骤降；(2) PR 曲线对不均衡敏感——Precision=TP/(TP+FP)，当负类远多过正类时，哪怕一点 FP 都能大幅拉低 Precision。结论：类别均衡时两者都能用 —— ROC 更全局，PR 给出更详细的阈值层面信息；类别极其不均衡时（正类 < 1%）→ 必须用 PR 曲线或 PR-AUC —— AUC-ROC 会给出虚高的乐观评估。PR-AUC 的范围从基线（正类占比）到 1，没有数学上偏好的属性但有区分度。工程上可以用 Average Precision (AP) = PR 曲线下的近似面积，在 sklearn 的 average_precision_score 即可计算。',
    tags: ['PR曲线', 'ROC', '精确率', '召回率', '不均衡'],
    subTopic: '评估指标',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 优化 (4 questions: ml-57 ~ ml-60)
  // ============================================================
  {
    id: 'ml-57',
    category: 'machine-learning',
    question: '请从数学上推导梯度下降的更新公式，并解释为什么是负梯度方向。',
    answer: '梯度下降目标是最小化损失函数 J(w)。泰勒一阶展开：J(w+Δw) ≈ J(w) + ∇J(w)ᵀ·Δw。我们希望每次更新让函数值降低最大，即最小化 J(w+Δw)。令 Δw = -η·∇J(w)（负梯度方向），一阶近似下 J(w-η·∇J) ≈ J(w) - η·‖∇J(w)‖² ≤ J(w)，即每一步严格下降（忽略高阶误差）。从方向导数的角度看，单位向量 d 的方向导数为 ∇J·d = ‖∇J‖·‖d‖·cosθ，当 cosθ=-1（即 d 与梯度反向成 180°）时方向导数最小（负值最大），所以负梯度是局部最陡下降方向。最终更新公式 w:=w - η·∇J(w)，其中 η 为学习率。梯度下降的局限性：(1) 一阶方法收敛慢（二阶方法如牛顿法用 Hessian 修正方向收敛更快但计算 O(n³) 不可行）；(2) 非凸函数可能陷入鞍点（Hessian 有正有负特征值）而非局部最小值，负梯度对鞍点也近似零。',
    tags: ['梯度下降', '泰勒展开', '方向导数', '优化推导'],
    subTopic: '优化',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-58',
    category: 'machine-learning',
    question: 'Batch GD、SGD（随机梯度下降）和 Mini-Batch SGD 有什么区别？Adam 有什么优点？',
    answer: 'Batch GD（批量梯度下降）：每次用全部训练数据计算梯度，梯度方向准确但每步计算 O(N·D)，内存爆炸、缓慢且不能在线学习；(2) SGD（随机梯度下降）：每次随机一个样本计算梯度，更新快 O(D)、可用于在线学习/流式数据，但梯度有极大噪声，更新路径震荡曲折收敛慢，也不利于并行计算；(3) Mini-Batch SGD：折中方案——每次随机 B 个样本（如 32/64/128/256），梯度有一定精度的同时仍可高效计算。GPU 友好的向量化运算提升吞吐量，是实践中标准做法。Adam 是自适应学习率算法，综合了动量（Momentum：维护历史梯度指数加权平均来平滑震荡加速收敛）和 RMSprop（自适应每个参数的学习率：按参数历史梯度平方根缩放步长），核心公式：mₜ=β₁·mₜ₋₁+(1-β₁)·gₜ，vₜ=β₂·vₜ₋₁+(1-β₂)·gₜ²，偏置校正后 m̂ₜ=mₜ/(1-β₁ᵗ), v̂ₜ=vₜ/(1-β₂ᵗ)，更新 wₜ=wₜ₋₁-α·m̂ₜ/(√v̂ₜ+ε)。Adam 优点：超参数鲁棒（默认 β₁=0.9, β₂=0.999, α=0.001 即可）、适用稀疏梯度、很少需要手动调学习率。AdamW 解耦 weight decay 进一步提升泛化。',
    tags: ['SGD', 'Adam', 'Mini-Batch', '优化器'],
    subTopic: '优化',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-59',
    category: 'machine-learning',
    question: '学习率调度（Learning Rate Scheduling）有哪些策略？为什么需要学习率衰减？',
    answer: '训练早期大步走快速逼近最优区域，后期小步走精细收敛到最小点——这是学习率衰减的核心直觉。常见策略：(1) 阶梯衰减（Step Decay）：每 N 个 epoch 学习率乘以因数 γ（如每 30 epoch γ=0.1），简单有效但需预设 epoch 数；(2) 指数衰减（Exponential Decay）：α=α₀·γ^epoch，平滑下降但后期衰减过快；(3) 余弦退火（Cosine Annealing）：αₜ=(α₀/2)·(1+cos(tπ/T))，从 α₀ 平滑降至 0，常配合 Warm Restart——周期结束后重置 α₀，多次余弦循环探索参数空间多个局部最优；(4) 线性衰减 + 预热（Warmup）：前几个 epoch 从小升到大（避免初期不稳定的高梯度和动态网络权重），再线性/余弦下降，Transformer 训练的标准做法；(5) Plateau 自适应衰减（ReduceLROnPlateau）：验证损失不再下降时自动降低学习率；(6) 循环学习率（Cyclical LR）：在一个范围内循环震幅学习率，帮助跳出局部最优和鞍点。不衰减则后期梯度下降会在最优值附近震荡不收敛。',
    tags: ['学习率', '调度', '余弦退火', 'warmup'],
    subTopic: '优化',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-60',
    category: 'machine-learning',
    question: 'MSE（均方误差）和交叉熵损失（Cross-Entropy）分别适用于什么场景？为什么逻辑回归不能用 MSE？',
    answer: 'MSE（均方误差）适用于回归任务，公式 L=(y-ŷ)²/2，输出连续值，损失函数对称且可微。交叉熵适用于分类任务，公式 L = -[y·log(ŷ)+(1-y)·log(1-ŷ)]，衡量两个概率分布的距离（似然最大化）。为何逻辑回归不能用 MSE？两个问题：(1) 梯度消失——对 sigmoid 输出 ŷ=σ(z) 的 MSE 损失求导得 ∂L/∂z = (ŷ-y)·ŷ·(1-ŷ)，当预测值接近 0 或 1 极度自信时，ŷ(1-ŷ)≈0，梯度接近 0 参数不更新，即使预测完全错误也学不动。而交叉熵损失梯度 ∂L/∂z = ŷ-y，梯度与错误量成正比，无消失问题；(2) 损失函数非凸——MSE + sigmoid 的组合损失曲面有多个局部最优（非凸），梯度下降可能卡在差的局部解，而交叉熵 + sigmoid 的组合是凸的（二分类时），收敛到全局最优。因此机器学习中分类用交叉熵、回归用 MSE 是经过理论验证的最优组合。',
    tags: ['MSE', '交叉熵', '梯度消失', '非凸'],
    subTopic: '优化',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 深度学习基础 (10 questions: ml-61 ~ ml-70)
  // ============================================================
  {
    id: 'ml-61',
    category: 'machine-learning',
    question: '激活函数有哪些？ReLU、Sigmoid、Tanh、GELU、Swish的优缺点对比？',
    answer: '激活函数为神经网络引入非线性变换，是深度学习的核心组件。Sigmoid：σ(x)=1/(1+e^{-x})，输出 (0,1)，优点是平滑可导、输出有界（适合门控机制），缺点包括梯度饱和——当 |x|>5 时梯度接近 0，导致深层网络梯度消失；输出非零中心（均值≈0.5），后续层输入的偏移会减慢收敛；指数运算计算量大。Tanh：tanh(x)=(e^x-e^{-x})/(e^x+e^{-x})，输出 (-1,1)，零中心解决了 Sigmoid 的非零中心问题，但仍存在梯度饱和。ReLU：f(x)=max(0,x)，优点是计算极简（仅比较操作）、非饱和性——x>0 时梯度恒为 1 解决梯度消失、生物学合理性（单侧抑制）；缺点包括 Dead ReLU 问题——神经元在负区间梯度为 0 且可能永远不再激活（尤其在较大学习率或负偏置时），输出无界可能导致后续层激活值爆炸。Leaky ReLU/PReLU：f(x)=max(αx,x)，α 为小斜率（如 0.01），解决 Dead ReLU 但增加计算量。GELU：f(x)=x·Φ(x)，Φ 是标准正态 CDF，近似为 x·σ(1.702x)，在 Transformer（BERT/GPT）中广泛使用，平滑且处处可导，比 ReLU 更符合经验上的激活分布。Swish/SiLU：f(x)=x·σ(βx)，当 β=1 时即 SiLU，具备自门控（self-gating）特性——输入越大门越开，平滑非单调（可能轻微下凸），在深层网络中常优于 ReLU。选择建议：CNN 默认用 ReLU/Leaky ReLU；Transformer 用 GELU；追求极致精度可试 Swish；永远避免在隐藏层用 Sigmoid/Tanh（仅用于输出层或门控）。',
    tags: ['激活函数', 'ReLU', 'Sigmoid', 'GELU', '梯度消失'],
    subTopic: '深度学习基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-62',
    category: 'machine-learning',
    question: '什么是梯度消失和梯度爆炸？如何解决？',
    answer: '梯度消失（Vanishing Gradient）指深层网络中反向传播时梯度逐层指数衰减，导致靠近输入的层参数几乎不更新，网络无法学到有效特征。数学原因：以 sigmoid 为例，∂σ/∂x = σ(1-σ) ≤ 0.25，经过 L 层连乘后梯度 ∝ (0.25)^L，L≥10 时梯度数量级 10^{-6}。梯度爆炸（Exploding Gradient）相反——梯度逐层指数放大，参数更新的步幅巨大导致损失震荡甚至数值溢出为 NaN，常见于 RNN 中权重矩阵的谱范数大于 1 时。解决方案分五类：(1) 激活函数——用 ReLU 替代 Sigmoid/Tanh（正区间梯度恒为 1 不衰减）；(2) 归一化层——Batch Normalization 将每层输出标准化到均值 0 方差 1，有效控制梯度尺度，Layer Normalization 常用于 Transformer/RNN；(3) 残差连接（Residual Connection）——通过跳跃连接允许梯度直接回传 y=F(x)+x，∂y/∂x=∂F/∂x+1，即使 ∂F/∂x≈0 梯度也不会消失；(4) 梯度裁剪（Gradient Clipping）——设定阈值将梯度范数截断到最大值，直接解决梯度爆炸，RNN 训练的标准操作；(5) 权重初始化——Xavier 初始化（tanh/sigmoid）和 Kaiming/He 初始化（ReLU）通过控制初始化方差 Var(w)=2/n_in 或 2/(n_in+n_out)，使每层输出的方差保持一致，防止前向传播中的信号衰减。此外 LSTM/GRU 的门控机制（遗忘门控制信息流）本质上也缓解了 RNN 的梯度消失。',
    tags: ['梯度消失', '梯度爆炸', 'Batch Normalization', '残差连接', '初始化'],
    subTopic: '深度学习基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-63',
    category: 'machine-learning',
    question: 'Batch Normalization的原理是什么？为什么有效？训练和推理阶段有何不同？',
    answer: 'Batch Normalization（BN）通过对每个 mini-batch 的每一维特征做标准化，使得该层输入的分布保持稳定。训练时步骤：(1) 计算 mini-batch 均值和方差：μ_B = (1/m)Σx_i，σ²_B = (1/m)Σ(x_i - μ_B)²；(2) 标准化：x̂_i = (x_i - μ_B) / √(σ²_B + ε)；(3) 缩放和平移：y_i = γ·x̂_i + β，γ 和 β 是可学习参数恢复网络的表达能力（标准化会限制表示空间，γ 和 β 允许网络学习最优分布）。BN 有效的五个原因：(a) 减少内部协变量偏移（Internal Covariate Shift）——每层输入分布稳定，避免下层持续适应变化的输入，加速训练；(b) 平滑损失曲面——BN 使得损失函数的 Hessian 矩阵条件数降低、梯度更可预测，可使用更大学习率；(c) 正则化效果——mini-batch 的统计量带有噪声，类似 Dropout 引入随机性，减少对正则化的依赖；(d) 缓解梯度消失/爆炸——通过控制各层输出的尺度阻止信号在传播中指数衰减或放大；(e) 减弱对权重初始化的敏感性。推理阶段的区别：不再使用 mini-batch 计算均值和方差（单样本无法算 batch 统计量），而是使用训练期累积的全局移动平均 μ_running 和 σ²_running 替代。具体：训练时维护一个指数移动平均 μ_ema = α·μ_ema + (1-α)·μ_B，推理时直接使用 μ_ema 和 σ²_ema 做标准化，保证确定性输出。PyTorch 中 model.eval() 即切换到此模式。',
    tags: ['Batch Normalization', 'BN', '内部协变量偏移', '标准化', '推理'],
    subTopic: '深度学习基础',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-64',
    category: 'machine-learning',
    question: 'Dropout的原理是什么？为什么能防止过拟合？训练和推理时有何不同？',
    answer: 'Dropout 由 Hinton 等提出，训练时每个神经元以概率 p 随机"失活"（输出置零），p 通常取 0.2~0.5，每次前向传播的失活模式不同，相当于在采样不同的子网络。防止过拟合的核心机理有四种解释：(1) 集成学习视角——训练 2^n 个共享权重的子网络的隐式集成，推理时所有神经元都激活（相当于取子网络集合的几何平均），方差降低；(2) 降低神经元共适应（Co-adaptation）——神经元不能依赖特定其他神经元的存在，被迫学习更鲁棒、更独立的特征表示，类似有性繁殖中基因不依赖特定配偶；(3) 特征噪声正则化——Dropout 等价于在隐藏层注入乘性伯努利噪声，迫使网络在信息不完整时仍能做出正确预测，本质是数据增强在特征空间的推广；(4) 贝叶斯近似——Gal & Ghahramani 证明带 Dropout 的网络等价于深度高斯过程的变分推断近似。训练时：对每个 mini-batch 的每个神经元独立抽样 Bernoulli(p) 掩码，失活的神经元输出为 0，同时需按 1/(1-p) 缩放剩余神经元的输出（Inverted Dropout），保持该层总激活值的期望不变，避免推理时需要做额外缩放。推理时：Dropout 关闭，所有神经元参与计算但无需缩放（因为训练时已通过 Inverted Dropout 做了尺度补偿），直接输出确定性结果。实践中 Dropout 放在激活函数后或全连接层之间，通常不对卷积层使用高 Dropout 率（卷积层参数共享本身已有正则化效果）。',
    tags: ['Dropout', '过拟合', '共适应', '集成学习', '正则化'],
    subTopic: '深度学习基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-65',
    category: 'machine-learning',
    question: '什么是反向传播（Backpropagation）？请推导其数学过程。',
    answer: '反向传播是训练神经网络的核心算法，本质是利用链式法则高效计算损失函数对每个参数梯度的动态规划算法。推导流程：(1) 前向传播：输入 x 逐层通过线性变换 z^{[l]}=W^{[l]}·a^{[l-1]}+b^{[l]} 和激活函数 a^{[l]}=g^{[l]}(z^{[l]})，最终输出 a^{[L]}=ŷ，计算损失 L(y,ŷ)；(2) 反向传播从输出层开始反向递推：输出层误差 δ^{[L]} = ∂L/∂a^{[L]} ⊙ g^{[L]}\'(z^{[L]}) = (a^{[L]}-y)（当损失为交叉熵且激活为 softmax+sigmoid 时的简化形式）；(3) 隐藏层的误差通过链式法则反向传递：δ^{[l]} = [(W^{[l+1]})ᵀ·δ^{[l+1]}] ⊙ g^{[l]}\'(z^{[l]})，即后一层误差经过权重矩阵转置传播并乘以当前层激活函数导数；(4) 最终计算各参数的梯度：∂L/∂W^{[l]} = δ^{[l]}·(a^{[l-1]})ᵀ，∂L/∂b^{[l]} = δ^{[l]}。关键洞察：链式法则使得每层的局部梯度只需计算一次并存储，避免了重复计算指数级膨大的计算图。计算图（Computation Graph）视角更一般化：网络是一个有向无环图，反向传播从损失节点开始，按拓扑逆序遍历各节点计算局部梯度（local gradient）并与上游梯度相乘（chain rule），PyTorch/TensorFlow 的 autograd 引擎即基于此。时间复杂度 O(E)，E 为边数，与前向传播相同量级。',
    tags: ['反向传播', '链式法则', '梯度计算', '深度学习'],
    subTopic: '深度学习基础',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-66',
    category: 'machine-learning',
    question: 'CNN中的卷积层、池化层、全连接层各自的作用是什么？',
    answer: '卷积层（Convolutional Layer）是 CNN 的核心，通过一组可学习的卷积核（滤波器）在输入上滑动做局部加权求和运算。核心作用：(a) 局部连接（稀疏交互）——每个神经元仅连接输入的局部区域（感受野），参数数量与输入尺寸解耦，大幅降低参数量，提取局部特征如边缘/纹理/角点；(b) 参数共享——同一卷积核遍及整个输入，使得检测平移不变的特征成为可能，同时也进一步减少参数量；(c) 平移等变性——输入平移 → 输出特征图对应平移，等价性对分割/检测任务至关重要。池化层（Pooling Layer）：对特征图下采样降低空间维度的无参操作。两种主要方式——最大池化（Max Pooling）保留局部最大值，对微小平移和形变有鲁棒性，且梯度仅反向传播到最大值位置；平均池化（Average Pooling）取局部平均值，保留更多背景信息但弱化了显著特征。池化作用：(a) 降维减少计算量和内存；(b) 增大后续层的感受野（下采样后同样大小的卷积核覆盖更大原始区域）；(c) 提供局部平移不变性（微小位移导致的输出变化被池化窗口吸收）；(d) 防止过拟合（减少参数和特征图尺寸）。全连接层（Fully Connected Layer）：每个输出神经元与所有输入神经元连接，参数数量 = n_in × n_out + n_out。作用：将卷积/池化提取的分布式特征表示映射到最终输出空间（分类概率/回归值），可以看作全局特征混合器。在 CNN 最后通常接 1-2 层全连接层，现代架构如 ResNet/ConvNeXt 倾向于用全局平均池化（GAP）+ 单全连接替代多层全连接，进一步降低参数量。',
    tags: ['CNN', '卷积层', '池化层', '全连接层', '感受野'],
    subTopic: '深度学习基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-67',
    category: 'machine-learning',
    question: 'RNN、LSTM、GRU的区别是什么？LSTM的遗忘门、输入门、输出门如何工作？',
    answer: 'RNN（循环神经网络）通过隐藏状态 h_t = tanh(W·h_{t-1} + U·x_t + b) 在序列步骤间传递信息，本质是一个递推式的深度网络（时间维度上的深度）。致命缺陷：BPTT（时间反向传播）时梯度连乘导致梯度消失（长期依赖无法学习）或梯度爆炸。LSTM（长短期记忆网络）引入细胞状态 C_t（贯穿网络的"信息高速公路"）和三个门控机制——(a) 遗忘门：f_t = σ(W_f·[h_{t-1}, x_t] + b_f)，输出 (0,1) 向量决定细胞状态中哪些信息被丢弃，f_t≈0 则遗忘对应维度信息，是 LSTM 解决梯度消失的关键（当 f_t≈1 时梯度可以畅通无阻流过时间长链）；(b) 输入门：i_t = σ(W_i·[h_{t-1}, x_t] + b_i) 决定哪些新信息写入细胞状态，候选值 C̃_t = tanh(W_c·[h_{t-1}, x_t] + b_c) 生成候选记忆内容，细胞状态更新为 C_t = f_t ⊙ C_{t-1} + i_t ⊙ C̃_t；(c) 输出门：o_t = σ(W_o·[h_{t-1}, x_t] + b_o) 决定输出多少细胞状态内容，隐藏状态 h_t = o_t ⊙ tanh(C_t)。GRU（门控循环单元）是 LSTM 的简化版，合并遗忘门和输入门为更新门 z_t、用重置门 r_t 控制历史信息混合，无独立细胞状态仅用隐藏状态 h_t = (1-z_t) ⊙ h_{t-1} + z_t ⊙ h̃_t。LSTM vs GRU：LSTM 参数更多表达能力更强，适合大数据集；GRU 参数量少 1/3 训练快，小数据集表现常不输 LSTM。现代实践中两者逐渐被 Transformer（自注意力无递推结构）替代，但 LSTM 在低资源场景仍有竞争力。',
    tags: ['RNN', 'LSTM', 'GRU', '遗忘门', '细胞状态'],
    subTopic: '深度学习基础',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-68',
    category: 'machine-learning',
    question: '什么是残差网络（ResNet）？跳跃连接（Skip Connection）解决了什么问题？',
    answer: 'ResNet（残差网络）由何恺明等 2015 年提出，核心创新是引入了跳跃连接（残差连接）。一个残差块定义为 y = F(x, {W_i}) + x，其中 F 是待学习的残差映射（通常由 2-3 个卷积层+BN+ReLU 组成），x 通过恒等映射直接跳跃连接到输出。解决了三个层次的问题：(1) 退化问题（Degradation Problem）——传统直觉认为"网络越深效果越好"，但实验发现深层网络的训练误差反而高于浅层网络（非过拟合所致，而是优化困难）。残差连接使得深层网络至少可以学到恒等映射（当 F→0 时 y=x，网络退化为浅层），因此深层网络效果不劣于浅层是理论上可保障的；(2) 梯度消失——反向传播时 ∂y/∂x = ∂F/∂x + 1，梯度中恒等于 1 的分量确保即使 ∂F/∂x 很小也有梯度信号回传，使训练数百甚至上千层的网络成为可能（ResNet-152 只需 7.6 GFLOPS 远少于 VGG-19 的 19.6）；(3) 特征复用——跳跃连接允许浅层信息跳过中间变换直达深层，网络自动学习哪些层需要修改哪些信息可直接传递，类似 LSTM 遗忘门的思路。ResNet 的成功促使残差连接成为深度网络标配——Transformer 的 Add & Norm、DenseNet 的稠密连接、U-Net 的长跳跃连接等都是该思想的变体。没有残差连接，深度学习"深度"的威力将大打折扣。',
    tags: ['ResNet', '残差连接', '跳跃连接', '退化问题', '梯度消失'],
    subTopic: '深度学习基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-69',
    category: 'machine-learning',
    question: 'Transformer中的Multi-Head Attention为何比单头注意力更有效？',
    answer: 'Multi-Head Attention 将 Q（Query）、K（Key）、V（Value）通过 h 组独立线性变换投射到不同子空间 [Q_i = Q·W^Q_i, K_i = K·W^K_i, V_i = V·W^V_i]，每组并行计算缩放点积注意力 Attention(Q,K,V) = softmax(QK^T/√d_k)·V，最后拼接各组输出再线性变换：MultiHead = Concat(head_1,...,head_h)·W^O。比单头注意力更优的原因：(1) 多子空间的信息捕获——每个注意力头可以关注输入的不同方面：有的头专注于位置近邻的局部语法结构（如形容词-名词搭配），有的头捕捉长距离的语义关系（如指代消解），有的头学习特定任务模式（如主谓一致），类似 CNN 中不同卷积核提取不同特征；(2) 集成与鲁棒性——多头相当于在注意力机制内做了一个浅集成，不同初始化导致各头收敛到不同的注意力模式，组合后平均了噪声、提高了表示鲁棒性；(3) 表达能力增强——单头注意力的输出空间受限于 QK^T 的表达能力，多头通过增加参数和子空间投影大幅扩展了表达空间，同时单头的维度 d_k = d_model/h 较小，总计算量与单头大维度相近（均为 O(n²·d)）；(4) 可解释性——不同头学到的注意力模式可以可视化分析（如 BERT 某些头负责位置关系、某些头负责语义关系），提供模型行为洞察。实验上 h=8 或 16 时效果最佳，再增多头收益递减。多头注意力是 Transformer 在 NLP/CV 等各领域取得突破的核心机制。',
    tags: ['Transformer', 'Multi-Head Attention', '自注意力', '注意力机制'],
    subTopic: '深度学习基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-70',
    category: 'machine-learning',
    question: '什么是迁移学习（Transfer Learning）？在深度学习中的常见策略有哪些（微调、特征提取、冻结层等）？',
    answer: '迁移学习（Transfer Learning）利用在大规模数据集（如 ImageNet/TEXT）上预训练的模型来解决目标任务，尤其当目标任务数据稀缺时效果显著。理论基础：深度网络的浅层学习通用特征（边缘/纹理/语法结构），深层学习任务特定的高级语义，前者可以在不同任务间迁移。常见策略：(1) 特征提取（Feature Extraction）——冻结预训练模型的所有卷积层/编码器权重作为固定的特征映射器，仅训练新加的分类器/任务头。适用：目标任务数据极少（几百到几千样本）且与源领域相似；(2) 微调（Fine-tuning）——加载预训练权重后，解冻全部或部分层的权重，用较小的学习率（通常为预训练时的 1/10~1/100）在整个目标数据上继续训练。适用：目标数据量中等（几千到几万），通常微调最后几层效果最好，因为浅层特征更通用；(3) 逐层解冻（Progressive / Discriminative Fine-tuning）——从顶层开始逐层解冻训练，每解冻一层训练若干 epoch 再解冻下一层，避免同时更新所有参数导致灾难性遗忘（Catastrophic Forgetting）。ULMFiT 首次系统化了此策略；(4) 冻结+部分微调——冻结前 k 层，微调后 l 层，在计算量和效果间取折中；(5) 多任务学习的迁移——同时训练多个相关任务共享底层表示，知识在任务间交叉迁移。实践中关键技巧：(a) 学习率应小于预训练阶段；(b) 使用 warmup 逐渐增加学习率；(c) 当源域和目标域差距大时（如自然图像→医学影像），微调的有效性会下降，需重新评估。迁移学习是现代 CV/NLP 工程的默认范式——几乎没有公司从随机初始化训练 ResNet/BERT。',
    tags: ['迁移学习', '微调', '特征提取', '预训练', '深度学习'],
    subTopic: '深度学习基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 模型评估与调优 (10 questions: ml-71 ~ ml-80)
  // ============================================================
  {
    id: 'ml-71',
    category: 'machine-learning',
    question: '什么是超参数调优（Hyperparameter Tuning）？网格搜索 vs 随机搜索 vs 贝叶斯优化的对比？',
    answer: '超参数是模型训练前设定的参数（如学习率、正则化强度、树深度、dropout 率），不能通过梯度下降自动学习，需要通过验证集性能人工或自动选择最优组合。三种主流方法对比：(1) 网格搜索（Grid Search）——为每个超参数预设离散候选值，遍历所有组合。优点：简单可并行、穷举保证找到格点内最优、可复现；缺点：维数灾难（m 个参数各 n 个候选值 → O(n^m) 指数规模），大部分算力浪费在无关组合上，对连续超参数离散化可能漏掉最优解。当超参数 ≤ 3 个可用，否则不可行。(2) 随机搜索（Random Search）——在超参数空间中随机采样组合，通常从对数均匀分布或均匀分布中采样。优点：与维度无关，可设任意多的采样次数；能比网格搜索更高效——当只有少数几个超参数真正重要时（Bergstra & Bengio 2012 证明随机搜索有更高概率在同等算力下找到更优解），适合高维空间探索；缺点：不可复现（除非固定种子），可能错过小块最优区域。(3) 贝叶斯优化（Bayesian Optimization）——使用概率代理模型（通常为高斯过程 GP 或 Tree-structured Parzen Estimator/TPE）建模"超参数→验证得分"的函数，并基于采集函数（Acquisition Function，如 EI/Expected Improvement）智能选择下一个最值得评估的超参数组合。优点：样本效率极高（几次到几十次评估即可逼近最优，远超随机/网格搜索）；能处理连续和离散超参数，自动学习参数间的关系；缺点：每次评估需完整训练模型、串行决策（新参数依赖历史评估结果）、GP 的 O(n³) 复杂度限制了评估次数上限。实践中常用工具：Optuna（TPE+剪枝）、Hyperopt、Ray Tune。推荐先用随机搜索粗定位，再用贝叶斯优化精调。',
    tags: ['超参数调优', '网格搜索', '随机搜索', '贝叶斯优化', '高斯过程'],
    subTopic: '模型评估与调优',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-72',
    category: 'machine-learning',
    question: '如何处理类别不平衡（Class Imbalance）？从数据层面和算法层面分别讨论。',
    answer: '类别不平衡是指分类任务中各类样本数量差异悬殊（如欺诈检测中正负比 1:1000），直接训练会导致模型偏向多数类——把所有样本都预测为多数类也能获得高准确率，但少数类的召回率极低。(1) 数据层面——欠采样（Under-sampling）：删除多数类样本使类别平衡。方法包括随机欠采样（可能丢失关键样本）、NearMiss（保留距离少数类最近的多数类样本）、Tomek Links（移除边界模糊的多数类样本）、Edited Nearest Neighbors（删除与 K 近邻多数类别不一致的样本）。过采样（Over-sampling）：增加少数类样本。随机过采样（复制少数类样本→过拟合风险）、SMOTE（在少数类样本对的连线上随机插值生成新样本 x_new = x + rand(0,1)·(x_nn - x)，增加多样性但可能引入噪声）、Borderline-SMOTE（仅在分类边界的少数类样本处插值）、ADASYN（自适应合成——为更难学习的少数类样本生成更多合成样本）。混合方法：SMOTE + Tomek Links / ENN（先合成少数类再清洗分类边界的噪声样本）。(2) 算法层面（代价敏感学习）——类别权重（Class Weight）：在损失函数中对不同类别赋予不同权重，少数类样本的损失乘以更大的权重 ∝ 1/频率或自定义，大多数库（sklearn/XGBoost）的 class_weight=\'balanced\' 自动按逆频率加权；Focal Loss：L = -α(1-p_t)^γ·log(p_t)，通过 (1-p_t)^γ 因子降低易分类样本的权重（p_t 大→因子小），让模型聚焦难分类样本（多数为少数类）；阈值移动（Threshold Moving）：训练后调整分类阈值，用验证集上的 PR 曲线或 ROC 曲线找最优阈值而非默认 0.5；集成方法：EasyEnsemble（多次欠采样训练多个模型集成）和 BalanceCascade（级联式的欠采样模型串行）。评估时必须用 Precision/Recall/F1/AUC 替代 Accuracy。实践中常用 SMOTE + 类别权重组合，LightGBM 内置的 scale_pos_weight 参数设置正负样本权重比。',
    tags: ['类别不平衡', 'SMOTE', '欠采样', 'Focal Loss', '代价敏感'],
    subTopic: '模型评估与调优',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-73',
    category: 'machine-learning',
    question: '什么是模型可解释性？SHAP值和LIME的原理和区别？',
    answer: '模型可解释性（Model Interpretability）指理解模型为何做出某个预测的能力，在金融风控、医疗诊断、法律决策等高风险场景尤为重要。可分为内在可解释模型（线性回归、决策树）和事后解释方法（post-hoc）。SHAP（SHapley Additive exPlanations）基于博弈论中的 Shapley 值：将每个特征视为合作博弈中的"玩家"，SHAP 值衡量每个特征对预测的边际贡献在所有特征子集上的加权平均 φ_i = Σ_{S⊆N\\{i}} [|S|!(|N|-|S|-1)!/|N|!]·[f_{S∪{i}}(x_{S∪{i}}) - f_S(x_S)]。SHAP 满足三个理想性质——局部准确性（所有特征 SHAP 值之和 = 预测值 - 平均预测值）、缺失性（未出现特征 SHAP=0）、一致性（若模型改变使某特征边际贡献单调不降，SHAP 值也不降）。TreeSHAP 利用树结构在 O(TLD²) 时间复杂度内精确计算（T=树数 L=叶数 D=深度）。LIME（Local Interpretable Model-agnostic Explanations）通过在被解释样本附近采样噪声数据、用简单可解释模型（如线性回归）局部逼近黑盒决策边界：argmin_g L(f, g, π_x) + Ω(g)，π_x 是距离权重核（near=RBF），g 为简单模型。核心区别：(1) SHAP 有严格的理论基础（Shapley 值），LIME 是启发式局部近似；(2) SHAP 全局一致性优于 LIME（LIME 对选择采样范围和核带宽敏感，同一样本多次运行可能给出不同解释）；(3) 计算复杂度：TreeSHAP 对树模型极快（精确值），LIME 需在该点周围采样训练；(4) SHAP 提供多种全局可视化（特征重要性汇总图/依赖图），LIME 更适合快速单样本解释。实践中两者互补使用：先用 SHAP 做全局分析，再用 LIME 验证个别案例。',
    tags: ['模型可解释性', 'SHAP', 'LIME', 'Shapley值', '黑盒解释'],
    subTopic: '模型评估与调优',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-74',
    category: 'machine-learning',
    question: '什么是数据泄露（Data Leakage）？常见的泄露场景有哪些？如何检测和防止？',
    answer: '数据泄露（Data Leakage）是模型在训练时意外访问了"在真实推理时无法获取的信息"，导致验证/测试集上的性能虚高但上线后效果断崖式下跌，是 ML 工程中最危险的隐蔽错误之一。常见泄露场景：(1) 时间序列泄露——用未来信息预测过去（如用 "用户下个月的购买金额" 作为本月的特征），应在时间维度做严格的时间切割而非随机划分；(2) 预处理泄露——在划分训练集/测试集之前对整个数据集做标准化/归一化（均值方差包含了测试集信息），必须在划分后用训练集的统计量去标准化测试集；(3) 目标编码泄露——Target Encoding 时用全量数据（含测试集中的真实标签）计算每个类别的目标均值，标准做法是使用交叉验证的 out-of-fold 统计量替换；(4) 特征工程泄露——特征生成时无意间使用了目标信息：如"用户是否逾期"作为特征去预测违约率，或者从 ID/日期中提取出隐含的目标相关信息；(5) 组/行级泄露——同一用户/设备的多条记录同时出现在训练集和测试集中，应确保按组（用户级/会话级）划分而非按行随机划分；(6) 拼接泄露——从外部数据源拼接特征时使用了目标标签做关联；(7) 采样偏移——测试集与训练集分布不一致但在评估中未察觉。检测方法：(a) 特征重要性检查——若某个特征的排列重要性异常高且在业务推理时不该有此信息，需排查；(b) 时间穿越检查——验证集上的特征值是否有未来信息；(c) 消融测试——移除可疑特征后性能是否合理下降；(d) 上线前影子部署对比。防止原则：一切数据变换必须在训练集/验证集/测试集划分之后进行，时间序列采用 time-based split，目标编码使用 K 折交叉统计量，推理 Pipeline 与训练 Pipeline 严格一致。',
    tags: ['数据泄露', '预处理泄露', '时间序列', 'Target Encoding', 'MLOps'],
    subTopic: '模型评估与调优',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-75',
    category: 'machine-learning',
    question: '什么是Ablation Study（消融实验）？在设计实验和研究论文中如何使用？',
    answer: 'Ablation Study（消融实验）指系统地移除/替换模型或方法的某个组件，观察性能变化以量化该组件的贡献，类似"如果拿掉X，模型会变差多少？"。命名源于神经科学中的 ablation（切除脑区观测行为变化），在 ML 论文中是证明方法有效性的核心验证范式。使用方法：(1) 组件消融——逐步移除单个组件并测量性能下降（如从完整模型中去掉 BN 层/Dropout/残差连接/注意力机制中的位置编码等），下降越多说明该组件越关键；(2) 特征/输入消融——逐个移除输入特征或模态（如去掉文本只用图像 vs 多模态融合），衡量各模态/特征对最终性能的贡献比例；(3) 数据量消融——以不同比例（10%/25%/50%/100%）训练模型，画数据量-性能曲线，分析当前瓶颈是算法能力还是数据不足；(4) 架构搜索——用单头替代多头注意力、用 SGD 替代 Adam、调整层数/隐藏维度，研究各架构选择的影响；(5) 训练策略消融——如比较有无 warmup/学习率衰减/预训练的影响。消融实验的设计要点：(a) 保持其他条件不变（控制变量法），每次只改变一个因素，否则无法归因；(b) 使用相同的随机种子和评估协议确保可比；(c) 合理选择 Baseline——最好展现组件从 Baseline 逐步增强到最终模型的递增收益；(d) 统计显著性——小数据集上多次运行报告均值和标准差；(e) 消融表格是论文中最有力的说服工具——第一行是完整模型，后续每行展示移除某一组件后的性能下降，清晰直观。工程实践中消融实验用于模型优化决策——移除冗余组件降低推理延迟/内存。',
    tags: ['消融实验', '控制变量', '论文方法', '组件分析', '实验设计'],
    subTopic: '模型评估与调优',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-76',
    category: 'machine-learning',
    question: '在线学习（Online Learning）和批量学习（Batch Learning）的区别？各自适用什么场景？',
    answer: '批量学习（Batch Learning / Offline Learning）用固定训练集一次性训练模型，模型上线后不再更新，直到下一次重新全量训练部署。优点：训练稳定（全量数据的梯度准确）、模型生命周期可控、离线评估完整、版本管理容易；缺点：无法适应数据分布变化（概念漂移后效果衰减）、训练数据量大时耗时长、存储全部历史数据成本高。适用场景：ImageNet 图像分类、Kaggle 竞赛、数据分布长期稳定且无实时更新需求的场景。在线学习（Online Learning）一次处理一条或一小批样本（stream 数据），模型参数即时更新并丢弃样本，一次只"看"一遍数据。优点：(1) 实时适应数据分布变化（广告推荐/股票交易中用户兴趣/市场模式随时变）；(2) 内存友好——不需要存储历史数据（海量日志流无法全存）；(3) 低延迟——新数据到达即可影响模型；(4) 理论上可处理无限数据流。缺点：梯度噪声大不稳定、参数更新可能"遗忘"旧知识（Catastrophic Interference）、学习率需特别设计。代表算法：SGD 天然可在线学习；FTRL（Follow The Regularized Leader）是 Google 广告点击率预估中业界经典的在线学习算法，每个特征维度的学习率自适应调整；在线 Boosting、Hoeffding Tree 等。实践中常用混合方案：Mini-batch 在线学习——累积小批量样本后用 Mini-batch SGD 更新（平衡稳定性和实时性）；在线学习 + 离线全量校正——每日/每周用积累的最近数据做一次离线全量训练，平滑在线学习的噪声。',
    tags: ['在线学习', '批量学习', 'FTRL', '概念漂移', '流式数据'],
    subTopic: '模型评估与调优',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-77',
    category: 'machine-learning',
    question: '如何处理模型训练中的过拟合？至少列出8种方法并解释各自的原理。',
    answer: '过拟合的解决策略按机制分类：基于数据的方法：(1) 增加训练数据——最根本的方法，更多样本让模型更难"记住"噪声，数据增强（图像旋转/裁剪/颜色抖动、文本回译/同义词替换）在不收集新数据的条件下增加多样性；(2) 数据增强（Data Augmentation）——通过变换原始数据生成新训练样本，强制模型学到变换不变性，相当于在数据空间做了正则化。基于模型约束的方法：(3) L1/L2 正则化——在损失函数中添加 λ·Σ|w|（L1）或 λ·Σw²（L2），L1 产生稀疏解驱动部分权重为零（天然特征选择），L2 收缩所有权重使其接近零但不为零，等效于在贝叶斯框架下给权重施加先验分布约束；(4) Dropout——训练时随机失活神经元，等效于隐式集成大量子网络，降低神经元间共适应，推理时全部激活作为模型平均；(5) Batch Normalization——每层的标准化操作带有 mini-batch 随机噪声，有轻微正则化效果；(6) 早停（Early Stopping）——监控验证集损失，当不再下降时停止训练，防止模型在训练集上继续"死记硬背"。基于模型结构的方法：(7) 降低模型复杂度——减少层数/隐藏单元/树的深度/叶子数，限制模型容量，本质是控制方差；(8) 剪枝（Pruning，针对决策树）——预剪枝（训练时限制生长）和后剪枝（训练后删除冗余子树），防止树在噪声样本上过度分裂。其他方法：(9) 集成方法——Bagging/Random Forest 通过多模型平均降低方差；(10) 权重衰减（Weight Decay）——与 L2 正则化在 SGD 中数学等价，但 AdamW 中解耦权重衰减与自适应学习率更有效。实践中通常组合 2-3 种方法（如数据增强 + Dropout + 早停），效果远超单一方法。',
    tags: ['过拟合', '正则化', '数据增强', 'Dropout', '早停', '集成学习'],
    subTopic: '模型评估与调优',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-78',
    category: 'machine-learning',
    question: '什么是Early Stopping？它的工作原理是什么？为什么可以防止过拟合？',
    answer: 'Early Stopping（早停）是一种通过监控验证集性能来动态决定训练终止时机的正则化技术。工作原理：将数据划分为训练集和验证集，每训练若干步（或每个 epoch）在验证集上评估一次损失或指标，记录历史最优验证损失对应的模型参数（checkpoint）；当验证损失在连续 p 个 epoch（称为 patience/容忍轮数）内不再创新低时，停止训练并恢复到历史最优参数。本质上，Early Stopping 利用了"训练误差单调下降、验证误差先降后升"的现象——训练初期模型学习数据中的通用模式（训练和验证误差同时下降），但随着训练继续，模型开始"记忆"训练集中的噪声和特殊模式（训练误差续降、验证误差始升），早停即在验证误差上升前截断训练。为何能防止过拟合：(a) 限制有效训练轮数，相当于在参数空间中以初始权重为中心构造了一个限制区域（L2 正则化在参数空间中的等价效果——早停以时间维度限制了参数搜索半径）；(b) 相比固定训练 epoch 数，早停自适应数据集和模型复杂度的差异——简单数据自动早停节省算力，复杂数据自动多训练。实践要点：(a) patience 设置 10-20 epoch（太小可能错过暂时的验证损失震荡，太大延迟早停收益）；(b) 配合学习率衰减使用效果更佳；(c) 最终模型不应使用早停的 checkpoint（那是验证集上的最优，有信息泄露），应在早停确定最佳训练轮数后，在全量训练数据上重新训练相同轮数。',
    tags: ['Early Stopping', '早停', '验证集', '过拟合', '正则化'],
    subTopic: '模型评估与调优',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-79',
    category: 'machine-learning',
    question: '什么是模型校准（Model Calibration）？为什么需要校准？可靠性曲线和预期校准误差（ECE）是什么？',
    answer: '模型校准（Model Calibration）指模型的预测概率与实际发生频率一致。一个模型是完美校准的，当且仅当：对所有预测为 p 概率的样本，实际正类比率恰等于 p。例如模型预测"违约概率 80%" 的一组用户中，恰好有 80% 真正违约。为何需要校准：(1) 风险评估需要准确概率（如贷款审批需精确违约率定价）；(2) 集成模型和现代神经网络虽然排序能力（AUC）强，但输出概率往往"过于自信"（极端趋近 0 或 1）——深层网络的置信度通常偏高，需要校准后才能解释；(3) 概率阈值决策依赖——不同阈值的业务成本不同。可靠性曲线（Reliability Diagram / Calibration Curve）：横轴=预测概率按区间分桶（如 [0,0.1), [0.1,0.2)...），纵轴=该桶内正类的实际比例。完美校准时曲线为对角线 y=x；曲线在对角线之上表示模型过于保守（低估概率/underconfident），之下表示过于自信（高估概率/overconfident）。预期校准误差 ECE（Expected Calibration Error）：ECE = Σ (|B_m|/N)·|acc(B_m) - conf(B_m)|，其中 B_m 是第 m 个桶，acc(B_m) 是桶内实际正类率，conf(B_m) 是桶内平均预测概率。ECE 是对可靠性曲线的量化总结，值越小校准越好。校准方法：(a) Platt Scaling——对模型输出的 logits 做逻辑回归校准，仅引入两个参数；(b) Isotonic Regression——非参数阶梯函数拟合，更灵活但需要更多校准数据且可能过拟合；(c) Temperature Scaling——对分类 logits 除以温度参数 T：softmax(z/T)，T>1 降低置信度、T<1 提高，仅需一个参数，深度学习中最常用。',
    tags: ['模型校准', 'ECE', '可靠性曲线', 'Temperature Scaling', '概率校准'],
    subTopic: '模型评估与调优',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-80',
    category: 'machine-learning',
    question: '如何比较两个模型的性能？统计检验（如McNemar检验、5x2交叉验证t检验）在模型比较中的作用？',
    answer: '仅凭单次随机划分的评估指标高低不足以断言一个模型优于另一个——差异可能来自随机波动。统计检验量化了"差异是否显著"的概率。(1) McNemar 检验——配对分类器比较的非参数检验，构建 2×2 列联表：模型 A 和 B 同时对同一样本做预测，计数 A 对 B 错（n_01）和 A 错 B 对（n_10），忽略两者都对/错的样本。零假设：两模型错误率相同。检验统计量 χ² = (|n_01 - n_10| - 1)² / (n_01 + n_10) 或精确二项检验：p-value = P(Bin(n_01+n_10, 0.5) ≤ min(n_01,n_10))。优点：简单直观、不需要多次训练；缺点：仅用单次划分，不代表数据的抽样变异性。(2) 5×2 交叉验证配对 t 检验——Dietterich 提出，执行 5 次 2 折交叉验证，每次随机分半得两折上的性能差 d_i^{(j)}（j=1,2 表示两折，i=1,...,5 表示第 i 次划分）。计算第 1 次划分第 1 折的性能差 d_1^{(1)} 及其方差估计 s² = (d_1^{(1)} - d_1^{(2)})² + ... + (d_5^{(1)} - d_5^{(2)})²，t 统计量 = d_1^{(1)} / √(s²/5)，自由度为 5。优点：控制了多次评估中的重叠数据带来的方差低估问题（简单 K 折 CV 的 t 检验因数据重叠会高估显著性）。一般流程建议：(a) 先用 K 折交叉验证（K=10）获得各折性能差的均值及标准差；(b) 使用配对 t 检验或 Wilcoxon 符号秩检验（无需假设正态分布）；(c) 报告效应量（Cohen d）和置信区间而非仅 p 值——p<0.05 仅说明"有差异"，效应量说明"差异有多大"；(d) 多模型比较时用 Friedman 检验 + Nemenyi post-hoc 控制整体显著性水平（避免多重比较的 Family-wise Error）。工程中若性能差异小于业务阈值（如准确率差 < 0.1%），即使统计显著也可能无实用价值。',
    tags: ['统计检验', 'McNemar', '交叉验证', 't检验', '模型比较'],
    subTopic: '模型评估与调优',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 模型部署与MLOps (5 questions: ml-81 ~ ml-85)
  // ============================================================
  {
    id: 'ml-81',
    category: 'machine-learning',
    question: '模型部署有哪些常见方式？（在线API、批处理、边缘部署、嵌入式部署）各自的优缺点？',
    answer: '(1) 在线 API 部署（Real-time Serving）：将模型封装为 REST/gRPC 微服务（如 TensorFlow Serving/TorchServe/Triton Inference Server），客户端通过 HTTP 请求发送单条或多条数据，模型即时返回预测结果。优点：延迟低（毫秒级），支持在线推理/个性化推荐/实时风控；缺点：需持续服务器运维、并发压力大时需弹性伸缩（K8s HPA）、GPU 成本高。(2) 批处理部署（Batch Inference）：定期（每小时/每天）对积攒的大量数据做离线批量预测，结果写入数据库/特征存储供后续使用。优点：计算效率高（GPU 跑满无空闲）、容错易重试、成本低于在线服务；缺点：延迟高（T+1），不能支持实时场景。典型应用：夜间批量跑反欺诈模型更新风险评分、T+1 推荐结果生成。(3) 边缘部署（Edge Deployment）：模型运行在终端设备上（手机/摄像头/IoT 设备/车载芯片），数据不出设备本地推理。优点：超低延迟（无需网络往返）、离线可用、数据隐私保护（敏感数据不传云端）；缺点：设备算力和内存受限（模型必须做量化/剪枝/蒸馏压缩）、更新模型麻烦（需 OTA 推送）。典型应用：手机人脸解锁、自动驾驶感知、智能音箱唤醒词检测。(4) 嵌入式部署（Embedded / On-Device）：类似边缘部署但更轻量化，通常针对 MCU/低功耗芯片，模型以 C/C++ 编译进固件（如 TensorFlow Lite Micro）。优点：极低功耗、实时性高；缺点：模型容量极度受限（仅支持微型模型）、开发门槛高。部署选型指南：(a) 需求延迟 < 50ms + 大流量 → 在线 API + GPU；(b) 需求 T+1 报表/离线预测 → 批处理；(c) 数据隐私/网络不稳定 → 边缘部署；(d) 传感器实时信号处理 → 嵌入式。大厂常采用分层部署：在线 API 服务高价值用户 + 批处理覆盖长尾。',
    tags: ['模型部署', '在线推理', '批处理', '边缘部署', 'MLOps'],
    subTopic: '模型部署与MLOps',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-82',
    category: 'machine-learning',
    question: '什么是模型服务（Model Serving）？如何处理高并发和低延迟的矛盾？',
    answer: '模型服务（Model Serving）是将训练好的模型发布为可被其他系统调用的在线预测服务的过程，核心挑战是保证高并发（Thousands of QPS）下的低延迟（p99 < 50-100ms）。优化策略：(1) 推理框架选型——TensorFlow Serving/Triton Inference Server 内置动态批处理（Dynamic Batching）：将短时间窗口内到达的多个独立请求合并为一个 batch 送入 GPU，batch 越大 GPU 利用率越高（矩阵运算的并行优势），但 batch 等太久会增加延迟——需在 batch 大小和等待时间之间权衡（max_batch_size + max_queue_delay 参数）；(2) 模型优化——量化（Quantization）：FP32 → INT8/FP16，推理速度 2-4 倍提升，精度损失 < 1%；剪枝（Pruning）：移除贡献小的权重/通道，减少计算量；知识蒸馏（Knowledge Distillation）：大模型（Teacher）教小模型（Student），保持较高精度的同时大幅缩小模型体积和推理时间；(3) 硬件加速——GPU Tensor Cores（混合精度 FP16）提供峰值计算性能，NVIDIA Triton 支持多模型并行在单 GPU 上分时复用缓解 GPU 利用率低的问题；CPU 推理用 ONNX Runtime/OpenVINO 等专门优化的推理引擎（Intel MKL-DNN/ARM Compute Library 底层加速）；(4) 服务架构优化——模型预热（Warmup）：启动时发送假请求触发模型加载和 GPU kernel JIT 编译，避免真实请求的冷启动延迟；模型副本（Replica）：Kubernetes HPA 根据 CPU/GPU 使用率或请求队列长度自动扩缩副本，多个 Pod 均摊流量；请求队列/背压（Backpressure）：当所有 worker 繁忙时，请求排队等待而非直接报错；缓存（Caching）：对相同/相似请求缓存结果（如推荐系统中热门物品的 Embedding 实时查）；(5) 模型图优化——TensorRT/ONNX 的层融合（Layer Fusion）将卷积+BN+ReLU 等连续层合并为一个 Kernel 减少显存带宽消耗。工程权衡：量化和蒸馏可能轻微降精度但大幅提 QPS；动态批处理在低 QPS 时帮倒忙（等不够一个 batch 反而延迟更高）需设置适度的 max_queue_delay。',
    tags: ['模型服务', '推理优化', '动态批处理', '量化', 'GPU推理'],
    subTopic: '模型部署与MLOps',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-83',
    category: 'machine-learning',
    question: '什么是概念漂移（Concept Drift）和数据漂移（Data Drift）？如何监控和应对？',
    answer: '数据漂移（Data Drift）指模型输入特征 X 的分布发生变化，即 P(X) 变了但 P(Y|X) 不变。例如：电商用户平均年龄上升（人口结构变化），但"年龄→购买概率"的关系没变。概念漂移（Concept Drift）指 P(Y|X) 即特征到标签的映射关系发生改变，X 不变但同样特征的标签含义变了。例如：同样"浏览某手机 3 次"，半年前意味着"高意向"（高购买概率），现在该手机已过时，同样行为意味着"随便逛逛"（低购买概率），同样的 X 对应不同的 Y。实际场景中两者常同时发生（如疫情期间消费模式剧变）。监控方法：(a) 数据漂移监测——使用 KS 检验/卡方检验/JSD（Jensen-Shannon 散度）比较训练集和线上最新数据的特征分布差异，对每个特征设置漂移阈值告警；或训练"区分训练数据 vs 线上数据"的二分类器，AUC 越接近 0.5 说明两分布越相似。(b) 概念漂移监测——跟踪线上预测的准确率/F1 变化（需有延迟标签——即事件发生后一段时间才能标注，如用户 30 天后是否真正违约）；或监控预测分数的分布变化（如平均预测概率突然上升/下降可能暗示概念变化）；(c) 分群监控——对关键切片（高价值用户/新用户/不同地区）单独监控，避免整体指标被大群掩盖。应对策略：(1) 定期重训练（Fixed Retraining）——每周/每月用最近数据全量重训（简单但慢，可能赶不上漂移速度）；(2) 在线学习——模型对新数据即时更新（FTRL/SGD），适合高频快速漂移；但风险是不能控制模型质量退化，需配合人工审核或自动化回滚；(3) 触发式重训练——监测到漂移告警时自动触发的条件训练（Conditional Retraining）；(4) 多模型竞争——同时维护 A/B 两组模型，新模型的数据可在线评估决定是否替换；(5) 增量学习（Continual Learning）——不遗忘旧知识的情况下增量吸收新知识，需引入正则化或数据回放机制防止灾难性遗忘。实践中推荐：多层次监控面板 + 自动化告警 + 人工审核 + 定期重训练流水线的闭环体系。',
    tags: ['概念漂移', '数据漂移', 'ML监控', '模型衰退', 'MLOps'],
    subTopic: '模型部署与MLOps',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-84',
    category: 'machine-learning',
    question: '什么是特征存储（Feature Store）？在ML系统中解决什么问题？（训练-服务特征一致性、特征复用）',
    answer: '特征存储（Feature Store）是机器学习基础设施中统一管理特征定义、计算、存储和服务的中央平台，由 Uber Michelangelo 率先大规模实践后成为 MLOps 标准组件（如 Feast/Tecton/Vertex AI Feature Store）。解决的核心问题：(1) 训练-服务一致性（Training-Serving Skew）——离线训练时的特征计算逻辑与在线推理时的特征计算逻辑不一致是模型上线翻车的头号元凶（如训练用 pandas 统计的上月均值，推理用 Java 实时计算的，实现差异导致模型行为迥异）。特征存储将特征定义和计算逻辑统一封装为"特征视图"，训练和推理从同一代码库/配置生成特征，彻底消除歧义；(2) 特征复用——不同团队/模型需要相同的特征（如电商的用户购买力特征可能同时被推荐、风控、营销模型使用），没有特征存储则每个团队各自实现采集/计算/维护管道，造成重复开发和特征定义冲突。特征存储提供特征注册和发现机制，一处定义多处使用；(3) 低延迟在线服务——特征存储内置在线存储层（如 Redis/DynamoDB），将历史离线计算的特征以键值对形式存储，推理时毫秒级获取用户/物品的最新特征向量；(4) 时序一致性（Point-in-time Correctness）——训练数据构造时需保证特征的时间戳早于标签时间戳（不能用"未来"特征预测"过去"标签），特征存储自动确保时间对齐，防止植入时间泄漏；(5) 特征血缘和治理——记录每个特征的来源、计算逻辑、变更历史和下游模型依赖，方便回溯排查错误和模型退役时的波及分析。工业界标准架构：离线存储（Parquet/Hive 存储历史特征快照）+ 在线存储（KV 存储服务实时特征）+ 特征注册中心（元数据管理），批流一体计算管道定期同步。',
    tags: ['Feature Store', '特征存储', 'MLOps', '训练服务一致性', '特征复用'],
    subTopic: '模型部署与MLOps',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-85',
    category: 'machine-learning',
    question: '什么是A/B测试在模型上线中的作用？影子部署（Shadow Deployment）、金丝雀部署（Canary Deployment）和蓝绿部署怎么选？',
    answer: 'A/B 测试（在线对照实验）将用户随机分流到对照组 A（当前模型）和实验组 B（新模型），收集在线业务指标（CTR/转化率/留存等）并通过假设检验判定新模型是否显著优于旧模型。这是从离线评估（AUC/F1）过渡到线上决策的科学方法，覆盖离线指标无法衡量的业务真实影响。影子部署（Shadow Deployment / Dark Launch）：新模型并行运行但预测结果不实际影响用户，仅记录预测日志用于与当前模型对比离线分析。优点：零风险，可以测试新模型在真实流量下的性能（延迟/吞吐/稳定性）和预测质量；缺点：无法评估业务指标（用户行为未受影响）。适用：新模型首次上线前的安全验证。金丝雀部署（Canary Deployment）：将少量流量（如 1%-5%）导向新模型，大部分流量仍走旧模型，监控期间若无异常指标（错误率、延迟、业务指标下降）则逐步扩大新模型流量比例至 100%，期间出现问题可立即回滚。优点：风险可控（爆炸半径小），验证真实业务效果；缺点：需设计流量路由和监控体系，初期新旧模型共存增加系统复杂度。蓝绿部署（Blue/Green Deployment）：同时维护两套完全独立的生产环境——蓝色=当前版本、绿色=新版本，新模型在绿色环境完成预热后通过负载均衡器一键切换全部流量。优点：切换和回滚极快（秒级），无中间状态；缺点：资源成本翻倍、数据库迁移复杂（两环境共用一库时需保证数据兼容）。选择指南：(a) 首次上线且对延迟/稳定性无把握 → 影子部署先行 → 金丝雀部署逐步放量；(b) 大模型架构重大变更（如从树模型切到深度学习）→ 蓝绿部署确保秒级回滚；(c) 持续迭代小模型且系统架构成熟 → 金丝雀部署（多数大厂推荐流水线的默认模式），配合自动回滚策略（错误率超阈值 X% 自动切回旧模型）。务必联动监控看板（Grafana/Datadog）实时追踪灰度过程中的关键指标。',
    tags: ['A/B测试', '影子部署', '金丝雀部署', '蓝绿部署', 'MLOps'],
    subTopic: '模型部署与MLOps',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 推荐系统 (5 questions: ml-86 ~ ml-90)
  // ============================================================
  {
    id: 'ml-86',
    category: 'machine-learning',
    question: '协同过滤（Collaborative Filtering）的User-based和Item-based有什么区别？各自适用场景？',
    answer: '协同过滤利用"相似用户喜欢相似物品"或"相似物品被相似用户喜欢"的集体行为做推荐，分为两种范式。(1) User-based CF：找到与目标用户兴趣相似的其他用户（近邻），推荐这些用户喜欢但目标用户尚未交互的物品。步骤：计算用户间相似度（皮尔逊相关系数/余弦相似度/调整余弦），选 K 个最相似用户，加权聚合他们的评分预测目标用户对新物品的兴趣。优点：推荐结果多样性和惊喜度更高（可能挖掘出跨品类的新颖物品）；缺点：用户量远超物品量时计算量巨大（O(m²)，m=用户数），用户兴趣变化时需重新计算相似度，新用户冷启动严重（无历史无法找相似用户）。适用场景：用户数不太多且物品快速更新（如新闻推荐——用户数百万级、文章日增数千篇，Item-based 更新成本高）。(2) Item-based CF：基于物品被相似用户喜爱程度的相似性。步骤：计算物品间的相似度（基于用户评分向量：被同样一群用户高分或低分的物品相似），对目标用户已交互的物品找出其相似物品作为推荐。优点：物品间相似度相对稳定（可以离线算好缓存），可解释性强（推荐某护肤品因为"你买过类似的另一款"），计算复杂度 O(n²)（n=物品数）通常小于用户量；缺点：推荐多样性较差（推荐的物品往往与已有偏好相似，缺少跨品类惊喜），新物品冷启动问题（无交互历史无法算物品相似度）。适用场景：物品数相对固定且用户量巨大的平台（如 Amazon 商品推荐——亿级用户、百万级商品，Item-based 计算量和更新频率友好）。工业界如今两步法为主：CF 仅作为召回阶段的候选生成方法之一（User-CF、Item-CF、矩阵分解召回等），排序阶段再用更复杂的模型（GBDT/DNN）融合多信号精排。',
    tags: ['协同过滤', 'User-based', 'Item-based', '推荐系统', '相似度'],
    subTopic: '推荐系统',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-87',
    category: 'machine-learning',
    question: '矩阵分解（Matrix Factorization / SVD）在推荐系统中的原理是什么？如何处理冷启动问题？',
    answer: '矩阵分解是协同过滤的隐语义模型（Latent Factor Model），核心思想是将用户-物品交互矩阵 R（稀疏 m×n，m 用户 n 物品）分解为两个低维稠密矩阵的乘积 R ≈ P·Qᵀ，其中 P∈R^{m×k} 是用户的隐向量矩阵（每行是一个用户的 k 维 Embedding），Q∈R^{n×k} 是物品的隐向量矩阵（每行是一个物品的 k 维 Embedding），k 是隐因子维度（通常 50-200）。用户 u 对物品 i 的预测评分 r̂_ui = p_u·q_i^T。优化目标：min Σ_{(u,i)∈known} (r_ui - p_u·q_i^T)² + λ(‖p_u‖² + ‖q_i‖²)，通过 SGD 或交替最小二乘法（ALS）求解。ALS 对每个用户/物品求解带正则的线性最小二乘，天然可并行，Spark MLlib 的实现即基于 ALS。处理冷启动问题：(a) 新用户冷启动——没有交互历史无法估计用户隐向量 p_u。方法：基于人口统计/设备特征的相似用户推荐（找同年龄段/同地区用户的 Top 热门物品）；请求用户在注册时选少数兴趣标签建立初始画像；将用户侧特征融入矩阵分解（如 Factorization Machine 用用户特征 XOR 物品特征的 FM 交叉项而非学习 ID Embedding）；(b) 新物品冷启动——无交互无法估计物品隐向量 q_i。方法：利用物品属性（标题/类目/价格）通过内容基方法估计初始隐向量（如用 CNN 提取图片特征 / BERT 提取文本描述特征，将得到的向量作为初始 q_i 并用 0 的置信度逐步更新）；先曝光给随机小部分活跃用户迅速积累反馈；(c) 混合推荐系统——CF + Content-Based + 热门/流行度推荐的多路融合，每种方法覆盖不同的冷启动缺口。进阶：SVD++ 在 SVD 基础上加入隐式反馈（浏览/点击等非评分行为），BPR（Bayesian Personalized Ranking）将矩阵分解从点估计改造为成对排序学习，更适合 Top-N 推荐。',
    tags: ['矩阵分解', 'SVD', 'ALS', '冷启动', '推荐系统'],
    subTopic: '推荐系统',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-88',
    category: 'machine-learning',
    question: '推荐系统中的召回（Recall）和排序（Ranking）阶段分别做什么？为什么需要两阶段架构？',
    answer: '现代推荐系统采用漏斗式多阶段架构，核心分为召回和排序两大阶段。召回（Recall / Matching / Candidate Generation）阶段：从百万/亿级别的物品全集中，用计算轻量但覆盖面广的算法快速筛选出几百到几千个候选物品。常用召回策略：(a) 协同过滤（User-CF/Item-CF）——相似用户/物品的协同信号；(b) 向量化召回——用户 Embedding 和物品 Embedding 近邻搜索（ANN，如 Faiss/ScaNN 的 HNSW/IVF 算法），双塔模型（用户塔/物品塔）是工业界主流方案；(c) 热门/流行度——全局 Top-N 热门，覆盖新用户冷启动；(d) 地理/时效召回（如附近商超、最新上架）。排序（Ranking / Scoring）阶段：对召回集内的每个候选物品，用复杂的、可融合大量上下文特征的模型（通常为 DNN + 大量特征工程）精确预估每个物品的 CTR（点击率）/CVR（转化率）/价值等分数，按分数降序截取 Top-K 展示。排序模型特征维度可达数百上千维，包括用户侧特征（年龄/性别/历史行为序列）、物品侧特征（价格/品牌/类别/CTR统计）、交叉特征（用户年龄段 × 物品类目）、上下文特征（时间/设备/网络状态）。为何需要两阶段而非直接对全集做精排：(a) 计算量——全集百万级物品，若每个都用深度网络推理，延迟和算力成本不可接受（一次请求需百万次 DNN Forward）；(b) 内存——所有物品的特征/Embedding 缓存于内存中（如 1000 万物品 × 512 维 × 4 字节 = 20GB+），召回阶段通过与索引近邻搜索仅需检索相关物品特征；(c) 多路互补——不同召回策略覆盖不同需求（协同召回=个性化、热门召回=兜底、内容召回=新物品），精排统一打分融合。实践中常在召回和排序间加粗排（Pre-Ranking）阶段，用轻量模型（如 LR / 简化双塔）将候选从数千进一步筛选到数百，再喂精排。',
    tags: ['推荐系统', '召回', '排序', '两阶段', '漏斗架构'],
    subTopic: '推荐系统',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-89',
    category: 'machine-learning',
    question: '什么是Embedding？在推荐系统中如何学习和使用用户/物品的Embedding？',
    answer: 'Embedding 是将高维稀疏的离散对象（用户 ID、物品 ID、类别标签等）映射到低维稠密连续向量空间的技术，使得语义上相似的对象在向量空间中距离相近。本质是一个可训练的查找表（Lookup Table）E∈R^{V×d}，V 为词汇量/类别数，d 为嵌入维度（通常 16-256），每个离散值被编码为一个 d 维浮点向量。如何学习：(1) 矩阵分解——SVD 直接分解交互矩阵得到用户/物品的隐向量；(2) Word2Vec / Item2Vec——将用户的行为序列视为"句子"，物品视为"词"，Skip-gram 模型预测上下文物品（CBOW 反之），训练 softmax 分类器后取出隐层权重即为物品 Embedding；(3) 双塔模型（Two-Tower / DSSM）——用户塔（编码用户历史行为/特征 → 用户向量 u）和物品塔（编码物品特征 → 物品向量 v），通过最大化正样本内积 u·v 和最小化负样本内积（负采样）训练，最终得到可直接做 ANN 检索的用户/物品 Embedding，YouTube DNN / DLRM 即该范式；(4) GNN（图神经网络）——将用户-物品交互关系建模为二部图，通过消息传递聚合邻域信息得到图的 Embedding。如何使用：(a) 召回——用户 Embedding 和所有物品 Embedding 通过 ANN（近似最近邻）索引检索 Top-K 最相似物品，延迟毫秒级完成百万/亿级候选筛选；(b) 特征输入——Embedding 作为排序模型的特征输入，替代原始高基数 ID 的 One-Hot，大幅降低输入维度同时携带语义信息；(c) 迁移——预训练好的 Embedding 可在相关任务间复用（如电商中的商品 Embedding 同时用于推荐和搜索排序）。工程核心：Embedding 需要频繁随机访问（查表）和全库 ANN 检索，通常部署在 GPU 缓存或专用向量数据库（Milvus/Weaviate）中。负采样策略（随机负样本/难负样本挖掘）对 Embedding 质量影响极大——难负样本驱动模型学习更精细的区分能力。',
    tags: ['Embedding', '向量化', '双塔模型', 'ANN', '推荐系统'],
    subTopic: '推荐系统',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-90',
    category: 'machine-learning',
    question: '如何评估推荐系统的效果？离线指标（Recall/Precision/NDCG）和在线指标（CTR/转化率/用户停留时长）分别有什么局限性？',
    answer: '推荐系统评估需结合离线、在线和长期效果三维度。(1) 离线指标（Offline Metrics）——在历史日志上模拟推荐效果：(a) Recall@K：在 Top-K 推荐列表中命中用户实际交互物品的比例（=命中数/用户实际交互数），关注"有多少用户需要的东西被覆盖到"；(b) Precision@K：Top-K 列表中相关物品的比例（=命中数/K），关注"推荐结果噪音率"，Precision 受 K 值影响大；(c) NDCG@K（归一化折损累积增益）：考虑排序位置权重的排序质量指标，位置越靠前的命中贡献越大，是目前最常用的综合离线指标，NDCG = DCG/IDCG，DCG = Σ_{i=1}^K (2^{rel_i}-1)/log₂(i+1)；(d) MRR（平均倒数排名）：首次命中物品排名的倒数的平均值 1/rank_{first_hit}；(e) Hit Rate@K：Top-K 中至少有一个命中的用户比例；(f) Coverage / Diversity / Novelty 等辅助指标。离线指标的局限性：只能评估"模型是否能推荐用户历史交互过的物品"，无法衡量"推荐用户未交互但实际会喜欢的新物品"；受日志偏向（exposure bias）影响——训练日志中只有系统曾经推荐过的物品的交互记录，未推荐的不代表用户不喜欢；离线高指标不等于线上好效果。(2) 在线指标（Online Metrics）——真实 A/B 测试中的业务指标：(a) CTR（点击率）=点击数/曝光数，衡量推荐吸引了多少注意，但容易被标题党/低俗内容刷高；(b) CVR（转化率）=转化数/点击数，衡量推荐质量（用户真实需求满足），但归因周期长（浏览后可能几天后才购买）；(c) 用户停留时长/人均浏览深度——反映推荐吸引力，但算法可能通过"信息茧房""低质但高热内容"拉高时长伤害用户体验；(d) 留存率——衡量推荐系统对用户长期回访的影响，是最重要的长线指标。各指标的权衡：CTR 和停留时长可能存在矛盾（高点击低质量内容导致快速跳出），不同阶段推荐应优化不同指标——新用户重探索（多样性/发现感），老用户重效率（转化/CTR）。最佳实践：离线指标用于模型快速迭代筛选，在线 A/B 实验做最终决策，长期留存/用户满意度调研做终极评判。',
    tags: ['推荐评估', 'NDCG', 'CTR', '离线指标', '在线指标'],
    subTopic: '推荐系统',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 生成模型 (8 questions: ml-91 ~ ml-98)
  // ============================================================
  {
    id: 'ml-91',
    category: 'machine-learning',
    question: 'GAN（生成对抗网络）的原理是什么？生成器和判别器如何博弈？',
    answer: 'GAN（Generative Adversarial Network）由Goodfellow 2014年提出，通过一个极小极大二人博弈框架训练生成模型。系统由两个网络组成：生成器G（Generator）——接收随机噪声z~p_z（通常高斯分布），输出伪造样本G(z)旨在逼近真实数据分布p_data；判别器D（Discriminator）——二分类器判断输入样本来自真实数据还是G生成。博弈过程可形式化为价值函数：min_G max_D V(D,G) = E_{x~p_data}[log D(x)] + E_{z~p_z}[log(1-D(G(z)))]。D的目标最大化该函数——对真实样本D(x)趋近1，对生成样本D(G(z))趋近0；G的目标最小化该函数——让D(G(z))趋近1，即骗过判别器。收敛的纳什均衡点：当p_G = p_data即生成分布完美复现数据分布时，判别器无法区分真伪（D(x)=0.5 everywhere），此时V(D,G) = -log 4 ≈ -1.386。训练采用交替优化——k步D优化、1步G优化（k通常为1~5），D更新k次给G提供更好的梯度信号。训练技巧：实际训练中用非饱和损失替代原始损失——G优化最大化log D(G(z))而非最小化log(1-D(G(z)))，提供更好的梯度（当D太强时D(G(z))≈0，log(1-x)在x=0附近梯度平坦导致G学不动）。GAN是生成模型历史上的里程碑，之后衍生出条件GAN（cGAN→根据条件生成特定类别）、DCGAN（CNN做GAN稳定训练）、WGAN（Wasserstein距离替代JS散度解决梯度消失）、CycleGAN（无配对图到图翻译）等众多变体。',
    tags: ['GAN', '生成对抗网络', '生成器', '判别器', '博弈论'],
    subTopic: '生成模型',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-92',
    category: 'machine-learning',
    question: 'GAN训练中常见的问题有哪些？（模式坍塌 Mode Collapse、训练不稳定等）如何缓解？',
    answer: 'GAN训练的四大核心难题：(1) 模式坍塌（Mode Collapse）——最严重的失败模式，生成器只学会生成数据分布中的少数几种模式而忽略其余。例如训练生成手写数字MNIST时，G只生成数字"1"和"7"而遗漏0/2/3/4/5/6/8/9，输出缺乏多样性。本质原因是G找到了一个能骗过当前D的局部最优解，但该解覆盖的数据模式非常有限。缓解方法：(a) Unrolled GAN——G训练时考虑D未来K步的更新，学习具有泛化性的生成策略；(b) Minibatch Discrimination——在D中加入跨样本特征统计量，允许D鉴别一个batch内样本是否太相似；(c) WGAN/WGAN-GP——Wasserstein距离是平滑可微的距离度量（不像JS散度在分布不相交时梯度为零），配合梯度惩罚约束1-Lipschitz条件，训练更稳定且能通过生成样本多样性检测模式坍塌。(2) 训练不稳定——GAN的参数在优化过程中剧烈震荡甚至发散，D与G交替训练极难平衡。原因：极小极大优化的非凸非解耦性——D更新改善G的目标但未必改善最终生成质量。缓解：(a) 谱归一化（Spectral Normalization）约束D的权重矩阵谱范数，强制1-Lipschitz条件，是SAGAN的关键创新；(b) 梯度惩罚（Gradient Penalty，WGAN-GP）替代权重裁剪，更平滑的约束；(c) 两时间尺度更新规则（TTUR）——D用更大学习率更快学习。(3) 梯度消失——D完美分离真假时D(x)→1,D(G(z))→0，JS散度梯度消失，G无法学习。缓解：WGAN用Wasserstein距离替代（处处可导），或使用最小二乘GAN损失。(4) 评估困难——无天然损失函数衡量生成质量，需依赖IS/FID等外部指标或人工视觉判断。',
    tags: ['GAN', '模式坍塌', '训练不稳定', 'WGAN', '梯度消失'],
    subTopic: '生成模型',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-93',
    category: 'machine-learning',
    question: 'VAE（变分自编码器）的原理是什么？重参数化技巧（Reparameterization Trick）解决了什么问题？',
    answer: 'VAE（Variational Autoencoder）由Kingma & Welling 2013年提出，融合变分推断和自编码器的深度生成模型。核心思想：假设潜在变量z服从简单先验分布（通常为标准正态N(0,I)），数据x通过似然p(x|z)从z生成。由于后验p(z|x)难以直接计算，用编码器q_φ(z|x)近似真实后验（推断网络），解码器p_θ(x|z)建模生成过程。优化目标是证据下界ELBO（Evidence Lower Bound）：L(θ,φ;x) = E_{q_φ(z|x)}[log p_θ(x|z)] - KL(q_φ(z|x) || p(z))。第一项是重建误差——希望z能解码出接近x的输出；第二项是KL散度正则项——迫使近似后验q_φ(z|x)接近先验p(z)=N(0,I)，若q_φ(z|x)是高斯N(μ(x),σ²(x))，KL散度有闭合形式：KL = -(1/2)·Σᵢ (1 + log σᵢ² - μᵢ² - σᵢ²)。重参数化技巧（Reparameterization Trick）解决的问题：z是随机变量，采样操作z~N(μ,σ)不可导（梯度无法通过随机节点反向传播）。核心技巧：将随机采样改写为确定性变换——z = μ + σ ⊙ ε，其中ε~N(0,I)是外部独立噪声变量。此时z的随机性完全转移到ε，对μ和σ的求导路径通畅，梯度可顺利反向传播。这使VAE可以用标准SGD端到端训练。生成过程：训练后不再需要编码器，直接从先验p(z)采样z，经解码器生成新样本。VAE优点是潜在空间连续光滑（KL正则迫使相近z生成相近x），支持插值生成（在两个z间线性插值可过渡生成），有坚实的概率理论基础。缺点：生成样本通常较模糊（原因可能是高斯似然假设导致的低维流形问题，或KL散度优化过程中的后验坍塌posterior collapse）。后续改进包括VQ-VAE（Vector Quantized VAE，用离散码本替代连续潜在空间）、β-VAE（调整KL权重β学习解耦表示）等。',
    tags: ['VAE', '变分自编码器', '重参数化', 'ELBO', 'KL散度'],
    subTopic: '生成模型',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-94',
    category: 'machine-learning',
    question: 'GAN和VAE在生成质量和潜在空间结构上的区别？什么时候选VAE什么时候选GAN？',
    answer: 'GAN和VAE代表了生成模型的两个极端范式。生成质量：(1) GAN——通过对抗训练直接优化生成样本的真实性，生成图像通常锐利细腻、细节丰富，可达到以假乱真的水平（如StyleGAN生成的人脸几乎无法分辨）。但GAN没有显式建模数据似然p(x)，无法直接评估某样本在模型下的概率。(2) VAE——优化ELBO等价于最大化似然下界，但高斯似然假设往往导致生成图像模糊（平均效应——对不确定区域取均值而非尖锐细节），生成质量通常不及GAN。不过VAE的训练更稳定可收敛到明确的局部最优，而GAN可能永无止境地振荡。潜在空间结构：(1) VAE——KL散度正则强制潜在空间连续平滑且接近正态分布：两个接近的z生成相似的x，向任意方向走潜在空间都能生成合理样本。插值效果优异——两个脸之间线性插值z生成的中间画面自然过渡。但潜在空间可能过平滑造成部分维度"坍塌"（posterior collapse，某些z维度被忽略不携带信息）。(2) GAN——潜在空间无显式正则化，z的各维度关系不受约束，插值可能不连贯甚至中途崩溃；但z中确实蕴含了语义方向（InfoGAN发现某些维度对应特定属性，StyleGAN的潜在空间有明确的风格分离）。选择指南：(a) 需要生成高真实感视觉内容（超分/风格迁移/创意设计）→ GAN或改进变体（StyleGAN/Diffusion）；(b) 需要概率建模和密度估计（异常检测——低p(x)即为异常，需要在给定模型下评估样本概率）→ VAE；(c) 需要学习可解释的解耦因子表示（下游任务如分类/回归需要结构化的潜在特征）→ VAE/β-VAE；(d) 需要稳定的训练过程和可靠的重建能力（压缩/去噪/修复）→ VAE；(e) 数据量少且需要避免模式坍塌→ VAE通常比GAN更鲁棒。实际上Diffusion Models已逐渐在生成质量和训练稳定性上同时超越两者，成为当前主流生成范式。',
    tags: ['GAN', 'VAE', '生成质量', '潜在空间', '生成模型对比'],
    subTopic: '生成模型',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-95',
    category: 'machine-learning',
    question: '扩散模型（Diffusion Models）的基本原理是什么？前向加噪和反向去噪的过程？',
    answer: '扩散模型（Diffusion Models）受非平衡热力学启发，通过扩散和逆扩散两个过程实现生成。前向扩散过程（Forward Process / Diffusion）是一个固定的（无参数）马尔可夫链，逐步向真实数据x_0加入高斯噪声：q(x_t | x_{t-1}) = N(x_t; √(1-β_t)·x_{t-1}, β_t·I)，其中β_t∈(0,1)是噪声调度参数（通常从β_1=10^{-4}线性增加到β_T=0.02，T≈1000步）。经过足够多步后，x_T近似完全噪声化（各向同性的标准正态分布N(0,I)）。通过重参数化可一步写出任意t时刻的条件分布：x_t = √(ᾱ_t)·x_0 + √(1-ᾱ_t)·ε，其中ᾱ_t = Π_{s=1}^t (1-β_s)，ε~N(0,I)。反向去噪过程（Reverse Process / Denoising）是学习的参数化高斯过程：p_θ(x_{t-1} | x_t) = N(x_{t-1}; μ_θ(x_t,t), σ²_t·I)，即训练一个神经网络（通常为U-Net架构）根据当前噪声图像x_t和时间步t预测去噪后的均值μ_θ。DDPM（Denoising Diffusion Probabilistic Models）发现μ_θ可由预测添加的噪声ε来间接表达，训练目标简化为去噪得分匹配（Denoising Score Matching）：L_simple = E_{t,x_0,ε}[‖ε - ε_θ(x_t, t)‖²]，即让模型预测在每个时间步加入的噪声ε。生成时从纯噪声x_T~N(0,I)开始，逐步用训练好的ε_θ去噪还原x_{T-1}, x_{T-2}, ..., x_0，最终得到逼真样本。扩散模型的核心优势：训练简单稳定（与GAN的对抗训练不同，仅需优化L2损失）、生成的样本多样性和保真度极高（DALL-E 2、Stable Diffusion、Midjourney皆基于扩散模型）、无模式坍塌问题。主要缺点是迭代采样慢（需T步串行去噪），后续DDIM通过去噪扩散隐式模型将采样步数从1000压缩到50-100步，以及蒸馏技术进一步加速。',
    tags: ['扩散模型', 'Diffusion', '去噪', 'DDPM', '生成模型'],
    subTopic: '生成模型',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-96',
    category: 'machine-learning',
    question: 'Stable Diffusion为什么叫"Stable"？它在潜在空间（Latent Space）中做扩散有什么好处？',
    answer: 'Stable Diffusion由Stability AI发布（基于Latent Diffusion Model，LDM论文），名称中的"Stable"强调的是在计算稳定性和训练代价上的突破——让扩散模型的训练和推理在消费级GPU（甚至手机）上变得可行。三大核心贡献：(1) 在潜在空间中扩散——传统扩散模型直接在高维像素空间（如512×512×3=786432维）中做扩散，计算和内存代价巨大，训练和采样均极其缓慢。Stable Diffusion使用预训练的VQ-VAE（或连续VAE）感知压缩编码器E将图像x压缩到低维潜在表示z=E(x)（如64×64×4=16384维，压缩因子约48倍），编码器和解码器在预训练后冻结，扩散过程完全在潜在空间中执行。好处：计算量大幅降低——维度减少约50倍，扩散的采样速度相比像素级扩散提升10-100倍；潜空间天然滤除了高频（不重要的纹理细节）噪声，模型专注于图像语义内容生成。(2) 条件生成能力——通过交叉注意力（Cross-Attention）机制将文本/图像等条件信息注入U-Net的去噪主干：对每个去噪层，注意力Q来自空间特征，K和V来自文本编码器（CLIP的文本编码器）生成的Conditioning Vector。这使得模型能根据任意文本提示精确控制生成内容，即text-to-image。(3) 开源生态——Stable Diffusion是首个大规模开源的高质量扩散模型，引爆了AIGC社区生态（ControlNet、LoRA、DreamBooth等后续工作）。潜在扩散的局限：依赖预训练VAE的压缩质量（VAE的重建误差成为理论上限），压缩过度可能丢失细小物体或文字细节；但对大多数自然图像和艺术创作场景，压缩-重建误差可忽略。',
    tags: ['Stable Diffusion', '潜在空间', 'LDM', '文生图', '交叉注意力'],
    subTopic: '生成模型',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-97',
    category: 'machine-learning',
    question: '什么是自回归生成模型（Autoregressive Models）？GPT如何逐token生成文本？',
    answer: '自回归模型（Autoregressive Model）将高维数据的联合概率分布分解为条件概率的乘积链，利用链式法则逐元素生成：p(x₁, x₂, ..., x_n) = p(x₁)·p(x₂|x₁)·p(x₃|x₁,x₂)·...·p(x_n|x₁,...,x_{n-1})。每个条件分布由同一个模型（参数共享）按序预测。GPT系列是自回归语言模型的标杆，逐token生成文本的过程：(1) 输入编码——输入文本序列经Tokenization（如BPE）转为token序列[x₁,x₂,...,x_m]，加上位置编码(Positional Encoding)提供顺序信息；(2) Transformer解码器——纯Decoder-only架构，只有掩码自注意力（Masked Self-Attention）确保每个token只能看到自身及之前的历史（自回归因果性约束），交叉注意力无源的（与Encoder-Decoder架构不同）。每层解码器输出第i个位置的表示h_i，该表示包含了从x₁到x_i的所有上下文信息；(3) 输出概率——顶部线性层将最后一层的h_i映射到词汇表大小的logits，经softmax得到下一个token的条件概率分布p(x_{i+1} | x₁,...,x_i)；(4) 生成——推理时从起始token（如<BOS>）开始，每一步根据已有token序列预测下一个token的概率分布，从中采样（或贪心选最大概率/束搜索）得到输出token，并将其拼接到输入序列末尾，重复直到遇到结束token<eos>或达到最大长度。自回归训练目标为最大化对数似然：L = Σ_{i=1}^{N} log p(x_i | x_1,...,x_{i-1})。自回归模型的优势：(a) 天然适合序列生成（文本/音频/视频帧），生成方向与因果一致；(b) 概率严格归一化（每次条件分布都是概率向量求和为1），可直接计算序列的困惑度Perplexity=e^{-L}；(c) 无独立生成假设（区别于非自回归模型需假设条件独立），建模能力强大。缺点：(a) 推理必须串行（不能并行解码），长序列生成慢；(b) 错误累积（Exposure Bias）——训练时用真实历史token（Teacher Forcing），推理时用模型自己生成的token，微小的预测错误可能随序列增长被放大；(c) 无法在生成中途"回头看"修订已生成内容。后续出现Non-Autoregressive Generation方法（并行解码、迭代精炼）尝试突破速度瓶颈。',
    tags: ['自回归模型', 'GPT', '逐token生成', '链式法则', '因果注意力'],
    subTopic: '生成模型',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-98',
    category: 'machine-learning',
    question: '如何评估生成模型的质量？Inception Score（IS）、FID（Fréchet Inception Distance）的原理和局限？',
    answer: '评估生成模型质量依赖自动指标和人工评测互补。(1) Inception Score（IS）——用预训练的Inception v3分类器评估生成图像。公式：IS = exp(E_{x~p_g}[KL(p(y|x) || p(y))])，p(y|x)是分类器对生成图像x的类别预测分布，p(y)是所有生成图像预测的边际类别分布。高质量生成满足两个条件：(a) 每个生成图像类别清晰（p(y|x)熵低——分类器"自信"），(b) 生成图像整体类别多样（p(y)熵高——类别分布均匀）。KL散度衡量两者的差异度，取指数放大数值。IS优点：计算简单、论文中广泛使用。局限：仅依赖ImageNet的1000类分类器视角，对不在ImageNet类别中的对象（如动漫/艺术）无效；不比较真实数据，生成图像可能都像"狗"却被判断高分（只要各自像不同种类的狗）无参考基准；对过拟合敏感（网络"记住"了真实图像会骗高IS），且单张高分不代表整体分布匹配。(2) FID（Fréchet Inception Distance）——比较真实图像分布和生成图像分布在Inception特征空间中的统计距离。将真实和生成图像分别通过Inception v3（取pool3层的2048维特征），假设两者服从多元高斯分布，计算其Fréchet距离：FID = ‖μ_r - μ_g‖² + Tr(Σ_r + Σ_g - 2(Σ_r Σ_g)^{1/2})。第一项是均值差异（生成图像"平均样子"是否接近真实），第二项协方差差异（生成图像的多样性是否匹配真实分布）。FID越小越好（0表示完全一致）。优点：与人类视觉判断的相关性远高于IS，是目前标准指标；考虑了分布的两个矩（均值和协方差）。局限：对特征空间假设（高斯分布）可能不成立；受Inception网络训练极限影响（特征提取能力有限）；对生成图像数量和预处理方式敏感（建议至少50000样本）；不能解释"为什么质量差"。实践中常同时报告IS和FID，配合人工盲评（Two-Alternative Forced Choice）和特定任务指标（如生成人脸用Face Detection Rate）全面评判。',
    tags: ['Inception Score', 'FID', '生成评估', 'Fréchet距离', 'Inception'],
    subTopic: '生成模型',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 图神经网络 (5 questions: ml-99 ~ ml-103)
  // ============================================================
  {
    id: 'ml-99',
    category: 'machine-learning',
    question: 'GNN的基本思想是什么？消息传递（Message Passing）框架的三个步骤？',
    answer: '图神经网络（Graph Neural Network, GNN）将深度学习扩展到非欧几里得空间的图结构数据上，核心思想是每个节点的表示通过"聚合其邻居节点的信息"来迭代更新——即消息传递（Message Passing）范式。图定义为G=(V,E)，V为节点集（每个节点v有特征向量x_v），E为边集（可带边特征e_uv）。消息传递框架每轮迭代包括三个步骤：(1) 消息计算（Message Computation）——对每条边(u→v)，计算从源节点u发送给目标节点v的消息 m_{uv}^{(k)} = MSG^{(k)}(h_u^{(k-1)}, h_v^{(k-1)}, e_{uv})，MSG通常是可学习的函数（如MLP），消息可依赖u和v的当前表示及边特征；(2) 消息聚合（Aggregation）——目标节点v收集来自所有邻居u∈N(v)的消息，通过置换不变函数聚合为一个向量 a_v^{(k)} = AGG^{(k)}({m_{uv}^{(k)}: u∈N(v)})。AGG必须对邻居的排列顺序不敏感（即置换不变性），常用操作包括求和（Sum，表达能力最强可区分不同度）、均值（Mean，归一化防止度大节点主导）、最大值（Max，关注最强信号）；(3) 节点更新（Update）——将聚合消息与节点自身历史表示融合，h_v^{(k)} = UPDATE^{(k)}(h_v^{(k-1)}, a_v^{(k)})，UPDATE通常为MLP或GRU。经过K轮消息传递后，每个节点的最终表示h_v^{(K)}融合了K跳邻域的全部信息，可用于节点分类/回归/图分类等下游任务。图读出（Readout）用于获取全图表示：h_G = READOUT({h_v^{(K)}: v∈V})，通常取节点表示的均值/和/最大值或层次化池化。GNN的关键性质：(a) 置换不变性——无论节点编号顺序如何，同一图得到相同表示；(b) 平移等变性——图结构平移（如蛋白质结构旋转）应当保留输出等价性。GNN在分子性质预测（药物发现）、社交网络分析、推荐系统、交通预测等领域大放异彩。',
    tags: ['GNN', '消息传递', '图神经网络', '聚合', '置换不变性'],
    subTopic: '图神经网络',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-100',
    category: 'machine-learning',
    question: 'GCN（Graph Convolutional Network）的图卷积与普通2D卷积有什么异同？',
    answer: 'GCN（Graph Convolutional Network）由Kipf & Welling 2017年提出，将卷积操作推广到图结构数据。异同分析：(1) 相同点——两者都是聚合局部邻域信息的数学操作：2D卷积在空间域内用固定大小的卷积核（如3×3）聚合像素周围邻居的加权和；GCN同样在每个节点的1跳邻域内聚合邻居特征，公式 H^{(l+1)} = σ(Ď^{-1/2} Ã Ď^{-1/2} H^{(l)} W^{(l)})，其中Ã=A+I（带自环的邻接矩阵），Ď是Ã的度矩阵。这一层的计算可理解为：对每个节点，取自身和所有邻居特征做加权平均（归一化后），再通过线性变换W和激活σ。(2) 不同点：(a) 卷积核结构——2D卷积的权重在规则网格上共享且有明确的空间排列（左/右/上/下），卷积核大小固定（如3×3=9个参数共享于全体像素）；GCN的"卷积"基于图的拓扑结构自适应，每个节点的邻居数量不固定（度不同），"核"的视野由邻接关系动态决定；(b) 权重共享方式——2D卷积在空间维度上共享同一组参数（平移不变性）；GCN的W在所有节点间共享（与度无关），但邻居的加权仅依赖图的归一化度矩阵Ď^{-1/2}ÃĎ^{-1/2}，没有空间方向性；(c) 感受野——2D卷积的感受野由层数和卷积核大小精确控制（第l层感受野≈l×kernel_size）；GCN的感受野由图的连通性决定——第l层GCN可达l跳邻居范围，小世界网络中几层即可覆盖几乎全图；(d) 谱域vs空间域——GCN可同时从谱图论（Chebyshev多项式逼近图拉普拉斯特征值）和空间邻居聚合两种视角解释，而2D卷积主要在空间域理解；(e) 计算效率——2D卷积通过im2col+矩阵乘法和FFT高度优化；GCN涉及稀疏邻接矩阵的乘法（通常用稀疏矩阵运算库），不适合超大图（百万+节点）。GCN适合转导学习（Transductive，训练时已知所有节点但仅部分有标签），但无法直接泛化到训练未见的新图（归纳式GCN如GraphSAGE使用邻居采样解决此问题）。',
    tags: ['GCN', '图卷积', '2D卷积', '邻接矩阵', '谱图论'],
    subTopic: '图神经网络',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-101',
    category: 'machine-learning',
    question: 'GAT（Graph Attention Network）如何用注意力机制聚合邻居信息？',
    answer: 'GAT（Graph Attention Network）由Veličković 2018年提出，在消息聚合中引入注意力机制，让每个节点自动学习其邻居的重要性权重，而非像GCN那样仅根据图拓扑（度）做固定归一化加权。核心计算——对节点v及其邻居u∈N(v)∪{v}：(1) 注意力系数计算——通过共享的可学习线性变换W将节点特征映射到更高层，计算注意力对数（logits）：e_{vu} = LeakyReLU(aᵀ·[Wh_v || Wh_u])，a是可学习的注意力向量，||表示拼接。即节点v和u的特征经过线性变换和拼接后，通过一个单层前馈网络映射为标量注意力分数。(2) 归一化——用softmax对v的所有邻居的注意力分数进行归一化（确保v对所有邻居的注意力之和为1）：α_{vu} = softmax_u(e_{vu}) = exp(e_{vu}) / Σ_{j∈N(v)∪{v}} exp(e_{vj})。(3) 特征聚合——v的新表示是其邻居特征的加权和：h_v\' = σ(Σ_{u∈N(v)∪{v}} α_{vu} · Wh_u)，σ为激活函数（通常ELU），α_{vu}是通过注意力自动学出的归一化权重。(4) 多头注意力（Multi-Head Attention）增强稳定性——独立运行K对注意力机制（每组不同的W^k和a^k参数），将K个头的输出拼接或平均：h_v\' = ||_{k=1}^K σ(Σ_{u} α_{vu}^k · W^k h_u)（拼接版，每头维度较小）或 h_v\' = σ((1/K)·Σ_k Σ_u α_{vu}^k · W^k h_u)（平均版，通常在最后一层）。GAT的优势：(a) 权重可解释——α_{vu}天然提供了节点间依赖的视觉化解释；(b) 自适应——不同邻居的重要性根据特征（非仅拓扑）动态分配，某些邻居即使远但特征相似会被赋予高权重，反之近但不相关的邻居权重低；(c) 归纳式——注意力参数W和a在所有节点/边间共享，可泛化到训练未见的新图（与GCN的Ď^{-1/2}ÃĎ^{-1/2}不同，后者依赖训练时的固定邻接矩阵）。缺点：邻域需全部加载（大图时度高的节点计算开销大），训练时需全图或大批量图，难以扩展到十亿级图。后续工作GATv2改进注意力分数的表达式（改拼接顺序）提升动态注意力的柔性。',
    tags: ['GAT', '图注意力', '注意力机制', 'Graph Attention', '多头注意力'],
    subTopic: '图神经网络',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-102',
    category: 'machine-learning',
    question: '图神经网络面临哪些挑战？（过平滑Over-smoothing、可扩展性、异构图等）',
    answer: 'GNN虽然强大，但面临五大核心挑战。(1) 过平滑（Over-smoothing）——随着GNN层数增加（通常≥3层），所有节点的表示趋于几乎相同（收敛到同一向量），失去区分性。数学原因：如果把消息传递视为图的扩散/随机游走过程，多步后的节点表示等于其K跳邻域内所有节点表示的加权平均（扩散），K大时每个节点覆盖了大面积重叠的邻域，均值趋于全局平均。GCN本质上是低通滤波器——抑制高频（局部变化）保留低频（全局趋势），过多层将所有变化全部平滑掉。缓解方法：残差连接（GCNII）、JK-Net（Jumping Knowledge，将各层表示拼接而非仅用最后一层）、DropEdge（随机删除边降低收敛速度）、归一化层。实践中大多数GNN深度≤2-3层。(2) 可扩展性——真实图（社交网络数十亿节点、边数更多）无法全图同时加载到GPU内存。GCN/GAT等全批量方法需全图邻接矩阵（O(|E|)内存），梯度需通过整个图反向传播。缓解：邻居采样（Neighbor Sampling）——GraphSAGE对每个节点随机采样固定数量的邻居（2-3跳），构建小批量计算图，内存和计算量O(K·S^K)；聚类方法（Cluster-GCN）将图按Metis等聚类算法分成子图簇，以子图为单位做mini-batch训练；图采样（GraphSAINT）随机采样子图。LightGCN去除特征变换和非线性简化推荐场景计算。(3) 异构图（Heterogeneous Graph）——节点和边类型多样（如电商图中user/item/category/brand多种节点，交互/浏览/购买多种边类型）。标准GNN假设同类节点和同类边，异构图需设计类型特定的参数（RGCN——每种边类型独立的W_r转换矩阵）。(4) 动态图——图结构随时间演化（金融交易网络、社交关系动态变化），GNN需做时间感知的图学习（Temporal GNN / EvolveGCN）；(5) 可解释性——节点表示中各邻居的贡献不易解释（注意力机制的GAT可部分缓解）。总体上，工业界GNN实践多用2层结构的轻量模型（SGC/LightGCN）配合高效的工程实现（邻居采样+GPU优化的稀疏矩阵库）应对大规模场景。',
    tags: ['GNN', '过平滑', '可扩展性', '异构图', '邻居采样'],
    subTopic: '图神经网络',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-103',
    category: 'machine-learning',
    question: '图神经网络在推荐系统、风控、分子预测等领域的具体应用？',
    answer: 'GNN在工业级应用的核心场景：(1) 推荐系统——将用户-物品交互建模为二部图（User和Item两种节点，交互为边），核心是学习高质量的用户和物品Embedding。LightGCN（简化图卷积）去除了特征变换和非线性激活，直接做线性邻居聚合，在诸多召回任务上效果超越传统矩阵分解和NeuMF。PinSage——Pinterest的工业级GNN，基于GraphSAGE通过随机游走采样构建训练子图，将用户与pin（图片）的交互图用于学习超大规模视觉推荐Embedding，支持数十亿节点。GNN在推荐中的优势：协同信号通过多层图扩散——不仅利用直接交互（1跳），还利用同好用户的共同交互物品（2跳及以上）。(2) 风控/反欺诈——将交易网络、设备关联网络、通信网络建模为图，欺诈节点具有特定的图拓扑特征（如欺诈账号通常聚集为小连通分量、过度同步注册/登录、与正常用户共享设备IP形成2跳关联）。GraphSAGE和GAT学习节点表示后，接入分类器检测欺诈节点/边（如欺诈交易/虚假账号）。阿里巴巴的GEM通过异构图聚合用户行为序列检测恶意行为。(3) 分子性质预测——将分子的原子（节点）和化学键（边）构造成分子图。GNN（特别是GIN——Graph Isomorphism Network）学习节点级/图级表示，用于预测分子性质（毒性/溶解度/药物活性），加速虚拟药物筛选。该领域要求GNN具备强大的图同构测试能力（区分不同的分子结构）——GIN通过求和聚合+MLP更新实现与WL（Weisfeiler-Lehman）同构测试等价的表达能力。(4) 交通预测——路网建模为空间图（路口=节点，路=边），结合GNN与时间卷积/RNN/Transformer构成时空图网络（ST-GCN/DCRNN/Graph WaveNet），预测各路段未来的流量/速度（节点回归）。(5) 知识图谱推理——实体=节点，关系=边类型，用RGCN（关系图卷积）或R-GAT学习实体表示，预测缺失的关系（链接预测/知识图谱补全）。GNN成功的关键前提：数据天然具有图结构、图拓扑携带了重要的信号（仅用节点特征不足以完成任务）。',
    tags: ['GNN', '推荐系统', '风控', '分子预测', '图应用'],
    subTopic: '图神经网络',
    difficulty: 'easy',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // NLP基础 (8 questions: ml-104 ~ ml-111)
  // ============================================================
  {
    id: 'ml-104',
    category: 'machine-learning',
    question: 'Word2Vec的两种架构（CBOW和Skip-gram）有什么区别？各自适用什么场景？',
    answer: 'Word2Vec是Mikolov 2013年提出的分布式词嵌入学习方法，通过学习预测任务来获得词的稠密向量表示。两种架构的核心区别在于预测方向：(1) CBOW（Continuous Bag of Words）——用窗口内所有上下文词预测中心目标词。给定窗口大小c，输入是目标词两侧各c个词的词向量之和（或平均，Bag-of-Words忽略词序），经线性层+softmax预测中心词w_t。目标函数最大化log P(w_t | w_{t-c},...,w_{t-1}, w_{t+1},...,w_{t+c})。训练速度快于Skip-gram（每次更新只预测一个词），适合频繁词（有充足的上下文证据）。CBOW假设上下文词相互独立，对词序不敏感（"猫 吃 鱼"和"鱼 吃 猫"的上下文化向量一致）。(2) Skip-gram——用中心词预测窗口内的所有上下文词。目标函数最大化Σ_{-c≤j≤c, j≠0} log P(w_{t+j} | w_t)，每个上下文词独立预测。训练速度慢（每个中心词预测2c个词，计算量2c倍），但每个中心词有多个正样本训练信号。适用于低频词——低频词往往作为中心词的训练机会少，但每个低频词同时作为其他词的上下文出现，CBOW中低频词被上下文平均淹没，而Skip-gram中每个词无论频率都同等作为独立训练实例。实验结论：(a) 小数据集或低频词多→Skip-gram（更多训练信号、对低频词更友好）；(b) 大数据集且追求训练速度→CBOW；(c) Skip-gram学到的词向量在高维度的语义关系任务上通常略优于CBOW（尤其在类比推理、词汇相似度等测度上）；(d) 包含大量噪音的文本（如Web数据）→CBOW更鲁棒（平滑效应减轻噪声词干扰）。两者共同的问题：softmax分母需对全词典求和（O(|V|)），巨大词典（百万词）不可行，需负采样或层次softmax加速。后续的FastText将内部字符n-gram加入词表示，GloVe用全局词共现矩阵替代局部窗口，ELMo/BERT则引入上下文相关的动态词嵌入终结了静态词向量的时代。',
    tags: ['Word2Vec', 'CBOW', 'Skip-gram', '词嵌入', 'NLP'],
    subTopic: 'NLP基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-105',
    category: 'machine-learning',
    question: '什么是负采样（Negative Sampling）？为什么Word2Vec需要负采样？',
    answer: '负采样（Negative Sampling）是一种高效的训练近似方法，用于解决Word2Vec全softmax计算量过大的问题。标准Skip-gram对每个中心词-上下文词对(w,c)，计算条件概率P(c|w) = exp(v_c·u_w) / Σ_{j∈V} exp(v_j·u_w)，分母需遍历整个词汇表V（百万级），每一步都做V次点积是灾难性的计算开销。负采样将多分类简化为二分类问题：给定真实中心-上下文对(w,c)为正样本（标签=1），同时从噪声分布P_n(w)（通常取词频的3/4次幂平滑）中随机抽取K个词c\'_1,...,c\'_K作为负样本（标签=0，K通常=5-20）。损失函数为：L = -[log σ(v_c·u_w) + Σ_{k=1}^K E_{c\'_k~P_n}[log σ(-v_{c\'_k}·u_w)]]，其中σ是sigmoid函数。即希望正样本的内积得分高（接近1），负样本的内积得分低（接近0）。这一形式等价于噪声对比估计（NCE）的特例——当K→∞时负采样目标逼近真实softmax梯度。为何需要负采样：(a) 计算加速——每步从计算V次点积降到K+1次（K通常5-20），百万词典的训练变得可行；(b) 梯度聚焦——不是对全词表做均等更新，而是针对性区分当前的(正样本+少量负样本)，负样本选得越好梯度越有效；(c) 隐式学习词的表征分布——高质量的负样本（即与中心词有某种语义关联但非真实上下文的词）能驱动嵌入空间更好地分离语义上无关但可能混淆的词。负采样的关键设计：(a) 噪声分布——词频的3/4次幂平滑了高频词和低频词的采样差距，既不太偏高频（否则大量负样本都是"the/of"等无信息停用词），也不完全忽视高频；(b) 难负样本（Hard Negative）挖掘——随机负样本太简单导致模型学不到精细边界，后续工作提出从模型当前高分的假正例中选择负样本（如Batch内Hard Mining）。层次Softmax（Hierarchical Softmax）是另一种加速替代——用二叉树将概率计算从O(V)降到O(log V)，但与负采样的效果各有优劣。',
    tags: ['负采样', 'Word2Vec', 'NCE', '加速训练', '词嵌入'],
    subTopic: 'NLP基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-106',
    category: 'machine-learning',
    question: 'BPE（Byte Pair Encoding）、WordPiece、SentencePiece等分词方法的原理和异同？',
    answer: '子词分词（Subword Tokenization）是现代NLP的标配，在词级别和字符级别之间取折中——既能用紧凑表示表示常见词（不拆），又能通过子词组合表示未登录词（OOV）。三种主流方法的对比：(1) BPE（Byte Pair Encoding）——自底向上的合并式分词。从字符级开始，统计训练语料中所有相邻符号对的共现频次，迭代合并出现频率最高的符号对为新符号（如"e"+"r"频繁→合并为"er"），重复直到达到预设的词表大小。BPE本质是用最频繁的子串组合作为基本词元。优点：简单高效，自动发现常见词根/词缀组合（如英语中"ing""tion"等后缀被独立保留）。缺点：固定频率驱动的合并在语料不均衡时可能产生次优拆分（如训练语料中"low "频繁导致"low"反复合并扩产子词，但"lower"合适拆分"low"+"er"）。GPT系列使用BPE。(2) WordPiece——同样自底向上合并，但合并准则不同：不是选最高频率的字对，而是选最大化训练语料似然的字对——argmax_{A,B} (count(A,B) / (count(A)·count(B)))。即合并那些"一起出现概率远高于独立出现概率"的符号对（互信息更高的对）。优点：相对于频率，WordPiece合并更关注语义凝聚力，避免高频但无意义的合并。BERT使用WordPiece，词表大小通常30000。(3) SentencePiece——谷歌提出的一体化分词方案，核心创新：(a) 直接处理原始文本（不需要预分词/预切词），将空格也作为特殊字符`_`（U+2581）纳入词汇表，实现真正的语言无关（无需按空格分词，对中文/日语等无空格语言友好）；(b) 默认使用Unigram Language Model——不是逐步合并，而是初始构建一个大的候选子词集合（所有高频子串），通过EM算法迭代修剪——删除对训练似然贡献最小的子词，直至达到词表大小。此种方式将分词视为概率模型选择问题。代表模型T5、LLaMA使用SentencePiece。BPE与WordPiece本质是确定性的合并序列，SentencePiece/Unigram是概率模型。共性：都是无损的分词方案（可重建原始文本），都能有效处理OOV、通过子词组合表达任意新词。选择：中文/等多语言→SentencePiece；英文单语言且需要与BERT兼容→WordPiece；追求简单速度→BPE。',
    tags: ['BPE', 'WordPiece', 'SentencePiece', '分词', '子词', 'NLP'],
    subTopic: 'NLP基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-107',
    category: 'machine-learning',
    question: '什么是N-gram语言模型？它的稀疏性问题如何解决？（平滑技术：Laplace、Kneser-Ney等）',
    answer: 'N-gram语言模型是基于马尔可夫假设的统计语言模型——假设第i个词的出现仅依赖前N-1个词的历史（N-1阶马尔可夫），即P(w_i | w_1,...,w_{i-1}) ≈ P(w_i | w_{i-(N-1)},...,w_{i-1})。整个序列的概率 P(W) = Π_i P(w_i | w_{i-(N-1)},...,w_{i-1})，每一项从训练语料中极大似然估计：P(w_i | w_{i-(N-1)},...,w_{i-1}) = count(w_{i-(N-1)},...,w_i) / count(w_{i-(N-1)},...,w_{i-1})。N通常取1/2/3/4/5（unigram/bigram/trigram/4-gram/5-gram），N越大上下文越丰富但数据稀疏性越严重——长N-gram的出现频次极低，大量(上下文,下一个词)组合在训练集中从未出现（概率为0），导致模型给任何包含未见N-gram的句子分配概率0，这在开放领域完全无效。平滑技术解决零概率问题，核心思想是把一些概率质量从高频N-gram"劫"给零频N-gram：(a) Laplace平滑（Add-One）——最简单的平滑，所有N-gram计数+1：P(w_i|h) = (count(h,w_i)+1) / (count(h)+|V|)。缺点是过于平均——太多概率分配给"不可能"词（词汇量V很大时几乎把所有概率均摊给无意义的词），效果差，仅教学示意。(b) Add-k平滑——改进版加一个小于1的常数k：P(w_i|h) = (count(h,w_i)+k) / (count(h)+k|V|)，k通过交叉验证选。(c) 回退（Backoff）——当N-gram频次太低时，"退回"到(N-1)-gram的概率估计。如trigram频次为零则退到bigram，若bigram也零则退到unigram。Katz回退根据Good-Turing估计决定何时退回。(d) 插值（Interpolation）——将不同阶N-gram的概率加权混合，而非"非零即退回"的硬切换：P_interp(w_i|w_{i-2},w_{i-1}) = λ_3·P(w_i|w_{i-2},w_{i-1}) + λ_2·P(w_i|w_{i-1}) + λ_1·P(w_i)，Σλ_i=1，λ通过EM在保留数据上估计。(e) Kneser-Ney平滑——当前最优的N-gram平滑方法，核心创新：低阶模型不再估计"词的出现频率"，而是估计"词作为新上下文的延续能力"。例如"San Francisco"高频但"Francisco"几乎只跟在"San"后——若trigram缺失，普通bigram会给"Francisco"高概率（因其高频），但Kneser-Ney的bigram会基于"Francisco前面可接的不同词的数量"来估计——因为"Francisco"几乎总接在"San"后其continuation count=1，概率极低，这对真正的少见上下文更合理。Kneser-Ney平滑在困惑度上显著优于其他平滑方法，是N-gram时代的经典算法。但随着神经网络语言模型（RNN/Transformer）的兴起，平滑问题已不再是主流关注——神经网络天然通过共享相似上下文解决了稀疏性。',
    tags: ['N-gram', '语言模型', '平滑', 'Kneser-Ney', '稀疏性'],
    subTopic: 'NLP基础',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-108',
    category: 'machine-learning',
    question: '什么是序列标注（Sequence Labeling）？CRF与BiLSTM-CRF模型如何结合？',
    answer: '序列标注是为输入序列x=(x_1,...,x_n)中的每个元素预测一个标签y_i的任务，典型任务包括：命名实体识别NER（B-PER/I-PER/O标记人名）、词性标注POS、中文分词、语音识别的音素标注、信息抽取的关系标注等。标签之间存在强依赖关系（如B-PER后不能跟I-LOC，必须跟I-PER），因此独立逐位分类不足以捕捉全局序列结构。CRF（Conditional Random Field，条件随机场）是一种判别式概率图模型，建模序列标签的条件分布。CRF为无向概率图，公式 P(y|x) = (1/Z(x))·exp(Σ_i Σ_k λ_k t_k(y_{i-1},y_i,x,i) + Σ_i Σ_l μ_l s_l(y_i,x,i))，t_k是转移特征（相邻标签的关系），s_l是状态特征（当前位置的词与标签关系），Z(x)是归一化因子确保概率和为1。在链式CRF中最简形式：P(y|x) ∝ exp(Σ_i (Emission(y_i,x_i) + Transition(y_{i-1},y_i)))，其中Emission(y_i,x_i)衡量第i个位置词x_i与标签y_i的匹配度，Transition(y_{i-1},y_i)衡量标签y_{i-1}转移到y_i的合理性。训练时最大化整条序列的对数似然，解码（预测）用Viterbi算法全局搜索最优标签序列。BiLSTM-CRF模型：输入词序列经BiLSTM编码，每个位置输出一个标签特征向量h_i（融合了前后上下文），然后接一层全连接（线性层）将h_i映射到标签空间得到每个位置的标签得分（Emission Score），称为发射矩阵P（n×k维，k=标签数）。CRF层以P作为Emission的来源，同时学习一个可训练的转移矩阵T（k×k维，T[i][j]表示从标签i转移到标签j的得分）。整个序列的CRF得分 = Σ_i P[i][y_i] + Σ_i T[y_{i-1}][y_i]。训练时最小化负对数似然（softmax整个序列空间需要前向算法计算Z(x)），解码时Viterbi找总分最高的标签路径。BiLSTM-CRF的优势：(a) LSTM提取丰富的上下文特征替代手工特征工程（CRF的原始状态特征s_l需要人工设计词形/前缀/后缀等）；(b) CRF在输出层建模标签依赖（转移矩阵T防止非法标签序列如I-PER直接跟B-LOC），弥补LSTM的不足。该模型曾是NER任务的SOTA方案，现在逐渐被BERT+CRF或flat/span-based方法取代。',
    tags: ['序列标注', 'CRF', 'BiLSTM-CRF', 'NER', 'Viterbi'],
    subTopic: 'NLP基础',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-109',
    category: 'machine-learning',
    question: '预训练语言模型（如BERT）的MLM（Masked Language Model）和NSP（Next Sentence Prediction）预训练任务分别训练什么能力？',
    answer: 'BERT通过两个自监督预训练任务学习通用的语言表示：(1) MLM（Masked Language Model）——随机遮盖输入序列中15%的token，让模型预测被遮盖的token。具体操作：在被选中的15%的token中，80%替换为特殊标记[MASK]，10%替换为随机token，10%保持原样不换。三类操作的设计动机——如果全用[MASK]遮盖，预训练和微调之间会出现不匹配（微调时没有[MASK]标记）；用随机token替换则强迫模型根据上下文纠正错误token（学习"这个词在此语境下说不通"）。MLM训练的是深层双向上下文理解能力——模型必须融合左侧和右侧的所有token信息才能预测中心词，这使BERT成为真正的双向编码器（区别于GPT只看到左侧、ELMo的浅层双向（独立的L→R和R→L各管一边最后拼接））。MLM学到了词汇语义、句法依存（预测"猫___老鼠"中的"抓"需要理解动宾关系）、甚至是常识推理（"把烤箱预热到___度"需要世界知识）。(2) NSP（Next Sentence Prediction）——给定句子对(A,B)，50%为真实连续的上下句，50%为随机拼接的无关系句子，模型二分类判断B是否为A的下一句。NSP训练的是句子级别的连贯性和篇章结构理解能力（如判断两个句子是否有因果关系/时序关系/话题连贯性）。但事后多项研究（RoBERTa/XLNet）证明NSP对下游任务提升甚微甚至可省略——因为MLM在足够大数据上已经隐含地学到了句子间关系（跨句子的MLM预测自然需要关注句子边界），且NSP任务本身过于简单（随机选择两个句子基本毫无关联，区分度低）。RoBERTa去除了NSP、增大数据量和训练步数后全面超越BERT。所以现代预训练（如T5/LLaMA）基本只保留遮盖语言模型作为唯一预训练目标，或结合更精细的预训练任务（如ALBERT的SOP——Sentence Order Prediction，判断句子顺序是否打乱，比NSP更难）。',
    tags: ['BERT', 'MLM', 'NSP', '预训练', '双向编码'],
    subTopic: 'NLP基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-110',
    category: 'machine-learning',
    question: 'BERT为什么不适合做文本生成？GPT和BERT在架构上的核心区别是什么？',
    answer: 'BERT和GPT代表了NLP预训练的两个根本范式差异——架构和训练目标。核心区别：(1) 架构——BERT是Encoder-only（仅使用Transformer的编码器部分），GPT是Decoder-only（仅使用Transformer的解码器部分）。Encoder中每个位置的注意力可无限制地关注序列中所有其他位置（双向注意力/Bidirectional Attention），每个token的表示融合了全部上下文。Decoder中自注意力被限制为因果掩码（Causal Masking / Autoregressive Attention）——第i个token只能关注前i-1个token（从左侧到右侧的单向），这是自回归生成的必要条件。(2) 预训练目标——BERT的MLM（遮盖预测）训练双向语境理解；GPT的Next Token Prediction训练从左到右的文本生成概率。(3) 为什么BERT不适合生成：(a) 没有自回归解码头——BERT是为判别/理解设计的编码器，输出是每个token的上下文化向量，但没有自回归输出层（softmax over vocabulary生成下一个token的机制）；(b) 双向注意力信息泄露——如果强行用BERT做生成（每次加一个token再重新编码整个序列），每一步新token都会通过双向注意力"看到"之前token的表示而不仅是"看到"它们的内容，打破了自回归的条件概率链条；(c) BERT的MLM假设[MASK]位置的预测依赖左右上下文同时存在，而生成过程只有左半边的已知信息（右半边的"未来"不可见），这构成严重的训练-推理不一致（Training-Inference Discrepancy）。BERT更适合下游理解任务——文本分类（情感分析/主题分类）、序列标注（NER）、阅读理解（SQuAD）、文本相似度等需要全局语境的NLU任务。GPT适合文本生成、对话、翻译（GPT的少样本/零样本学习依赖语言模型的生成能力）。近年趋势：Encoder-only模型（BERT/RoBERTa/DeBERTa）擅长NLU，Decoder-only模型（GPT/LLaMA）擅长NLU和NLG的统一，Encoder-Decoder模型（T5/BART）天然适合序列转换任务（翻译/摘要/问答合成）。',
    tags: ['BERT', 'GPT', '文本生成', '编码器', '解码器', '双向注意力'],
    subTopic: 'NLP基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-111',
    category: 'machine-learning',
    question: '如何评估文本生成质量？BLEU、ROUGE、Perplexity各自衡量什么？有什么缺陷？',
    answer: '文本生成评估分为自动指标和人工评测，自动指标各有侧重和严重缺陷：(1) BLEU（Bilingual Evaluation Understudy）——基于n-gram精确匹配的指标，衡量候选译文与一个或多个参考译文的n-gram重合度（精确率）。公式：BLEU = BP·exp(Σ_{n=1}^N w_n·log p_n)，p_n为n-gram精确率，w_n通常等权（N=4），BP（Brevity Penalty）是长度惩罚因子（防止过短的翻译骗高分）。BLEU本质是在评估"候选文本的每个n-gram是否在参考中也出现"——即精确度优先，对召回不敏感。缺陷：(a) 忽略同义表达——"迅速"和"快速"完全不同分；(b) 不考虑语法连贯性和语义保真——胡扯但包含许多参考中n-gram的文本可得高分；(c) 只依赖表面n-gram匹配，无法评估事实一致性（摘要是否虚构内容）。BLEU主要用于机器翻译评估。(2) ROUGE（Recall-Oriented Understudy for Gisting Evaluation）——基于召回率的指标，衡量参考文本的n-gram/最长公共子序列（LCS）在候选中有多大比例被覆盖。ROUGE-N = Σ_{S∈Ref} Σ_{gram_n∈S} Count_match(gram_n) / Σ_{S∈Ref} Σ_{gram_n∈S} Count(gram_n)（n-gram召回率）。ROUGE-L使用最长公共子序列（LCS）做比较（不要求连续匹配）。ROUGE侧重覆盖面（Recall），适合摘要评估（候选能否抓取原文关键信息）。缺陷：同样无视语义和事实，且对语句长度异常敏感（暴力堆砌内容可刷高ROUGE）。(3) Perplexity（困惑度）——衡量语言模型对序列"诧异"程度的指标：PPL = exp(-(1/N)·Σ_i log P(w_i | w_{i-1},...,w_1))。等价于模型对序列给的平均概率的倒数，PPL越低→模型对序列的预测越确定（越不"困惑"）。用于评估语言模型本身的内在质量，而非生成文本与参考的相似度。缺陷：只能用训练/测试数据的语言分布做评估，依赖分词方式（不同tokenizer的PPL不能直接比），对被评估数据集质量和重复性敏感。三种指标的集合局限：都无法评估语义忠诚度、事实一致性、创造性（自动指标只会将创造性发散视为"偏离参考"）。因此实践中自动指标仅用于粗劣的模型筛选，最终决策需依赖人工评测（流畅度、忠实度、趣味性等维度打分）或专门的自然语言推断（NLI）模型评估事实一致性。',
    tags: ['BLEU', 'ROUGE', 'Perplexity', '文本生成评估', 'NLP指标'],
    subTopic: 'NLP基础',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 强化学习 (6 questions: ml-112 ~ ml-117)
  // ============================================================
  {
    id: 'ml-112',
    category: 'machine-learning',
    question: '强化学习的核心三要素是什么？（状态State、动作Action、奖励Reward）MDP如何形式化？',
    answer: '强化学习（Reinforcement Learning, RL）是智能体通过与环境交互试错学习最优策略的机器学习范式，不同于监督学习（有标签反馈）和无监督学习（无标签反馈）。核心三要素：(1) 状态（State）s∈S——环境在t时刻的景象，全量或部分观测。全观测MDP中状态包含决策所需全部信息（马尔可夫性质——下一个状态仅取决于当前状态和动作，与历史无关）；部分观测MDP（POMDP）中智能体只能观测环境的局部信息o_t。(2) 动作（Action）a∈A——智能体基于当前状态/观测采取的行为。根据动作空间分为离散动作（如上下左右移动、棋类落子）和连续动作（如机器人关节角度、自动驾驶方向盘角度/油门力度），后者通常由策略梯度或Actor-Critic方法处理。(3) 奖励（Reward）r∈R——环境对智能体行为的瞬时反馈信号，可以是正（奖励）、负（惩罚）或零。奖励是唯一引导学习的目标——RL的中心假设是一切目标都可归约为最大化累积奖励期望。MDP（Markov Decision Process，马尔可夫决策过程）是RL问题的数学形式化框架。一个MDP由五元组(S, A, P, R, γ)定义：(a) S：有限或连续状态空间；(b) A：动作空间；(c) P：状态转移概率矩阵P(s\'|s,a)——从状态s执行动作a后到达下一个状态s\'的概率（环境动力学模型）；(d) R：奖励函数R(s,a)或R(s,a,s\')——执行动作后获得的即时奖励；(e) γ∈[0,1]：折扣因子（Discount Factor），权衡即时奖励与未来奖励的相对重要性——γ=1全重未来、γ=0只看当前。智能体的目标是学习策略π(a|s)（状态到动作的映射或概率分布），最大化期望累积折扣回报 G_t = Σ_{k=0}^∞ γ^k·r_{t+k+1}。价值函数用于评估"某状态/状态-动作对"的期望累积回报：状态价值函数V^π(s) = E_π[G_t | s_t = s]，动作价值函数Q^π(s,a) = E_π[G_t | s_t = s, a_t = a]。MDP的关键性质——最优策略的存在性（至少一个最优确定性策略）、值函数的Bellman方程递归分解、策略迭代的收敛性保证——使得RL在理论上可被形式化建模和求解。',
    tags: ['强化学习', 'MDP', '状态', '动作', '奖励', 'Bellman方程'],
    subTopic: '强化学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-113',
    category: 'machine-learning',
    question: '基于价值（Value-based）和基于策略（Policy-based）的RL方法有什么区别？各自的代表算法？',
    answer: '基于价值（Value-based）的方法：核心是学习动作价值函数Q(s,a)——估计在状态s执行动作a的期望回报，然后通过贪心策略a* = argmax_a Q(s,a)做决策。不需要显式策略函数π(a|s)。代表算法：(a) Q-Learning——Off-policy的TD学习方法，用Bellman最优方程 Q(s,a)←Q(s,a)+α[r+γ·max_{a\'} Q(s\',a\') - Q(s,a)] 迭代更新；(b) DQN（Deep Q-Network）——用神经网络逼近Q函数Q_θ(s,a)，结合经验回放（Experience Replay，打破样本时序相关性）和目标网络（Target Network，稳定TD目标）实现Atari游戏超人类水平。Value-based的优缺点：训练样本效率高（经验回放重用历史数据）、可学习确定性策略；但仅适用于离散动作空间（max操作在连续空间不可行），且对策略的表示是隐式的（Q函数的最大值点），难以学出随机策略。基于策略（Policy-based）的方法：直接参数化策略π_θ(a|s)，通过梯度上升最大化期望回报的梯度∇_θ J(θ)。代表算法：(a) REINFORCE——用Monte Carlo采样估计策略梯度：∇_θ J(θ) ≈ (1/N)·Σ_i Σ_t ∇_θ log π_θ(a_t^i|s_t^i)·G_t^i（完整轨迹累积回报作为权重）；(b) Actor-Critic——用Critic（价值函数）替代完整轨迹回报G_t作为Baseline减少梯度方差：∇_θ J(θ) ≈ Σ_t ∇_θ log π_θ(a_t|s_t)·A(s_t,a_t)，A(s,a)=Q(s,a)-V(s)是优势函数（Advantage）；(c) PPO（Proximal Policy Optimization）——用截断的目标函数限制策略更新幅度，保证稳定的单调改进，是目前连续控制/复杂游戏的SOTA。Policy-based的优缺点：可学随机策略（天然用于博弈/探索）、支持连续动作和高维动作空间；但样本效率低（需要大量On-policy数据，经验回放不能直接用于On-policy算法的策略梯度），梯度方差大需要大量技巧（Baseline/GAE等）稳定训练。选择指南：离散动作+价值函数易估计（如Atari游戏）→Value-based/DQN系列；连续动作+需要稳定训练→Policy-based/PPO；两者结合→Actor-Critic系列（SAC/TD3/DDPG）。',
    tags: ['Value-based', 'Policy-based', 'DQN', 'PPO', 'Actor-Critic'],
    subTopic: '强化学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-114',
    category: 'machine-learning',
    question: 'Q-Learning的更新公式是什么？为什么Q-Learning是Off-policy的？',
    answer: 'Q-Learning是Watkins 1989年提出的经典时序差分（TD）算法，更新公式为：Q(s,a) ← Q(s,a) + α[r + γ·max_{a\'∈A} Q(s\',a\') - Q(s_t,a_t)]。其中α是学习率，r+γ·max_{a\'} Q(s\',a\')是TD目标（使用Bellman最优方程的单步自举bootstrap），方括号内的项是TD误差（TD Error）δ = r + γ·max_{a\'} Q(s\',a\') - Q(s,a)，衡量当前Q值与目标Q值的差距。Q-Learning是Off-policy的核心原因在于：更新Q值的TD目标 max_{a\'} Q(s\',a\') 假设下一步采用贪心策略（对所有可能动作取最大值），即它在学习"最优策略（贪心动作）下的Q值"。但智能体实际选择动作的行为策略（Behavior Policy）完全可以不是贪心的——例如ε-greedy策略（以ε概率随机探索，1-ε概率贪心选择），这些探索产生的数据(s,a,r,s\')同样可以用来更新Q。因此Q-Learning学的是目标策略（贪心最优策略），而数据由另一种行为策略（ε-greedy等探索策略）产生——这正是Off-policy的定义：从行为策略b产生的数据中学习目标策略π。Off-policy的核心优势：(a) 可以复用历史数据（经验回放DQN——缓冲区保存各种策略产生的数据），大幅提升样本效率；(b) 可以同时做探索和利用——行为策略持续随机探索，目标策略逐步逼近最优；(c) 可以从人类示范或其他智能体的经验中学习（学习他人经验而无需自己执行）。对比SARSA（State-Action-Reward-State-Action）是On-policy算法，更新公式 Q(s,a)←Q(s,a)+α[r+γ·Q(s\',a\') - Q(s,a)]，a\'是实际执行的下一个动作（取决于当前策略），因此它学的是当前行为策略（含噪声探索）的Q值，而非最优策略。特点：Q-Learning学最优策略但可能有高偏差（Maximization Bias——max操作系统性地高估Q值，因为取了随机Q的最大值），Double Q-Learning用两个Q网络的交叉估计解决此问题；SARSA学当前策略更保守安全（尤其在负奖励风险大时SARSA绕过风险区域），Q-Learning更大胆。',
    tags: ['Q-Learning', 'Off-policy', 'TD误差', 'SARSA', 'DQN'],
    subTopic: '强化学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-115',
    category: 'machine-learning',
    question: '什么是策略梯度（Policy Gradient）？REINFORCE算法的原理和方差问题？',
    answer: '策略梯度（Policy Gradient）是直接优化参数化策略π_θ(a|s)期望回报的梯度方法。核心公式——策略梯度定理：∇_θ J(θ) = E_{τ~π_θ}[Σ_{t=0}^T ∇_θ log π_θ(a_t|s_t)·G_t]，其中τ=(s_0,a_0,r_0,s_1,...)是完整轨迹，G_t=Σ_{k=0}^{T-t} γ^k·r_{t+k}是第t步的折扣累积回报。定理的美妙之处：不需要环境动力学模型（Model-free），不需要Q函数的梯度，梯度表达为"当前策略下动作对数概率的梯度"乘以"该动作带来的回报"，犹如监督学习中调整参数使高回报动作更加可能(正权重)和低回报动作更加不可能(负权重)。REINFORCE是最基础的Monte Carlo策略梯度算法：(1) 用当前策略π_θ收集一个完整轨迹（rollout）；(2) 对轨迹中每个时间步t，计算G_t（从此刻到轨迹结束的实际累积回报——Monte Carlo估计，无bootstrap！）；(3) 梯度更新 θ←θ + α·∇_θ log π_θ(a_t|s_t)·G_t。REINFORCE的核心缺点是方差极大——因为两个随机性来源：(a) 环境状态转移的随机性；(b) 策略本身的随机性（采样动作）。G_t是整个轨迹的实际累积回报，每一步采样的动作和下一次状态的噪声积累导致不同rollout的G_t差异巨大，策略梯度估计的方差因此极大，训练极不稳定，收敛极慢。降低方差的方法：(1) Baseline减法——从G_t中减去一个仅依赖状态s_t的基函数b(s_t)：∇_θ J(θ) ≈ Σ_t ∇_θ log π_θ(a_t|s_t)·(G_t - b(s_t))。b通常取价值函数V(s_t)的估计，得到的G_t-b(s_t)近似优势函数A(s_t,a_t)，有理论证明Baseline不增加偏差但显著降低方差。(2) Actor-Critic——用Critic估计Q函数（或V函数），替换G_t的Monte Carlo估计为单步/多步TD估计 Q(s_t,a_t)，同时引入bootstrap减少累积误差。(3) GAE（Generalized Advantage Estimation）——λ加权混合不同步数的TD误差，在偏差-方差之间柔性权衡。尽管方差高，REINFORCE仍是理解策略梯度的核心基础，后续的PPO/A3C/TRPO等SOTA算法皆在此基础上改进。',
    tags: ['策略梯度', 'REINFORCE', '方差', 'Baseline', '蒙特卡洛'],
    subTopic: '强化学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-116',
    category: 'machine-learning',
    question: 'Actor-Critic方法的原理是什么？如何结合Value-based和Policy-based的优势？',
    answer: 'Actor-Critic是融合了基于策略（Actor）和基于价值（Critic）的优势的RL算法家族。Actor（π_θ(a|s)）是策略网络，直接输出动作或动作分布，负责"做决策"——基于Critic的评估调整策略参数。Critic（V_w(s)或Q_w(s,a)）是价值网络，负责"评判决策的好坏"——估计状态的价值或状态-动作对的价值，提供梯度更新的方向。Actor-Critic的工作流程：(1) Actor执行动作a_t ~ π_θ(s_t)，环境返回奖励r_t和新状态s_{t+1}；(2) Critic计算TD误差 δ_t = r_t + γ·V_w(s_{t+1}) - V_w(s_t)（若Critic估计V函数）；(3) Actor按策略梯度方向更新——θ←θ+α_θ·∇_θ log π_θ(a_t|s_t)·δ_t（δ_t替代了REINFORCE中的G_t，此时δ_t是优势函数的无偏估计）；(4) Critic按TD学习更新——w←w+α_w·δ_t·∇_w V_w(s_t)。两者在每一步同步更新（在线学习）。如何结合双方优势：Value-based（Critic成分）提供低方差的评分基准——Critic通过bootstrap（TD学习）快速估计当前状态/动作的回馈，避免了Monte Carlo方法G_t的大方差。Policy-based（Actor成分）直接参数化策略——可学习随机策略（必要的探索机制）、适用于连续动作空间，且Actor学习的是期望回报的梯度方向以确保策略是单调改进的。Critic还作为Baseline削减策略梯度方差（δ_t = Q - V = Advantage，本质上就是减去V的Baseline）。A2C/A3C（Advantage Actor-Critic）是该范式的经典算法：A2C用单进程同步更新，A3C用多个并行环境异步更新加速训练。A3C曾是Atari和连续控制的SOTA。当代主流Actor-Critic变体：(a) PPO——用重要性采样和截断目标做更稳定的策略更新；(b) DDPG/TD3——将DQN扩展到连续动作空间，Actor输出连续动作向量，Critic用DQN-style Q函数评估（Off-Policy + Replay Buffer），是连续控制的首选；(c) SAC（Soft Actor-Critic）——引入最大熵目标（策略同时最大化回报和熵，鼓励探索），是目前连续控制的最佳base算法之一，样本效率和稳定性俱佳。Actor-Critic是当前RL算法设计的核心范式，几乎所有实用的RL系统都以此为框架。',
    tags: ['Actor-Critic', 'A2C', 'TD误差', '策略梯度', '优势函数'],
    subTopic: '强化学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-117',
    category: 'machine-learning',
    question: '强化学习在推荐系统、对话系统、游戏AI中的应用和挑战？',
    answer: '(1) 推荐系统中的RL——将用户与推荐系统的交互视为MDP：状态s_t=用户的历史行为+当前上下文（设备/时间/位置），动作a_t=推荐一个物品列表或单个物品，奖励r_t=用户即时反馈（点击/购买/观看时长等）。RL的优势：传统有监督推荐（预测点击率）优化单步即时反馈，但无法建模推荐的长线效果——如看完一个视频后是否会继续留在平台（用户留存/满意度/终身价值）。RL通过累积折扣回报γ权衡即时点击和长期用户留存。挑战：(a) 奖励延迟和稀疏——用户对推荐满意程度可能要数天或数周后通过回流行为体现（极长奖励延迟），信用分配极难；(b) 离线评估困难——无法像游戏那样快速重置环境刷经验，只能用历史日志的离线策略评估（Off-policy Evaluation，如IPS/DR估计器）近似，估计方差高；(c) 探索-利用的矛盾——推荐探索用户从未见过的物品可能带长线收益但也可能引发即刻流失，生产系统需要保守探索（如少量流量做随机探索）；(d) 状态/动作空间巨大——百万用户×百万物品的组合空间无法穷举，需用Embedding和泛化模块。典型应用：YouTube的Top-K离线强化推荐、淘宝多目标RL的素材组合生成。(2) 对话系统中的RL——对话被视为MDP：状态=对话历史编码+用户意图追踪，动作=系统回复（文本生成），奖励=任务成功率/用户满意度/对话长度倒数。RL用于微调Seq2Seq对话模型初稿（多由有监督预训练提供初值），通过RL优化对话级别的目标（任务完成率、用户耐心度等非微分的指标）。挑战：奖励函数定义难（怎样是"好"对话）、对话动作空间极大（所有可能回复文本的组合）、奖励信号极其稀疏（往往整段对话结束时才有用户评价）。(3) 游戏AI——RL最成功的领域：AlphaGo/AlphaZero用蒙特卡洛树搜索+深度RL战胜世界围棋冠军；OpenAI Five用PPO在Dota 2 5v5比赛中战胜人类半职业队；AlphaStar用多智能体RL在星际争霸II达到大师级。游戏AI是RL的天然试验田：环境反馈快（每帧都有状态/奖励）、可无限仿真重置（样本无限的On-policy训练无冷却）、奖励信号清晰（胜/负/得分）。挑战：多智能体博弈的纳什均衡求解、极长轨迹的信用分配（一局数万步的RTS游戏，最后的胜/负如何归因到前期每个决策）、以及训练需要巨大算力（AlphaStar训练需数百GPU·天）。整体趋势：RL在工业落地需结合监督学习（预训练提供好的初始策略）和模仿学习（从人类行为数据学习），纯RL从零探索在现实业务中极少可行。',
    tags: ['强化学习', '推荐系统', '对话系统', '游戏AI', 'RL应用'],
    subTopic: '强化学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 自监督学习 (5 questions: ml-118 ~ ml-122)
  // ============================================================
  {
    id: 'ml-118',
    category: 'machine-learning',
    question: '什么是自监督学习（Self-Supervised Learning）？和监督学习、无监督学习的区别是什么？',
    answer: '自监督学习（Self-Supervised Learning, SSL）是一种特殊的无监督学习范式——不需要人工标注标签，但通过精心设计的**前置任务（Pretext Task）**从数据本身自动构造"伪标签"进行监督式训练。核心思想：从输入数据的一部分预测另一部分，利用数据自身的结构作为监督信号。例如：遮盖图片的一部分让模型重建（图像修复）、遮盖句子中的词让模型预测（MLM）、预测视频下一帧、判断两段裁剪是否为同一张图的不同视角（对比学习）。与监督学习的区别：监督学习需要昂贵的人工标注标签（y），模型学的是"人定义的概念"；自监督学习的标签由数据自动派生，模型学的是"数据的内在结构和表示"。与无监督学习的区别：传统无监督学习（如聚类/PCA/密度估计）通常基于特定的数据假设（如簇假设、线性假设），输出的表示丢失大量信息；自监督学习虽然也不用人标，但采用监督学习的训练形态（损失函数、梯度下降），学到的表示往往质量更高且可直接迁移到下游监督任务（微调/线性探针评估）。自监督学习成功的根本逻辑：一个好的数据表示必须保留数据的本质结构信息——如果一个模型能通过"部分数据"预测"另一部分数据"，它一定学到了数据的内在语义规律的压缩表示。SSL在NLP中率先爆发：BERT/GPT仅用自监督预训练（MLM/Next Token Prediction）学到的上下文化词嵌入，微调后全面统治各项NLU任务。CV的SSL（SimCLR/MoCo/MAE）近年来逐渐缩小与监督预训练的差距甚至在某些任务上超越。自监督学习的两个主流范式：(a) 生成式——通过重建/预测被遮盖或加噪的数据学习表示（MLM/BERT、MAE视觉遮盖自编码器）；(b) 对比式——通过拉近同类样本（正样本对）、拉远不同类样本（负样本对）的表示学习不变性（SimCLR/MoCo）。SSL真正释放了无标注大数据的潜力（互联网上有近乎无限的未标注图像/文本），是目前大模型训练的默认范式。',
    tags: ['自监督学习', 'SSL', '前置任务', '无监督', '预训练'],
    subTopic: '自监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-119',
    category: 'machine-learning',
    question: '对比学习（Contrastive Learning）的核心思想是什么？SimCLR的四大组件和InfoNCE损失？',
    answer: '对比学习（Contrastive Learning）的核心思想是拉近相似样本对（正样本）的表示，同时推远不相似样本（负样本）的表示，在表示空间中形成有语义结构的嵌入分布——同语义样本在向量空间中聚集，异语义样本分散开。SimCLR（Simple Framework for Contrastive Learning of Visual Representations, Chen et al. 2020）是将对比学习简化为端到端可训练框架的代表作。四大核心组件：(1) 数据增强模块——对同一输入图像x，随机应用两种数据增强操作组合（随机裁剪+颜色抖动+高斯模糊+灰度化等），生成同一样本的两个不同视图（views）x̃_i和x̃_j。这两个视图被视为正样本对（代表"同一个物体"而拥有相同的语义内容，只有外观扰动差异）。数据增强的设计是对比学习的关键——过强破坏语义、过弱则模型的变换不变性不够。(2) 编码器f(·)——神经网络（通常ResNet-50/ResNet-101）提取图像的表示向量。编码器对两张增强视图分别输出表示h_i=f(x̃_i), h_j=f(x̃_j)。(3) 投影头（Projection Head）g(·)——小型MLP（1-2层），将编码器表示映射到对比损失计算的隐空间z_i=g(h_i)=W₂·σ(W₁·h_i)。关键发现：直接在编码器输出h上做对比效果差，额外投影头过滤了数据增强特定信息、保留语义不变信息。但下游任务使用时应丢弃投影头，仅取h作为特征。(4) 对比损失（InfoNCE Loss / NT-Xent）——对Batch内N个样本，每个样本增强两个视图共得到2N个表示。对正样本对(i,j)（同一样本的两个视图），InfoNCE损失定义为：L_{i,j} = -log [exp(sim(z_i,z_j)/τ) / Σ_{k=1}^{2N} 𝟙_{k≠i} exp(sim(z_i,z_k)/τ)]。sim常用余弦相似度（归一化后点积），τ是温度参数（控制分布的锐度，τ越小→softmax分布越集中于高相似度负样本，模型越聚焦难负样本，τ通常0.07~0.5）。分子是正样本对的相似度（应该高），分母是当前样本z_i与batch内所有其他2N-1个样本（包括另一个视图的正样本和所有其他样本作为负样本）的相似度之和。通过最小化这一损失，模型被迫将z_i与正样本z_j拉近、同时与所有其他2N-2个负样本推远。SimCLR的贡献：(a) 证明了"更大的batch size（4096/8192）提供更多负样本是提升对比学习的关键"；(b) 证明了数据增强的组合强度＝对比学习的核心；(c) 建立了简单的端到端对比学习基线，后续MoCo/BYOL/SimSiam等都是在其基础上的改进和简化。对比学习的局限：需要大量负样本（大batch或MoCo的记忆库）、对数据增强设计敏感、不适合某些需要精确重建的任务。',
    tags: ['对比学习', 'SimCLR', 'InfoNCE', '数据增强', '投影头'],
    subTopic: '自监督学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-120',
    category: 'machine-learning',
    question: 'MoCo（Momentum Contrast）如何用动量编码器和动态队列解决大batch问题？',
    answer: 'MoCo（Momentum Contrast, He et al. 2020）解决了SimCLR等端到端对比学习对大batch的严重依赖——SimCLR需要batch内同时有足够多的负样本（通常4096-8192），大batch需要大量GPU显存，限制了研究的普及性。MoCo通过两个创新组件将负样本数量与大batch解耦：(1) 动态队列（Dynamic Queue）——维护一个大小为K的负样本特征队列（Queue），队列中存储的是最近K个mini-batch经过编码器提取的表示向量（先进先出FIFO更新）。当计算当前batch对比损失时，负样本来自整个队列（K可达65536以上）而非仅batch内样本，这样batch size可以很小（如256）但负样本池超大（65536），极大提高了对比学习的有效性和GPU内存效率。队列的更新方式：每个batch结束后，将当前batch的正样本特征入队、最早batch的正样本特征出队。(2) 动量编码器（Momentum Encoder）——解决动态队列带来的一致性破坏问题：若队列中的负样本来自过去不同步数的编码器参数，它们的表示空间不一致（编码器参数更新后表示空间"漂移"了），不统一特征空间的对比学习会失败。MoCo的解决方案——维护两套编码器参数：查询编码器（Query Encoder，θ_q）正常梯度更新；动量编码器（Key Encoder，θ_k）不参与梯度更新，而是通过指数移动平均（EMA）缓慢跟踪查询编码器：θ_k ← m·θ_k + (1-m)·θ_q，m为动量系数通常0.999（即每次仅用0.1%的查询编码器更新量调节键编码器，相当于在参数空间做了高度平滑）。队列中存储的特征来自动量编码器的输出——因为键编码器参数变化非常缓慢，队列中近期和较早期的负样本特征在几乎一致的表示空间中，确保了对比学习的一致性。MoCo的训练流程：(a) 对查询视图（data augmentation A）用查询编码器得到查询向量q；(b) 对键视图（data augmentation B）用动量编码器得到键向量k₊（正样本），以及队列中的K个负样本键向量k_i（i=1..K）；(c) 计算InfoNCE损失：L = -log [exp(q·k₊/τ) / (exp(q·k₊/τ) + Σ_{i=1}^K exp(q·k_i/τ))]，对比目标同SimCLR——拉近正样本远离负样本；(d) 仅更新θ_q（梯度反向传播），θ_k平滑更新（无梯度）；键视图特征k₊入队，最早特征出队。MoCo v2将SimCLR的投影头和数据增强改进融入MoCo框架，性能进一步逼近甚至超越监督预训练。MoCo的优势：batch size灵活（不依赖GPU规模）、负样本数量可独立设置、表示质量高。后续BYOL（Bootstrap Your Own Latent）更进一步——完全不需要负样本，通过动量编码器+预测头的自举学习避免模型坍塌（但机制与MoCo不同）。',
    tags: ['MoCo', '动量编码器', '动态队列', '对比学习', '自监督'],
    subTopic: '自监督学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-121',
    category: 'machine-learning',
    question: '掩码自编码器（Masked Autoencoder, MAE）在视觉中的原理？为什么能达到很好的效果？',
    answer: 'MAE（Masked Autoencoder, He et al. 2021）是将NLP中BERT的遮盖预测思想迁移到视觉领域的里程碑式工作。核心操作非常简单：随机遮盖输入图像的大部分patch（如75%），仅用可见的25% patch让编码器编码，解码器则接收编码后的可见patch特征和遮盖patch的共享[MASK] token，重建整张图像的原始像素。为什么遮盖75%（而非BERT的15%）效果更好：图像与文本的信息密度差异巨大——图像中相邻像素和patch高度冗余（遮盖一大片区域后仍可通过周围纹理推断缺失内容），文本中单个词的信息量远高于单个像素（遮盖15%就足够挑战），因此视觉需要对遮盖率更高才能形成有意义的自监督任务，否则仅基于25%遮盖的图像去"复制"像素的表示太容易（退化为恒等学习）。MAE的架构设计巧妙：(1) 非对称编码器-解码器——编码器仅处理可见的25% patch（极大节省计算，训练速度3-4倍于ViT全像素方案），处理后的特征与遮盖patch的可学习[MASK]向量按原始空间位置拼接，送入解码器重建完整图像。解码器只在预训练中使用（需要像素级重建），下游任务（分类/检测/分割）只需编码器输出的特征。(2) 仅重建遮盖patch的像素——损失函数只计算遮盖部分的重建误差（MSE），忽略可见patch的重建。这一设计确保了编码器无法"偷懒"（如果所有像素都重建，编码器可能学到的都是图片中显眼的大面积纹理而忽略了遮盖区域的推断能力）。MAE为什么效果好：(a) 遮盖重建迫使编码器学习图像的高级语义和场景结构——要从仅1/4的patch中推断其余3/4的视觉内容，模型必须理解物体部件、几何关系、场景上下文等高级概念（例如看到"猫耳+猫前腿"片段需要推断"猫身+猫尾"位置与大致的形状，这已超出纹理统计的浅层特征学习）；(b) 高遮盖率消除像素间的直接线性相关性，迫使模型建立超越低级统计的全局理解；(c) 训练效率极高——仅处理可见patch，即使遮盖75%的情况下训练成本接近传统的全像素方案。MAE下游迁移能力极强——在ImageNet-1K上微调后分类精度超越监督预训练；在检测/分割任务上，MAE预训练同样显著提升ViT的密集预测能力。MAE的成功印证了一个深刻直觉——无论是在语言（MLM）还是视觉（Masked Image Modeling），"预测缺失部分"是让模型学到数据本质结构高度通用的自监督范式。',
    tags: ['MAE', '掩码自编码器', '遮盖重建', '自监督', '视觉预训练'],
    subTopic: '自监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-122',
    category: 'machine-learning',
    question: '自监督学习在NLP（BERT、GPT）和CV（SimCLR、MAE）中的预训练范式有何异同？',
    answer: '自监督学习在NLP和CV中的范式差异源于两种数据形态的本质不同。相同点：(1) 核心动机一致——利用大规模无标注数据学出可迁移的通用表示，微调后大幅提升下游任务性能（尤其是数据稀缺的任务）；(2) 训练形式——都采用自监督前置任务（Pretext Task）构造伪标签，通过神经网络+梯度下降学习；(3) 学到的表示——浅层学到通用模式（NLP的词法→语法→语义层次；CV的边缘→纹理→物体部件→物体）、深层学到任务相关的抽象表示；(4) 都证明了大规模数据+大规模模型是提升性能的简单有效途径（Scaling Law）。核心不同点：(1) NLP的预训练以**生成式/自回归**为主——BERT（MLM遮盖语言建模，Encoder-only）学习双向上下文表示，GPT（Next Token Prediction，Decoder-only）学习单向生成能力。预测目标是离散token（从文本片段预测缺失的离散词），损失函数为交叉熵。(2) CV的预训练以**对比学习**为主（SimCLR/MoCo/BYOL）——通过构造正负样本对（两张同一图像的不同增强->正样本，不同图像->负样本）学习表示空间的聚类结构，优化目标是拉近正样本/推远负样本（InfoNCE损失）。晚期CV也出现生成式预训练（MAE遮盖像素重建），思想更贴近MLM。(3) 数据特性决定方法差异——文本是离散符号序列，每个token承载高级语义，因此"预测缺失词"是足够困难且有意义的前置任务。图像是连续像素阵列，像素间信息高度冗余，"预测像素"太简单（易退化为浅层插值），早期生成式预训练在CV长期无法超越监督学习，直到MAE用高遮盖率（75%）将重建变难问题才被解决。而对比学习通过数据增强构建"同语义不改变"的先验，对视觉这种高冗余、高频空间外观变化的数据更自然。(4) 训练稳定性——NLP的生成式自监督训练通常非常稳定（离散token的交叉熵），CV的对比学习对数据增强策略/batch大小/负样本质量敏感，调参困难。(5) 当前融合趋势——Vision Transformer（ViT）架起了两者共享同一Transformer架构的桥梁，多模态模型（CLIP/ALIGN/BLIP）同时用对比学习对齐图文表示、在两者间共享自监督信号，进一步模糊了NLP和CV预训练的边界。',
    tags: ['自监督学习', 'NLP', 'CV', '预训练范式', '对比学习', '生成式'],
    subTopic: '自监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },

  // ============================================================
  // 无监督学习 (3 more questions: ml-123 ~ ml-125)
  // ============================================================
  {
    id: 'ml-123',
    category: 'machine-learning',
    question: 'DBSCAN算法的原理、两个关键参数（ε和minPts）如何选择？',
    answer: 'DBSCAN（Density-Based Spatial Clustering of Applications with Noise）是一种基于密度的聚类算法，通过样本分布的紧密程度来发现任意形状的簇，并能天然识别噪声点。核心思想基于三类点的定义：(1) 核心点（Core Point）——在半径ε邻域内至少包含minPts个样本的点；(2) 边界点（Border Point）——ε邻域内样本数少于minPts，但位于某个核心点的ε邻域内的点；(3) 噪声点（Noise/Outlier Point）——既不是核心点也不在核心点邻域内的点。紧密关系定义：(a) 直接密度可达——点q在核心点p的ε邻域内，则q从p直接密度可达；(b) 密度可达——存在一条核心点链条从p到q（p→p₁→p₂→...→q），则q从p密度可达；(c) 密度相连——存在点o使得p和q都从o密度可达，则p和q密度相连。一个簇定义为"所有密度相连的核心点和边界点的最大集合"。DBSCAN流程：(a) 随机未访问点p，若p是核心点则以p为种子开始扩展新簇（广度优先搜所有密度可达点加入簇），若p不是核心点标记为噪声（后续可能被其他核心点关联从而变为边界点）；(b) 重复直到所有点被访问。两个关键参数的选择：(1) ε（eps）——邻域半径，是最敏感的超参数。太大→不同密度的簇被合并为一个，所有点变成一簇（过连接）；太小→大部分点变成噪声，簇被切割过细。选择方法：(a) K-距离图（k-distance Graph）——对每个点计算其到第k个最近邻的距离（k=minPts-1），对所有点的k-距离排序后画图，找曲线"拐点"处（k-距离急剧上升的点）的距离作为ε，拐点前对应簇内的紧凑密度、拐点后对应噪声/不同簇分离处；(b) 多次尝试+轮廓系数评估；(c) 结合业务先验——如地理坐标中ε=500m有意义。(2) minPts——成为核心点所需的最少邻域样本数。经验规则：minPts≥D+1（D为数据维度），二维数据minPts=4典型；更大的minPts可降低噪声敏感度但需要更大的ε配合。也可以根据领域知识——已知簇的最小期望大小来设minPts。DBSCAN的优势：可发现任意形状簇、自动识别噪声、不需要预设K值。劣势：对参数极其敏感、密度差异大的数据表现差（单一ε无法同时适应稠密和稀疏区域，需OPTICS算法通过ε的层次扫描解决）、高维数据中欧氏距离失去区分度（维数灾难）。',
    tags: ['DBSCAN', '密度聚类', 'ε', 'minPts', '噪声', '核心点'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-124',
    category: 'machine-learning',
    question: '层次聚类（Hierarchical Clustering）的凝聚法和分裂法有什么区别？树状图（Dendrogram）如何解读？',
    answer: '层次聚类构建簇的层次嵌套结构，输出不是单一聚类划分，而是一棵描述不同粒度聚类的树状图（Dendrogram），可以在任意层次切分获得不同数量的簇。凝聚法（Agglomerative，自底向上）——初始时每个数据点自成一簇（共N簇），每步找出距离最近的两簇合并为一大簇，重复N-1步直至所有点合并为单一簇，形成二叉聚类树。簇间距离度量决定合并顺序（前文ml-27已详述Single/Complete/Average/Ward法）。分裂法（Divisive，自顶向下）——初始时所有点同属一巨型簇，每步选择一个簇按某种准则分裂为两个子簇，重复直到每个点自成一簇。分裂法计算复杂度远高于凝聚法——每步需要找出"如何将一簇最优二分"（本质是除以2的聚类问题），通常用K-Means(k=2)或最小生成树的分裂近似。实际中凝聚法远更常用，计算复杂度O(n²·log n)（若用堆优化最近簇查找）。树状图（Dendrogram）解读：(a) 纵轴（高度）——表示合并两簇时的距离值（dissimilarity），也可理解为"为了将这两簇合并需要容忍的簇内不纯度"；(b) 横轴——列出所有叶节点（单个数据点），顺序经过优化使树分支不交叉（如按聚类的相似度排列）；(c) 解读方法——从底部向上观察：最低处合并的是最相似的点对（微观簇），越往高处合并的距离越大，代表簇越来越松散宏观。想要获得K个簇？在树状图纵轴上水平画一条线，该线会切割出K个垂直分支，每个分支（线下的完整子树）即为一个簇。切割线越高（更大的簇间距离容忍）→ 簇数越少，反之切割线越低→ 簇数越多。(d) 不一致系数（Inconsistency Coefficient）——自动确定切割点位，比较一个合并的高度与其周围k个合并高度的统计偏差，若某合并的高度明显高于周围（"跳跃"大），则在此处切割能有效分离自然簇。层次聚类的优点：无需预设K（可在树状图上事后选择K），树状图提供清晰的聚类结构和层次关系可视化，适合层级性明显的场景（生物分类、文档主题树）。缺点：O(n²·log n)复杂度和O(n²)内存限制在小规模数据，凝聚法一旦合并就无法"反悔"调整（贪心局部最优，全局未必最优），对噪声和异常值敏感（异常点作为孤立簇被最后合并拉高整体聚类高度）。',
    tags: ['层次聚类', '凝聚法', '分裂法', '树状图', 'Dendrogram'],
    subTopic: '无监督学习',
    difficulty: 'medium',
    sm2: { ...SM2 },
    favorited: false,
  },
  {
    id: 'ml-125',
    category: 'machine-learning',
    question: '高斯混合模型（GMM）与K-Means的本质区别是什么？GMM如何用EM算法求解？',
    answer: 'GMM（Gaussian Mixture Model）是一种概率生成式聚类模型，假设数据由K个高斯分布混合生成，每个高斯分量N(μₖ, Σₖ)代表一个簇，混合权重πₖ代表样本属于第k簇的先验概率（πₖ≥0, Σπₖ=1）。数据生成过程：先按多项分布z~Categorical(π)选择簇标签，再从对应高斯采样 x~N(μ_z, Σ_z)。GMM K-Means的本质区别：(1) 软赋值vs硬赋值——GMM给出每个样本属于各簇的后验概率γ(z_nk) = P(z_n=k|x_n)（软聚类），反映分类不确定性；K-Means每个样本被强制全部分配给最近的中心（硬聚类，后验概率为0/1）。例如一个点位于两簇中间，GMM可能给出[0.55,0.45]概率，K-Means只能硬分给最近的簇。(2) 协方差结构——K-Means隐式假设各簇为等方差球形（距离计算全方向等权）；GMM支持四种协方差模式：full（各簇任意椭球形任意旋转，参数最多）、tied（所有簇共享同一协方差）、diagonal（各簇对角方差，椭球轴对齐坐标轴）、spherical（各簇方差标量，回到球形但大小可不同）。full模式表达能力最强但参数多易过拟合。(3) 生成vs判别——GMM是生成模型（可采样生成新数据），K-Means是判别式聚类。EM算法求解GMM参数θ={π_k, μ_k, Σ_k}：(E步) 给定当前参数估计值，计算每个样本属于各簇的后验概率（责任度Responsibility）：γ(z_nk) = π_k·N(x_n|μ_k, Σ_k) / Σ_{j=1}^K π_j·N(x_n|μ_j, Σ_j)。N(x|μ,Σ)是多元高斯密度的值。(M步) 用γ加权样本，更新模型参数：N_k = Σ_n γ(z_nk)（第k簇的有效样本量），π_k = N_k/N，μ_k = (1/N_k)·Σ_n γ(z_nk)·x_n（加权均值），Σ_k = (1/N_k)·Σ_n γ(z_nk)·(x_n-μ_k)(x_n-μ_k)ᵀ（加权协方差矩阵）。EM交替迭代。初始化敏感性：GMM尤其依赖初始化（可以用K-Means结果作为初始μ，Σ设为单位矩阵，π均匀分布）。GMM与K-Means的联系：当限制各簇协方差为σ²I且σ²→0时，后验概率退化为K-Means的硬分配（最邻近中心的后验→1），GMM退化为K-Means——K-Means是GMM在"极低温度"下的极限特例。GMM相比K-Means：更强表达力、软概率输出可评估聚类不确定性、有概率基础可做BIC/AIC模型选择（选K），缺点是参数多训练慢、对初始化敏感需多次随机重启、在高维数据协方差矩阵估计可能数值不稳定（需正则化Σ_k+=εI）。',
    tags: ['GMM', '高斯混合模型', 'EM算法', 'K-Means', '软聚类'],
    subTopic: '无监督学习',
    difficulty: 'hard',
    sm2: { ...SM2 },
    favorited: false,
  },
];
