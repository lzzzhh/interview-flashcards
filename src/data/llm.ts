import type { QACard } from '../types';

export const llmCards: QACard[] = [
  {
    id: 'llm-1',
    category: 'llm',
    question: "请推导 Self-Attention 的计算公式，并解释 Q、K、V 的含义。",
    answer: "Self-Attention 的核心公式为：Attention(Q, K, V) = softmax(QK^T / √d_k) V\n\n推导过程：\n1. 输入序列 X ∈ R^{n×d}（n 为序列长度，d 为 hidden dim）\n2. 通过三个可学习的线性变换矩阵 W_Q、W_K、W_V ∈ R^{d×d_k}（通常 d_k = d / num_heads），得到：\n   - Query: Q = X · W_Q\n   - Key:   K = X · W_K\n   - Value: V = X · W_V\n3. 计算注意力分数（相似度矩阵）：S = QK^T ∈ R^{n×n}\n   - S_{ij} 表示第 i 个 token 对第 j 个 token 的注意力原始分数\n4. 缩放：S' = S / √d_k\n   - 目的：防止 d_k 过大导致点积值过大，进入 softmax 饱和区（梯度趋于 0）\n5. 归一化：A = softmax(S')，沿最后一维（即 key 维度）做 softmax，得到注意力权重\n6. 加权求和：Output = A · V ∈ R^{n×d_k}\n\nQ/K/V 含义：\n- Q（Query）：\"我要查什么\"——当前 token 想要关注其他位置时发出的查询向量\n- K（Key）：\"我是什么\"——每个 token 被查询时用于匹配的键向量\n- V（Value）：\"我有什么信息\"——被关注后实际传递的内容向量\n\n直觉类比：图书馆检索系统——Q 是你的检索词，K 是书的索引标签，V 是书的正文内容。通过 Q 和 K 的相似度决定关注哪些 V，加权聚合得到最终输出。",
    tags: ['Self-Attention', 'QKV', '公式推导', '核心机制'],
    subTopic: 'Transformer',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-2',
    category: 'llm',
    question: "为什么 Self-Attention 中要除以 √d_k？",
    answer: "除以 √d_k 的原因是为了控制点积的方差，防止梯度消失。\n\n详细解释：\n1. 假设 Q 和 K 的每个元素都是独立同分布的随机变量（均值为 0，方差为 1），则 QK^T 中每个元素是 d_k 个乘积的累加和\n2. 根据中心极限定理，此时 QK^T 的元素服从均值为 0、方差为 d_k 的分布\n3. 当 d_k 很大时（比如 64 或 128），点积的绝对值会变得非常大，大的点积值经过 softmax 后，分布会变得极度 peaky（接近 one-hot），即 softmax 的输出中某些值接近 1，其余接近 0\n\n这会带来两个问题：\n- 梯度消失：softmax 在饱和区（输入很大或很小）的梯度接近 0，导致参数更新困难\n- 注意力退化：模型几乎只关注一个位置，丧失了\"多位置综合关注\"的能力\n\n4. 除以 √d_k 后，QK^T / √d_k 的方差被归一化回 1，softmax 的输出分布更平滑，梯度可以正常流动\n\n数学上：Var(Q_i · K_j^T) = d_k → Var((Q_i · K_j^T) / √d_k) = 1。如果不除以 √d_k，在实践中模型训练会不稳定，收敛速度显著变慢。这就是为什么这个操作被称为 Scaled Dot-Product Attention 中的 \"Scaled\"。",
    tags: ['Scaled Attention', '√d_k', '方差控制', '梯度消失'],
    subTopic: 'Transformer',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-3',
    category: 'llm',
    question: "Multi-Head Attention 的设计目的是什么？它是如何工作的？",
    answer: "Multi-Head Attention（多头注意力）的核心思想是：让模型在不同的表示子空间里，同时关注不同位置、不同语义层面的信息。\n\n工作原理：\n1. 将 d_model 维的 Q、K、V 线性投影到 h 个不同的低维子空间（每个头的维度 d_k = d_model / h）\n2. 每个头独立执行 Scaled Dot-Product Attention\n3. 将所有头的输出拼接（Concat）起来\n4. 通过一个额外的线性变换 W_O 融合多头信息\n\n设计目的（为什么要多头）：\n- 捕捉多重语义关系：一个头可能关注语法结构（主谓宾），另一个头可能关注语义关联（指代消解），另一个头关注位置邻近关系\n- 增加模型容量：在不改变计算复杂度的情况下，通过多组参数让模型学习更多样的注意力模式\n- 避免单头偏差：单头注意力可能过度关注某一类特征（如自己对自己），多头的平均效果更鲁棒\n- 可解释性：不同头的注意力模式可以分别可视化分析（例如 BERT 中发现某些头专门关注 [CLS]、某些头关注相邻词）\n\n常见配置：BERT-base: 12 头 d_model=768 d_k=64；GPT-3: 96 头 d_model=12288。每头的 d_k 通常是 64 或 128。",
    tags: ['Multi-Head Attention', '子空间', '语义多样性'],
    subTopic: 'Transformer',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-4',
    category: 'llm',
    question: "Transformer 中的位置编码（Positional Encoding）有哪些方案？请对比 Sinusoidal 和 RoPE。",
    answer: "Transformer 本身不具备序列顺序感知能力（Self-Attention 是置换等变的），因此必须引入位置编码。\n\n1. Sinusoidal Positional Encoding（原始 Transformer）\n公式：PE(pos, 2i) = sin(pos / 10000^{2i/d_model})，PE(pos, 2i+1) = cos(pos / 10000^{2i/d_model})\n特点：绝对位置编码，每个位置有固定的编码向量直接加到词嵌入上；无需学习；外推性差——训练时的最大序列长度固定，超过该长度效果下降；由于 sin/cos 的和角公式，PE(pos+k) 可以表示为 PE(pos) 的线性变换，具有一定相对位置感知能力。\n\n2. RoPE（Rotary Position Embedding，旋转位置编码）\n提出者：苏剑林（RoFormer，2021），主要用于 LLaMA、Qwen、ChatGLM 等主流开源模型。\n核心思想：通过旋转变换将位置信息融入 Q 和 K 的计算中。将 Q 和 K 的每两个维度看作一个复数，乘以一个旋转矩阵（角度由位置 m、n 决定）。计算注意力时 Q_m^T K_n 天然包含相对位置信息 (m-n)。\n特点：相对位置编码，天然编码相对位置关系；优秀的外推能力——训练长度外的序列推理效果更好（可通过 NTK-aware 缩放外推）；不增加参数量；计算高效，可与 Attention 计算融合。\n\n对比总结：Sinusoidal 是绝对位置、外推能力中等，代表作原版 Transformer/BERT；RoPE 是相对位置、外推能力强，代表作 LLaMA/Qwen/Mistral。其他方案还包括 Learned Positional Embedding（GPT 系列）和 ALiBi（线性偏置，Bloom 使用）。",
    tags: ['Positional Encoding', 'Sinusoidal', 'RoPE', '位置编码'],
    subTopic: 'Transformer',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-5',
    category: 'llm',
    question: "Transformer 中 Residual Connection（残差连接）的作用是什么？为什么需要它？",
    answer: "残差连接在 Transformer 中的公式：Output = LayerNorm(x + Sublayer(x))，其中 Sublayer 可以是 Multi-Head Attention 或 Feed-Forward Network。\n\n作用与原因：\n1. 解决深层网络的梯度消失/爆炸问题——在反向传播时，残差连接提供了一条\"梯度高速公路\"（gradient highway），梯度可以直接通过恒等映射传播到浅层，避免了连乘导致的梯度衰减。数学上：∂L/∂x_l = ∂L/∂x_L · (1 + ∂F/∂x_l)，其中的 \"1\" 来自于残差连接。\n2. 使得深层网络训练成为可能——Transformer 通常有 12/24/96 层甚至更多，没有残差连接深层网络几乎无法训练。残差连接让每一层学习的是\"残差\"（即与输入的差异），降低了学习难度。\n3. 保持信息的完整性——即使某一层的输出很差（如注意力权重混乱），残差连接保证原始输入信息不会完全丢失，这类似于\"宁可保持不变，也不要变坏\"。\n4. Pre-LN vs Post-LN：Post-LN（原始 Transformer）训练不稳定需要 warmup；Pre-LN（现代主流）训练更稳定，对学习率更鲁棒。\n5. 与 BatchNorm 残差的区别：Transformer 使用 LayerNorm 而非 BatchNorm，因为序列长度可变，BN 在小 batch 下不稳定，LN 在每个样本的特征维度上归一化不依赖 batch 统计量。\n\n简而言之：残差连接是训练深层 Transformer 的\"必要条件\"，它保证了梯度能有效回传、信息能稳定流动。",
    tags: ['Residual Connection', '残差连接', '梯度回传', '深层网络'],
    subTopic: 'Transformer',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-6',
    category: 'llm',
    question: "LayerNorm 和 BatchNorm 有什么区别？为什么 Transformer 使用 LayerNorm？",
    answer: "Batch Normalization (BN) 和 Layer Normalization (LN) 的区别（假设输入 X ∈ R^{N×L×D}，N=batch，L=序列长度，D=特征维度）：\n- BN：在 (N, L) 两个维度上求均值和方差，对每个特征维度 D 做归一化，依赖 batch 内所有样本的统计量\n- LN：在 D 维度上求均值和方差，对每个样本的每个位置独立归一化，不依赖 batch 内其他样本\n\nTransformer 使用 LayerNorm 的原因：\n1. 序列长度可变——NLP 中序列长度 L 是变化的，BN 在处理变长序列时需要对不同位置分别统计，实现复杂且不稳定；LN 只需在固定维度的 D 上统计，天然支持变长序列\n2. Batch 大小不敏感——BN 在 batch size 较小时（如 batch_size=1 或 2）统计量不准确，效果急剧下降；LN 的统计量只依赖单个样本，batch size 为 1 也能正常工作\n3. 训练/推理一致性——BN 在训练时用 batch 统计量，推理时用全局移动平均，存在 gap；LN 训练和推理行为完全一致\n4. 计算效率——LN 不需要维护全局移动平均和方差，实现更简洁\n\n补充 RMSNorm：LLaMA 等模型使用 RMSNorm，是 LayerNorm 的简化版——去掉了\"减去均值\"的步骤，只做缩放。计算更快，效果相当。",
    tags: ['LayerNorm', 'BatchNorm', '归一化', 'RMSNorm'],
    subTopic: 'Transformer',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-7',
    category: 'llm',
    question: "为什么说 Transformer 比 RNN/LSTM 更容易并行化？",
    answer: "Transformer 的并行化优势源于其核心设计——Self-Attention 的计算方式。\n\nRNN/LSTM 为什么难以并行：\n- RNN 的时序计算是串行的：h_t = f(h_{t-1}, x_t)，要计算 h_t 必须先计算 h_{t-1}\n- 训练时即使知道完整输入序列，RNN 也必须在时间维度上逐个展开计算（BPTT）\n- 推理时生成第 t+1 个 token 必须等待第 t 个 token 的隐藏状态\n\nTransformer 为什么可以并行：\n1. 训练阶段完全并行——Self-Attention 的计算 QK^T 是整个序列的矩阵乘法，所有位置的注意力可以同时计算。矩阵乘法是 GPU 最擅长的操作，充分利用并行计算单元\n2. 前馈层（FFN）也是并行的——每个位置的 FFN 相互独立，所有位置的 FFN 可以通过矩阵乘法一次性计算\n3. 但推理时仍需自回归——生成式 Transformer（GPT）在推理时是自回归的，需要用到 KV Cache 来缓存已计算的 K 和 V，避免重复计算历史序列\n\n总结：训练并行度高（n×n 矩阵乘法一次性完成），推理并行度低（自回归生成），长距离依赖好（O(1) 路径长度），GPU 利用率高（矩阵乘法密集）。这就是为什么 Transformer 能用更多的数据和更大的模型进行训练。",
    tags: ['并行化', 'RNN vs Transformer', 'GPU计算', '训练效率'],
    subTopic: 'Transformer',
    difficulty: 'easy',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-8',
    category: 'llm',
    question: "请对比 Transformer 的 Encoder 和 Decoder 架构，以及三种架构变体。",
    answer: "标准 Transformer 的 Encoder 和 Decoder 结构：\n\nEncoder（编码器）：\n- 由 N 个相同的 Encoder Layer 堆叠，每层包含 Multi-Head Self-Attention → Add & Norm → FFN → Add & Norm\n- Self-Attention 是双向的：每个 token 可以 attend 到所有其他 token（包括前后方向）\n- 输出为输入序列的上下文表示（contextualized representation）\n\nDecoder（解码器）：\n- 由 N 个相同的 Decoder Layer 堆叠\n- 每层包含：Masked Multi-Head Self-Attention → Add & Norm → Cross-Attention → Add & Norm → FFN → Add & Norm\n- Masked Self-Attention 是单向的：第 i 个 token 只能 attend 到位置 ≤ i 的 token（因果注意力）\n- Cross-Attention 的 Q 来自 Decoder 自身，K 和 V 来自 Encoder 输出\n\n三种架构变体：\n1. Encoder-only（如 BERT）——只有 Encoder，双向 Self-Attention，适合理解任务（文本分类、NER、问答），预训练任务为 MLM + NSP\n2. Decoder-only（如 GPT 系列、LLaMA）——只有 Decoder（去掉 Cross-Attention），Masked Self-Attention，自回归生成，适合生成任务（对话、续写），预训练任务为 Next Token Prediction。这是当前大模型主流架构（Scaling Law 更优）\n3. Encoder-Decoder（如 T5、BART）——完整包含 Encoder 和 Decoder，适合序列到序列任务（翻译、摘要生成）\n\nDecoder-only 成为主流的原因：参数效率更高（所有参数同时用于理解和生成），In-Context Learning 更强，多种任务可统一为生成式框架。但专用理解模型（如 BERT）在特定场景下仍有效率优势。",
    tags: ['Encoder', 'Decoder', '架构对比', 'Encoder-only', 'Decoder-only'],
    subTopic: 'Transformer',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-9',
    category: 'llm',
    question: "Self-Attention、Cross-Attention、Causal Attention（Masked Attention）有什么区别？",
    answer: "三种注意力机制的区别核心在于 Q、K、V 的来源不同。\n\n1. Self-Attention（自注意力）——Q、K、V 来自同一输入序列 X。每个 token 与同一序列中的所有 token 进行交互，注意力矩阵无 mask，每个位置可以看到所有位置（双向）。使用场景：BERT 的 Encoder、ViT。作用：捕获序列内部的上下文依赖关系。\n\n2. Cross-Attention（交叉注意力）——Q 来自 Decoder 的当前层输入，K 和 V 来自 Encoder 的输出。让 Decoder 在生成每个 token 时能够\"查阅\"Encoder 编码的源序列信息。注意力矩阵无 mask，每个 decoder token 可以看到所有 encoder token。使用场景：Encoder-Decoder 架构的 Decoder 中。作用：连接源序列和目标序列，实现跨序列信息传递。\n\n3. Causal Attention / Masked Self-Attention（因果注意力）——Q、K、V 都来自同一个 Decoder 输入序列。但应用了上三角 mask，使位置 i 只能看到位置 ≤ i 的 token。具体做法：在 softmax 之前将 QK^T 矩阵的上三角部分（未来位置）设为 -∞，softmax 后权重为 0。使用场景：GPT 类 Decoder-only 模型的 Self-Attention。作用：保证自回归生成的因果性——当前 token 不能\"偷看\"未来的 token。\n\n在实际代码中，Cross-Attention 通常是在 Self-Attention 基础上，将 K 和 V 替换为外部来源即可实现。",
    tags: ['Self-Attention', 'Cross-Attention', 'Causal Attention', 'Mask'],
    subTopic: 'Transformer',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-10',
    category: 'llm',
    question: "GPT 和 BERT 的架构差异是什么？为什么 GPT 更适合生成而 BERT 更适合理解？",
    answer: "GPT（Generative Pre-trained Transformer）和 BERT（Bidirectional Encoder Representations from Transformers）是两种经典的预训练范式。\n\n架构差异：\n- GPT 是 Decoder-only（12 层 Transformer Decoder），注意力方向为单向（Causal / Masked Self-Attention），预训练任务为 Next Token Prediction（自回归 LM），无 [CLS] token，推理方式为自回归生成逐 token 输出\n- BERT 是 Encoder-only（12 层 Transformer Encoder），注意力方向为双向（Full Self-Attention），预训练任务为 MLM + NSP，有 [CLS] 表示整句和 [SEP] 分隔句子，推理方式为一次性编码输出每个 token 的表示\n\nGPT 更适合生成的原因：\n- Causal Attention 天然适合从左到右的生成过程\n- 自回归训练目标（预测下一个 token）与推理时的行为完全一致\n- 训练时每个 token 只看到前面的上下文，推理时同理，无 train-test mismatch\n\nBERT 更适合理解的原因：\n- 双向 Self-Attention 让每个 token 可以看到完整上下文，语义理解更充分\n- [CLS] token 聚合全句信息，适合句子级别的分类任务\n- MLM 预训练任务直接学习词语的上下文相关表示\n\n演变趋势：GPT-3 展示强大的生成 + In-Context Learning 能力后，主流大模型（GPT-4、Claude、LLaMA、Qwen 等）几乎全是 Decoder-only 架构。理解任务也可通过生成式方式完成，但专用理解模型在特定场景下仍有效率优势。",
    tags: ['GPT', 'BERT', '架构对比', '生成 vs 理解'],
    subTopic: 'Transformer',
    difficulty: 'easy',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },

  // ============================================================
  // 训练微调 — 10 题 (llm-11 ~ llm-20)
  // ============================================================

  {
    id: 'llm-11',
    category: 'llm',
    question: "Pretraining 和 Finetuning 的区别是什么？为什么现在的大模型强调\"预训练 + 对齐\"两阶段？",
    answer: "Pretraining（预训练）和 Finetuning（微调）是大模型训练的两个核心阶段。\n\n预训练（Pretraining）：目标是让模型学习语言的通用知识和模式。数据为海量无标注文本（TB 级别），如网页、书籍、代码等。任务为 Next Token Prediction（自回归语言模型）。计算量极大（千卡 GPU 集群训练数周到数月），只做一次。产出为 Base Model（基座模型），具备语言能力但不一定\"听话\"。\n\n微调（Finetuning）：目标是让模型适配特定任务或对齐人类偏好。数据为有标注/精心构造的小规模数据集（几万到百万条）。任务为 SFT（指令遵循）、RLHF（偏好对齐）等。计算量相对小，可以在基座模型上多次微调。产出为 Chat Model / Instruct Model。\n\n\"预训练 + 对齐\"两阶段范式的意义：\n1. 知识获取与行为对齐解耦——预训练阶段学习\"世界知识\"，对齐阶段学习\"如何与人类交互\"\n2. 成本效率——预训练极昂贵但通用，一次预训练可以衍生出多种对齐版本\n3. 安全性——Base Model 可能生成有害内容，对齐阶段引入安全训练（RLHF、红队测试）\n4. 能力激发——许多能力（推理、编程）在预训练时已经\"学会\"，但需要通过指令格式\"激发\"\n\n当前趋势：预训练更大规模更多数据，RLHF 逐渐向 DPO 等简化方法演变，Post-Training 综合 SFT + RLHF 的完整流程。",
    tags: ['Pretraining', 'Finetuning', '预训练', '对齐', 'SFT'],
    subTopic: '训练微调',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-12',
    category: 'llm',
    question: "LoRA（Low-Rank Adaptation）的原理是什么？它如何实现参数高效微调？",
    answer: "LoRA 是微软提出的参数高效微调（PEFT）方法，核心思想是用低秩矩阵来近似权重更新。\n\n基本原理：\n1. 冻结预训练权重 W ∈ R^{d×d}，不更新\n2. 引入两个可训练的低秩矩阵 A ∈ R^{d×r} 和 B ∈ R^{r×d}（r << d）\n3. 前向传播时：h = W·x + B·A·x = W·x + ΔW·x，其中 ΔW = B·A，秩为 r 的矩阵\n4. 反向传播时只更新 A 和 B\n\n数学本质：假设权重更新矩阵 ΔW 是低秩的（这在微调场景中经验成立），用两个小矩阵的乘积来近似 ΔW，大幅减少可训练参数。参数压缩比约为 d/(2r)，例如 d=4096 r=16，压缩比约 128 倍。\n\n实现细节：A 用高斯分布初始化，B 用 0 初始化（保证初始时 ΔW = 0）；通常应用到 Attention 层的 Q、K、V、O 投影矩阵；推理时可以与原始权重合并：W' = W + B·A，不增加推理延迟。\n\nLoRA 的优势：显存节省（只存 A 和 B）、推理无开销（合并后等价于原始模型）、多任务切换（不同任务只需切换 LoRA 权重）、不遗忘原能力（冻结原始权重）、训练速度更快。\n\n变体与改进：QLoRA（4-bit 量化 + LoRA，在单张消费级显卡微调 65B 模型）、AdaLoRA（自适应分配不同层的秩）、DoRA（将 ΔW 分解为幅度和方向）。典型配置：LLaMA-7B 微调 r=8~64，学习率 1e-4~5e-4。",
    tags: ['LoRA', 'PEFT', '低秩分解', '参数高效微调'],
    subTopic: '训练微调',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-13',
    category: 'llm',
    question: "什么是 Instruction Tuning（指令微调）？它如何提升模型的指令遵循能力？",
    answer: "Instruction Tuning（指令微调）是在多样化的 NLP 任务上，以\"指令-回答\"格式对预训练模型进行微调的过程。\n\n核心做法：\n1. 收集/构造大量指令数据，格式为 (instruction, input, output)，其中 instruction 为任务描述（如\"将以下句子翻译成英文\"），input 为具体输入，output 为期望输出\n2. 使用这些数据对 Base Model 进行 SFT（Supervised Fine-Tuning）\n3. 训练目标仍然是 Next Token Prediction，只在 output 部分计算 loss\n\n为什么能提升指令遵循能力：\n1. 格式统一化——预训练数据格式杂乱，模型不知道\"用户想要什么\"。指令微调统一了交互格式，模型学会识别\"这是指令，我应该按要求执行\"\n2. 任务泛化——涵盖多种任务（翻译、摘要、分类、生成、推理等），模型学会\"按指令切换行为模式\"。FLAN 论文证明指令微调可显著提升 zero-shot 泛化能力\n3. 激发潜在能力——许多能力（如 Chain-of-Thought、代码生成）在预训练中已经学到，指令格式作为\"提示模板\"系统性激发这些能力\n4. 减少 Prompt Engineering 需求——指令微调后模型对指令的理解更鲁棒\n\n关键论文：FLAN（Google 2022）、T0（Hugging Face 2021）、Self-Instruct（2022）、Alpaca（Stanford 2023）。LIMA 论文证明仅 1000 条高质量指令微调效果已接近 RLHF 模型——数据质量远大于数据数量。",
    tags: ['Instruction Tuning', '指令微调', 'SFT', 'FLAN', 'Alpaca'],
    subTopic: '训练微调',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-14',
    category: 'llm',
    question: "RLHF（Reinforcement Learning from Human Feedback）的完整流程是什么？Reward Model 和 PPO 分别起什么作用？",
    answer: "RLHF 是让大模型对齐人类偏好的核心技术，由 InstructGPT/ChatGPT 论文提出，包含三个阶段：\n\n阶段一：SFT（Supervised Fine-Tuning）——收集人类标注的高质量 (prompt, response) 对话数据，对 Base Model 进行监督微调得到 SFT Model。目的：让模型初步学会对话格式和基本指令遵循。\n\n阶段二：训练 Reward Model（RM，奖励模型）——对同一个 prompt 用 SFT Model 生成多个不同 response（如 K=4 个），让人类标注者对这些 response 进行排序（不是打分，排序更稳定）。训练一个 Reward Model 输入 (prompt, response) 输出标量分数。RM 的损失函数为 Bradley-Terry 模型：L = -E[log σ(r(x, y_w) - r(x, y_l))]，其中 y_w 较优 response，y_l 较劣 response。RM 参数量通常与 SFT Model 相同，最后加一个线性层输出标量。目的：用模型代替人类进行奖励评估，因为 PPO 需要大量在线评估。\n\n阶段三：PPO（Proximal Policy Optimization，近端策略优化）——将 SFT Model 作为初始策略 π_0。对每个 prompt 当前策略生成 response，RM 给出奖励 r。使用 PPO 算法最大化 R = E[r - β · KL(π || π_0)]。其中 r 是 RM 打出的奖励，KL(π || π_0) 是当前策略与初始策略的 KL 散度作为正则项，β 是 KL 惩罚系数控制模型不要偏离 SFT Model 太远（防止\"奖励黑客\"）。\n\nPPO 的作用：策略优化（通过试错提升生成质量）、在线学习（每次迭代重新采样不断改进）、KL 约束（解决 RM 不完美的问题，防止模型遗忘 SFT 学到的能力）。\n\nPPO 的挑战：实现复杂需同时维护 4 个模型（Policy、Reference、Reward、Value/Critic），训练不稳定需精细超参调优，计算代价高。这也催生了 DPO 等简化方法。",
    tags: ['RLHF', 'Reward Model', 'PPO', '对齐', 'InstructGPT'],
    subTopic: '训练微调',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-15',
    category: 'llm',
    question: "PPO 和 DPO（Direct Preference Optimization）的区别是什么？为什么 DPO 逐渐流行？",
    answer: "PPO 和 DPO 都是基于人类偏好进行对齐训练的方法，但实现路径完全不同。\n\nPPO（Proximal Policy Optimization）——强化学习路径：\n- 需要显式训练一个 Reward Model（RM）\n- 在线 RL：当前策略生成 response → RM 打分 → PPO 更新\n- 需要维护 4 个模型：Policy Model、Reference Model、Reward Model、Value Model\n- 训练流程：SFT → 训练 RM → PPO 优化，三阶段\n- 优点：理论上限高，在线探索可发现新高质量 response\n- 缺点：实现复杂，超参敏感，训练不稳定，计算成本高\n\nDPO（Direct Preference Optimization）——直接优化路径：\n- 不需要显式训练 Reward Model\n- 将偏好的概率直接映射到策略优化目标，一步到位\n- 只需要 2 个模型：Policy Model 和 Reference Model\n- 损失函数：L_DPO = -E[log σ(β · (log π(y_w|x) - log π(y_l|x) - (log π_ref(y_w|x) - log π_ref(y_l|x))))]\n- 直观理解：让模型提升好回答的概率，降低差回答的概率，以 Reference 为基线\n\nDPO 逐渐流行的原因：\n1. 实现简单——不需要训练额外 RM，代码量大幅减少，训练流程简化为单一阶段\n2. 训练稳定性——SFT 风格的监督训练比 RL 训练稳定，没有 reward hacking、策略崩溃风险\n3. 数据效率——偏好对 (y_w, y_l) 直接用于训练，比 PPO 的在线采样效率更高\n4. 理论优雅——DPO 论文数学证明最优策略等价于 RLHF 中带 KL 约束的 RL 最优策略\n5. 成本优势——不需要 RM 训练和推理开销，不需要维护 Value/Critic 模型\n\nDPO 的局限性：缺乏在线探索，对偏好数据质量更敏感。实际选择：资源有限快速迭代用 DPO，追求极致性能有充足资源用 RLHF+PPO+在线迭代，折中用 DPO 快速对齐再 PPO 进一步优化。变体：ORPO（融合 SFT+偏好优化）、SimPO（以序列长度为参考）。",
    tags: ['PPO', 'DPO', '偏好对齐', 'RLHF替代'],
    subTopic: '训练微调',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-16',
    category: 'llm',
    question: "Prompt Tuning 和 P-Tuning 是什么？它们与 Prefix Tuning 有什么关系？",
    answer: "这三者都是\"参数高效微调\"（PEFT）中基于\"软提示（Soft Prompt）\"的方法，核心思想是在输入层或每一层添加可学习的虚拟 token，冻结大部分或全部预训练参数。\n\n1. Prefix Tuning（前缀微调，2021）——在每一层 Transformer 的 K、V 矩阵前面拼接可学习的向量（Prefix），这些 prefix 向量是连续的 embedding 而非离散的 token。可训练参数只有前缀向量（约 0.1% 的原始参数量）。适用场景：GPT-2 等生成任务的微调。\n\n2. Prompt Tuning（提示微调，2021）——只在输入 Embedding 层前面拼接可学习的 prompt 向量，比 Prefix Tuning 更简洁只修改第一层。可训练参数更少（仅几个到几十个虚拟 token 的 embedding），预训练模型完全冻结。适用于中等规模以上的模型（>1B），小模型效果有限。推理时几乎无额外开销。\n\n3. P-Tuning（2021）和 P-Tuning v2（2022）——P-Tuning v1 在输入层拼接可学习的 prompt embedding 并通过 LSTM/MLP 编码。P-Tuning v2（更流行）借鉴 Prefix Tuning 在每一层都拼接可学习的 prompt 向量，相当于 Prefix Tuning 的通用化版本（支持 BERT 等 Encoder 模型）。v2 在 NLU 任务上表现与全参数微调相当。\n\n三者共同优势：参数效率极高，多任务支持（不同任务对应一组 prompt 参数），不修改原始模型冻结预训练权重。共同局限：prompt token 占用上下文窗口，表达能力有限（复杂任务效果不如 LoRA/全参数微调），对模型规模敏感（小模型效果较差）。\n\n实际中 LoRA 比这些方法更流行（推理零开销 + 可合并权重），但 Prompt/P-Tuning 在超低资源场景下仍有价值。",
    tags: ['Prompt Tuning', 'P-Tuning', 'Prefix Tuning', 'Soft Prompt', 'PEFT'],
    subTopic: '训练微调',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-17',
    category: 'llm',
    question: "全参数微调（Full Finetuning）和参数高效微调（PEFT）各有什么优缺点？如何选择？",
    answer: "全参数微调（Full Finetuning）：更新预训练模型的所有参数。\n优点：表达能力最强，效果上限最高（足够数据和计算资源下通常最优），完全适配可彻底改变行为以适应新领域。\n缺点：显存需求大（LLaMA-7B 全量微调需约 60-80GB 显存），灾难性遗忘风险（过度更新丢失预训练通用能力），成本高训练慢，多任务不友好（不同任务需存储完整模型副本），容易过拟合。\n\n参数高效微调（PEFT）：只更新模型的一小部分参数（<1%），冻结其余。代表方法有 LoRA/QLoRA、Adapter、Prefix Tuning/Prompt Tuning/P-Tuning、IA³。\n优点：显存需求极低（QLoRA 可在一张 24GB 显卡微调 65B 模型），训练快可训练参数少，抗灾难性遗忘（冻结主参数天然保留原始能力），多任务友好（不同任务仅存储对应 adapter/LoRA 权重几 MB 到几百 MB），推理零开销（LoRA 合并模式）。\n缺点：表达能力受限（与预训练数据差异极大领域可能力不从心），需要更长训练步数，方法选择需经验。\n\n选择建议：领域相近数据充足（>10K）用 LoRA；领域相近数据少（<1K）用 QLoRA/Prompt Tuning；领域差异大数据充足用全参数微调；多任务/多租户用 LoRA/Adapter；资源极度受限用 QLoRA；追求极致效果用全参数微调+多卡。趋势：绝大多数场景 LoRA/QLoRA 已足够，PEFT 方法不断逼近全参数微调效果。",
    tags: ['Full Finetuning', 'PEFT', 'LoRA', '微调策略'],
    subTopic: '训练微调',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-18',
    category: 'llm',
    question: "什么是 Catastrophic Forgetting（灾难性遗忘）？在 LLM 微调中如何缓解？",
    answer: "Catastrophic Forgetting（灾难性遗忘）是指神经网络在学习新任务时，之前学到的知识被覆盖/遗忘的现象。\n\n在 LLM 微调中的表现：微调后模型在某些通用能力上退化（如数学推理、代码生成、多语言能力），过度适应微调数据的分布和风格失去多样性，安全对齐能力被覆盖（恶意微调攻击）。\n\n为什么发生：梯度覆盖（新数据的梯度更新覆盖对旧知识重要的权重配置），分布偏移（微调数据与预训练数据分布差异大），过拟合（微调数据量小模型过度记忆），可塑性-稳定性困境（stability-plasticity dilemma）。\n\n缓解方案：\n1. PEFT 方法（最直接有效）——LoRA 只更新低秩矩阵冻结主体参数，Adapter 在层间插入小网络主模型不动，几乎完全消除灾难性遗忘\n2. 数据混合（Data Mixing）——在微调数据中混入 5%-10% 预训练数据，GPT-3 称之为 \"rehearsal\"\n3. KL 正则化——损失函数加入 KL(π_finetune || π_pretrained)，限制模型不要偏离太远，在 RLHF 的 PPO 阶段广泛使用\n4. 学习率控制——使用较小学习率（如 1e-5~2e-5），配合 warmup 和余弦退火\n5. Elastic Weight Consolidation（EWC）——估计每个参数对旧任务的重要性，重要参数的更新被约束，实践中计算成本高较少使用\n6. 多任务联合训练——同时训练所有任务强制模型在多个分布上保持性能\n7. 渐进式微调——先用大量通用指令数据微调再逐步过渡到特定领域\n\n评估遗忘：用常见 benchmark（MMLU、GSM8K、HumanEval 等）在微调前后分别评测，关注多个维度的退化。",
    tags: ['Catastrophic Forgetting', '灾难性遗忘', '知识保留', 'KL正则化'],
    subTopic: '训练微调',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-19',
    category: 'llm',
    question: "微调数据的质量如何影响模型效果？如何筛选高质量微调数据？",
    answer: "数据质量对微调效果的影响甚至大于数据数量，LIMA 论文用仅 1000 条高质量数据微调即超越了使用大量数据的模型。\n\n数据质量的影响维度：\n1. 准确性（Accuracy）——错误答案直接误导模型，且模型会自信地输出错误内容\n2. 多样性（Diversity）——覆盖不同任务类型、领域、风格，低多样性导致模型只在窄分布上表现好\n3. 一致性（Consistency）——相似问题应有相似风格和质量的回答，矛盾标注让模型学到混乱模式\n4. 难度梯度（Difficulty Gradient）——从简单到困难的任务分布帮助模型渐进学习\n5. 格式规范——统一格式（如 system prompt 使用、markdown 格式），格式混乱让模型对指令理解模糊\n\n筛选高质量微调数据的方法：\n1. 去重（Deduplication）——精确去重 + 模糊去重（MinHash、SimHash），过度重复导致过拟合\n2. 长度过滤——过短答案信息量不足，过长答案包含噪音\n3. 质量打分（Quality Scoring）——使用强模型（如 GPT-4）对数据多维度打分（准确性、完整性、有用性、流畅度）\n4. 难度筛选（Difficulty Filtering）——用当前模型推理，正确率为 40-60% 的样本最有价值（太简单已学会，太困难超出能力）\n5. 多样性筛选——用 embedding 聚类数据从每个簇采样，确保覆盖不同语义空间\n6. 格式检查——验证 instruction/input/output 格式完整性，过滤特殊字符异常数据\n7. 安全审查——过滤有害、偏见、违规内容，使用安全分类器批量筛选\n\n经验法则：数据质量高时 1000~10000 条可达到不错效果，质量中等时需要 5 万~10 万条，质量差时再多数据效果也有限。投入优先级：质量 > 多样性 > 数量。",
    tags: ['数据质量', '微调数据', '数据筛选', 'LIMA'],
    subTopic: '训练微调',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-20',
    category: 'llm',
    question: "什么是 SFT（Supervised Fine-Tuning）？它与 RLHF 阶段的关系是什么？",
    answer: "SFT（Supervised Fine-Tuning，监督微调）是 LLM 对齐流程的第一步，使用高质量的 (prompt, response) 对进行监督学习训练。\n\nSFT 的具体做法：数据为人工标注或高质量模型生成的 (指令, 回答) 对。损失函数为标准 Next Token Prediction 交叉熵损失，只在 response 部分计算 loss。目标是让模型学会按照指令格式生成符合人类期望的回答。产出为 SFT Model（又称 Instruct Model）。\n\nSFT 与 RLHF 的关系：\n1. SFT 是 RLHF 的前提和基础——RLHF 的 PPO 阶段需要 SFT Model 作为初始策略（π_0），SFT Model 的质量直接影响 RLHF 的上限\n2. 能力分工——SFT 负责\"能做\"（让模型学会遵循指令格式），RLHF 负责\"做好\"（让模型学会偏好对齐——更有用、更安全、更符合人类期望）\n3. RLHF 对 SFT 的提升——帮助模型区分\"能回答\"和\"应该怎样回答\"，对主观性问题（风格、语调）的优化特别有效，安全性提升显著减少有害内容\n4. SFT 的局限性——只能模仿训练数据中的回答分布无法超越，对所有正确回答一视同仁无法区分\"更好\"和\"还行\"，无法学习\"什么不应该说\"，容易出现 exposure bias\n\nSFT 数据的关键原则：质量远大于数量，多样性覆盖多领域多任务，一致性输出风格统一，安全性避免有害内容，难度适中包含一定比例复杂推理任务。",
    tags: ['SFT', '监督微调', 'RLHF基础', '指令遵循'],
    subTopic: '训练微调',
    difficulty: 'easy',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },

  // ============================================================
  // 推理部署 — 6 题 (llm-21 ~ llm-26)
  // ============================================================

  {
    id: 'llm-21',
    category: 'llm',
    question: "KV Cache 的原理是什么？它如何加速 LLM 推理？",
    answer: "KV Cache（键值缓存）是自回归生成中最关键的推理优化技术。\n\n为什么需要 KV Cache：自回归生成中每生成一个新 token 需要与所有历史 token 做 Attention。如果没有缓存，每次都要对完整序列（包括历史 token）重新计算 K 和 V，导致大量重复计算。计算量从 O(n) 变为 O(n²)（n 是生成 token 数）。\n\nKV Cache 的工作原理：\n1. 在 Prefill 阶段（处理输入 prompt）：一次性计算所有输入 token 的 K 和 V，将这些 K、V 存入缓存\n2. 在 Decode 阶段（逐个生成 token）：对于新生成的 token，只计算它自己的 Q、K、V。Q 与缓存的 K 做 Attention（新 Q × 旧 K），新 V 追加到 KV Cache 中。不需要重新计算历史 token 的 K 和 V\n3. 每个新 token 的计算量从 O(n·d²) 降至 O(d²)\n\n内存占用：每层需存储 K_cache 和 V_cache，大小 = 2 × batch_size × num_layers × seq_len × num_heads × head_dim × dtype_size。例：LLaMA-7B 在 batch=1 生成 2048 tokens 时 KV Cache 约 4GB（FP16）。这是推理显存的主要瓶颈之一。\n\n优化方向：\n- Multi-Query Attention（MQA）/ Grouped-Query Attention（GQA）：多个 Q 头共享同一个 K、V 头，减少 KV Cache 大小（LLaMA-2-70B 使用 GQA 减少到约 1/8）\n- PagedAttention（vLLM）：将 KV Cache 分成固定大小 page/block 管理，类似操作系统虚拟内存分页，减少内存碎片\n- 量化 KV Cache：将 K、V 用 INT8/FP8 存储，H2O 等方法只保留重要 KV pairs\n- FlashAttention：通过分块计算和重计算避免将完整注意力矩阵写入 HBM\n\nKV Cache 本质是\"以空间换时间\"，用显存存储中间结果避免重复计算。没有 KV Cache，LLM 推理几乎不可行。当前的推理优化大多围绕 KV Cache 的压缩和管理展开。",
    tags: ['KV Cache', '推理优化', '显存管理', '自回归'],
    subTopic: '推理部署',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-22',
    category: 'llm',
    question: "Beam Search 和 Greedy Decoding 的区别是什么？各自的优缺点？",
    answer: "Beam Search（束搜索）和 Greedy Decoding（贪心解码）是两种最基础的文本生成解码策略。\n\nGreedy Decoding（贪心解码）：每一步选择概率最高的单个 token，公式为 y_t = argmax P(y_t | y_{<t})。特点：简单、快速、确定性（同输入同输出）。时间复杂度每步 O(V)（V 为词表大小）。\n\nBeam Search（束搜索）：每步保留概率最高的 K 个候选序列（K 为 beam size）。对每个候选序列扩展 V 个可能的下一个 token 得到 K×V 个新序列，选择累积概率最高的 K 个作为下一轮候选。结束时输出累积概率最高的完整序列。时间复杂度每步 O(K·V)。\n\n优缺点对比：Greedy 生成质量较低可能陷入局部最优、多样性极低但速度快显存占用低；Beam Search 生成质量较高但多样性低（K 个序列往往相似）、速度慢每步算 K 条路径、显存占用高（K 套 KV Cache）。\n\nBeam Search 的问题：长度偏差（短序列天然累积概率更高，需长度归一化）、多样性缺失（需 Diverse Beam Search）、不适用于开放式生成（对话创意写作中概率高的回答往往平庸）。\n\n现代 LLM 解码策略演进：Top-K Sampling（从概率最高 K 个 token 中采样）、Top-P（Nucleus）Sampling（从累积概率达 P 的最小集合中采样）、Temperature（调整 logits 锐度）。Beam Search 在现代对话模型中较少使用，更多用于有标准答案的任务（代码生成、翻译）。\n\n实际工程建议：翻译/代码生成用 Beam Search(beam=4~8)或 Greedy，对话/创意写作用 Top-P sampling(p=0.9, temperature=0.7~1.0)。",
    tags: ['Beam Search', 'Greedy Decoding', '解码策略', '采样方法'],
    subTopic: '推理部署',
    difficulty: 'easy',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-23',
    category: 'llm',
    question: "Temperature 参数在 LLM 推理中的作用是什么？如何选择合理的值？",
    answer: "Temperature（温度）是在 softmax 之前应用于 logits 的缩放参数，用于控制生成文本的随机性。\n\n数学定义：P_i = exp(z_i / T) / Σ_j exp(z_j / T)，其中 T 是 temperature 参数。\n\n作用机制：\n- T → 0：exp(z/T) 趋近于极端分布，softmax 趋近于 argmax（退化为 Greedy），只有概率最高的 token 有显著概率，输出高度确定但缺乏多样性\n- T = 1：标准 softmax，不改变原始分布\n- T → ∞：exp(z/T) 趋近于均匀分布，所有 token 概率趋于相等，输出高度随机可能混乱\n- 0 < T < 1：锐化分布抑制低概率 token；T > 1：平滑分布提升低概率 token\n\n如何选择合理值：代码生成 T=0.0~0.2（需要确定性和正确性）；数学/逻辑推理 T=0.0~0.3（避免随机性干扰推理链）；事实问答 T=0.2~0.5（需要准确但可接受语言变化）；通用对话 T=0.7~0.9（平衡流畅性和多样性）；创意写作 T=0.8~1.2（需要多样性鼓励出人意料的表达）；翻译 T=0.2~0.5（有标准答案但语言表达可微调）。\n\n工程实践：Temperature 与 Top-P 常配合使用——常见组合 T=0.7 + Top-P=0.9（OpenAI 默认）。动态 Temperature：推理型任务用低温，创意型任务用高温。温度采样的陷阱：高温 + 长序列 = \"drift\" 现象文本逐渐偏离主题；低温 + 低 Top-K = 容易生成重复短语。配合重复惩罚（repetition_penalty）使用。Temperature 只适用于 Sampling 解码，对 Greedy/Beam Search 无影响。",
    tags: ['Temperature', '采样', '随机性控制', '解码参数'],
    subTopic: '推理部署',
    difficulty: 'easy',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-24',
    category: 'llm',
    question: "模型量化（Quantization）的原理是什么？GPTQ 和 AWQ 有什么区别？",
    answer: "模型量化是将模型的浮点参数（FP32/FP16）转换为低位宽整数（INT8/INT4 等）以压缩模型、加速推理的技术。\n\n基本原理：\n1. 量化映射——对称量化：x_int = round(x_float / scale)；非对称量化：x_int = round((x_float - zero_point) / scale)。反量化：x_float ≈ scale · x_int + zero_point\n2. 为什么有效——减少显存（FP16→INT4 模型大小减少 4 倍），加速计算（INT8/INT4 矩阵乘法利用 Tensor Core 等硬件加速），减少内存带宽\n3. 量化粒度——Per-Tensor（整个矩阵共用 scale）、Per-Channel（每输出通道独立 scale 精度更高）、Per-Group（通道分组每组共用 scale）\n\nGPTQ（GPT Post-Training Quantization）：核心思想基于 Optimal Brain Quantization（OBQ），逐列量化权重并补偿误差。算法流程：使用少量校准数据（128 条样本）做前向传播，对权重矩阵每一列量化并计算误差，通过海森矩阵信息将误差补偿到剩余未量化列上。特点：精度高（利用二阶信息最小化误差），速度快（不需训练/微调），但量化过程本身较慢（需 GPU 上迭代处理所有权重矩阵）。\n\nAWQ（Activation-aware Weight Quantization）：核心思想是并非所有权重同等重要——保存对激活值影响大的权重通道的精度。算法流程：分析激活值分布找出重要权重通道，对这些通道进行 salient channel scaling（先放大激活值再量化以减少相对误差），量化后调整恢复关系。特点：实现简单（不需要海森矩阵计算），速度快（比 GPTQ 快一个数量级），精度好在同等位宽下精度相当甚至更好。\n\nGPTQ vs AWQ 对比：GPTQ 核心为二阶误差补偿需要校准数据、量化速度慢但精度好 ~1% PPL 下降、实现复杂度高需要 Cholesky 分解；AWQ 核心为激活感知通道重要性加权需要少量数据统计激活值、量化速度快、精度同等好 ~1% PPL 下降、实现简单。生态支持：GPTQ→AutoGPTQ/ExLlamaV2；AWQ→vLLM/llama.cpp/TGI。\n\n其他量化方法：Bitsandbytes 的 LLM.int8() 混合精度量化，GGUF/GGML（llama.cpp CPU 推理），SmoothQuant（通过平滑因子转移量化难度），FP8 量化（NVIDIA H100 原生支持）。",
    tags: ['Quantization', '量化', 'GPTQ', 'AWQ', 'INT4'],
    subTopic: '推理部署',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-25',
    category: 'llm',
    question: "Speculative Decoding（推测解码）的原理是什么？它如何加速 LLM 推理？",
    answer: "Speculative Decoding（推测解码）是一种\"用草稿模型加速主模型推理\"的技术，由 DeepMind 和 Google 在 2023 年独立提出。\n\n核心思想：自回归生成的主要瓶颈是每次只能生成 1 个 token，但每个 token 的验证可以并行完成（因为验证不需要自回归）。用一个快速的小模型生成多个\"候选 token\"，再用大模型一次性验证。如果候选 token 正确，则等效于一次生成了多个 token。\n\n工作流程：\n1. Draft（草稿阶段）——使用一个很小的 Draft Model（参数量为主模型的 1/10~1/100），自回归生成 K 个候选 token（如 K=5），这一步很快因 Draft Model 很小\n2. Verify（验证阶段）——主模型接收 K 个候选 token，一次性前向传播得到每个位置的概率分布。对位置 i 比较：如果 p_main(token_i) >= p_draft(token_i) 接受该 token，如果 p_main(token_i) < p_draft(token_i) 以概率 p_main/p_draft 接受否则拒绝\n3. 匹配/纠正——如果某位置 token 被拒绝，主模型从自己的分布中重新采样该位置及之后的所有 token。结果保证与主模型自回归生成的分布完全相同（无损加速）\n\n为什么可以加速：主模型验证 K 个 token 的 cost ≈ 生成 1 个 token（矩阵乘法并行）。如果平均接受 3 个 token 则加速约 3 倍。整体生成 N 个 token 需要主模型运行约 N/accept_length 次。\n\n关键要素：Draft Model 选择（最好与主模型同词表可直接复用 embedding），接受率直接影响加速比。变体和改进：Medusa（不依赖独立 Draft Model 在主模型上加多个 Medusa 头预测后续 token）、Eagle（特征级别草稿模型接受率可达 0.7+）、Staged Speculative Decoding（级联多个 Draft Model）、Lookahead Decoding（Jacobi 迭代法不需要单独 Draft Model）。\n\n实际效果：标准配置 K=5 接受率 70% 加速 2x~3x，高质量 Draft Model 接受率 85%+ 加速 3x~4x。当前主流推理框架（vLLM、TGI）已支持 Speculative Decoding。",
    tags: ['Speculative Decoding', '推测解码', 'Draft Model', '推理加速'],
    subTopic: '推理部署',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-26',
    category: 'llm',
    question: "vLLM 的 PagedAttention 机制是什么？它解决了什么问题？",
    answer: "PagedAttention 是 vLLM 推理框架提出的 KV Cache 管理算法，灵感来源于操作系统的虚拟内存分页机制。\n\nKV Cache 传统管理方式的问题：\n1. 内存碎片化——传统做法为每个序列预分配最大长度的连续 KV Cache 空间，但生成的实际长度不确定，预留空间造成大量浪费（内部碎片），多个序列间分配/释放产生外部碎片\n2. 无法共享——Beam Search 中不同 beam 的序列共享相同前缀，传统实现为每个 beam 维护独立完整 KV Cache 造成大量冗余\n3. 显存利用率低——显存往往成为推理吞吐瓶颈（而非计算），低效 KV Cache 管理导致 batch size 受限\n\nPagedAttention 的解决方案：\n1. 分页管理——将 KV Cache 划分为固定大小 Page/Block（如 block_size=16 tokens），每个 block 存储连续 16 个 token 的 K 和 V。序列的 KV Cache 通过 block table 记录哪些 block 属于该序列以及顺序。不需要物理上连续内存，只需逻辑 block table 映射\n2. 内存效率——block 大小固定消除外部碎片（所有分配是 block 整数倍），只有序列实际使用的 block 才分配内存\n3. KV Cache 共享（Copy-on-Write 风格）——Beam Search 中多个 beam 可共享前缀 block，当某个 beam 生成不同 token 时只为分叉后新 block 分配内存。共享 block 通过引用计数管理，计数为 0 时回收\n4. 显存利用率提升——更好显存管理 → 更大 batch size → 更高吞吐量。vLLM 论文报告同等延迟下吞吐量提升 2x~4x\n\n实现细节：Block Table 通常所有层共享同一映射。每个 block 存储 shape = [num_heads, block_size, head_dim] 的 tensor。Attention 计算时需根据 block table 重新组装 K 和 V，需要自定义 CUDA kernel。FlashAttention 的 tiling 策略与 PagedAttention 天然兼容。\n\n影响和生态：几乎所有主流 LLM 推理框架（TGI、TensorRT-LLM）都借鉴了 PagedAttention，已成为 serving 系统标配技术。后续改进：vAttention（PyTorch 原生支持）、FlashInfer 等。",
    tags: ['vLLM', 'PagedAttention', 'KV Cache管理', '内存优化'],
    subTopic: '推理部署',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },

  // ============================================================
  // Agent — 6 题 (llm-27 ~ llm-32)
  // ============================================================

  {
    id: 'llm-27',
    category: 'llm',
    question: "什么是 ReAct 框架？它如何让 LLM 进行推理和行动的交替？",
    answer: "ReAct（Reasoning + Acting）是 Google 在 2022 年提出的框架，让 LLM 交替进行\"思考（Reasoning）\"和\"行动（Action）\"以完成复杂任务。\n\n核心理念：传统 CoT（Chain-of-Thought）只推理不行动依赖模型内部知识可能产生幻觉；Act-only 只行动不思考缺乏规划容易在错误路径上浪费步骤。ReAct 将两者结合——思考（Thought）分析当前状态规划下一步判断是否需要更多信息，行动（Action）调用外部工具（搜索、计算器、API 等），观察（Observation）获取行动结果（外部反馈），循环执行 Thought→Action→Observation→Thought→...\n\n框架流程示例（问答）：Question: \"2023 年诺贝尔物理学奖得主是哪国人？\" → Thought: 需要查找得主信息 → Action: Search[\"2023 Nobel Prize Physics winners\"] → Observation: 得主为 Pierre Agostini, Ferenc Krausz, Anne L'Huillier → Thought: 需分别查询三位得主国籍 → Action: Search each winner → Observation: 分别是法国人、匈牙利人、法国-瑞典人 → Final Answer。\n\nReAct 的关键优势：\n1. 减少幻觉——模型通过外部工具验证信息，不依赖内部可能有误的知识\n2. 可解释性——Thought-Action-Observation 轨迹清晰可读，人类可理解每一步\n3. 动态规划——不预先决定全部步骤，每一步根据观察动态调整\n4. 任务分解——复杂问题分解为多个子任务各自调用不同工具\n5. 与 Tool-Use 天然结合——是当前 LLM Agent 的基础框架，Function Calling 可看作 ReAct 的特化版本\n\n实现要点：Few-shot Prompting 给出 ReAct 格式示例，正确解析 Thought/Action/Observation 边界，明确每个工具名称/输入参数/输出格式，设置停止条件（最大步数或 Final Answer）。",
    tags: ['ReAct', 'Agent框架', '推理+行动', 'Tool-Use'],
    subTopic: 'Agent',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-28',
    category: 'llm',
    question: "Function Calling 的机制是什么？LLM 如何实现\"调用函数\"这一能力？",
    answer: "Function Calling 是让 LLM 能够使用外部工具/API 的机制，其本质不是 LLM 真正\"执行\"函数，而是 LLM 生成结构化的函数调用参数。\n\n完整机制（以 OpenAI 为例）：\n1. 定义函数（Function Definitions）——在请求中告知 LLM 可以使用哪些函数。每个函数包含 name、description、parameters（JSON Schema 格式，如 {name: \"get_weather\", description: \"获取城市天气\", parameters: {city: string, unit: enum[celsius,fahrenheit]}}）\n2. LLM 决定是否调用函数——LLM 分析用户输入判断是否需要调用函数，如果需要则选择最合适的函数并生成结构化 JSON 参数\n3. 外部执行函数——开发者代码接收到 LLM 的函数调用请求，实际执行函数（调用天气 API、数据库查询等），获取真实结果\n4. 将结果反馈给 LLM——将函数执行结果以 role: \"tool\" 格式添加到对话历史，LLM 看到结果后生成最终自然语言回答\n\n为什么 LLM 能做到这一点：\n1. 指令微调——使用大量 (user_message, function_call, tool_result, final_answer) 格式数据进行微调，模型学会识别\"何时调用函数\"及\"如何生成正确参数\"\n2. Prompt Engineering——开源模型可通过精心设计 system prompt 实现函数调用，Few-shot 示例帮助模型理解正确行为\n3. 约束生成（Constrained Decoding）——在生成函数参数时限制 token 采样空间，通过修改 logits（mask 掉不合法 token）保证生成的 JSON 语法正确类型匹配\n\n实际工程注意事项：函数描述要足够清晰（不清晰的描述导致选择错误函数），参数要简单避免深层嵌套，错误处理（执行失败时反馈错误信息让模型重试），安全性（永远不要让 LLM 直接执行危险操作，所有函数调用通过沙箱或权限控制执行），并行调用（一个 user message 可能触发多个独立函数调用并行执行减少延迟）。",
    tags: ['Function Calling', 'Tool-Use', '结构化输出', 'API调用'],
    subTopic: 'Agent',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-29',
    category: 'llm',
    question: "Tool-Use 模式在 LLM Agent 中如何实现？有哪些关键设计考虑？",
    answer: "Tool-Use（工具使用）是 LLM Agent 的核心能力，让模型超越纯文本生成，与外部世界交互。\n\n实现模式：\n1. 工具定义（Tool Definition）——每个工具明确接口：name（唯一标识）、description（功能描述，这是 LLM 选择工具的关键依据）、parameters（输入参数 schema）、可选 output_schema（返回值格式）\n2. 工具注册（Tool Registry）——维护可用工具列表，LLM 决策时\"查看\"所有可用工具。工具可包括搜索 API、计算器、数据库查询、代码执行器、文件系统操作等\n3. 工具选择与调用——LLM 根据上下文决定使用哪个工具及参数，应用层实际执行工具调用，结果以格式化消息返回\n4. 多轮工具使用——一个任务可能需要多个工具调用，每次结果追加到对话历史，LLM 根据累积信息决定下一步\n\n关键设计考虑：\n1. 工具描述清晰度——差：\"get_data: 获取数据\"；好：\"search_database: 在用户数据库中搜索客户记录，输入 customer_name 和 date_range\"。描述应包含功能、输入参数、返回值、使用场景\n2. 工具粒度——太粗一个万能工具 → LLM 难以生成正确参数；太细数百个微小工具 → 选择困难决策开销大。最佳：合理抽象每个工具职责单一清晰（Unix 哲学），简单工具可组合成复杂工作流\n3. 错误处理与重试——工具执行可能失败，错误信息应结构化让 LLM 可理解，LLM 根据错误调整参数重试，设置最大重试次数防无限循环\n4. 安全与权限控制——分级权限：只读工具自动执行，低风险写操作自动执行，高风险操作需用户确认。沙箱执行代码/命令在隔离环境运行\n5. 上下文管理——工具调用和结果迅速消耗上下文窗口。策略：截断/摘要旧调用，重构工具输出为简洁格式，滑动窗口保留最近交互\n6. 工具调用并行化——无依赖的工具调用应并行执行减少总响应时间\n7. 工具调用格式——OpenAI Function Calling 用 JSON，Anthropic Tool-Use 用 XML-like 结构化输出，自定义格式用特殊 token 包裹\n\n进阶：MCP（Model Context Protocol）是 Anthropic 提出的标准化协议，统一 LLM 与外部工具/数据源的交互方式，类似\"LLM 世界的 HTTP 协议\"。工具提供者实现 MCP Server，LLM 作为 MCP Client。",
    tags: ['Tool-Use', '工具调用', 'Agent设计', 'MCP'],
    subTopic: 'Agent',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-30',
    category: 'llm',
    question: "多 Agent 协作（Multi-Agent Collaboration）是如何实现的？有哪些经典框架？",
    answer: "多 Agent 协作是指多个 LLM Agent（各自可能有不同角色、能力和工具）协同工作以完成复杂任务。\n\n协作模式：\n1. 顺序协作（Sequential）——Agent A 完成子任务 → 输出交给 Agent B → ... → 最终输出，类似流水线适合有先后依赖的任务（如需求分析→架构设计→代码编写）\n2. 层级协作（Hierarchical）——\"管理者 Agent\"负责任务分解和分配，多个\"工作者 Agent\"执行具体子任务，管理者汇总结果处理冲突\n3. 辩论式协作（Debate）——多个 Agent 独立生成答案，Agent 间互相审查和质疑（critique），通过多轮辩论收敛到更好答案\n4. 投票式协作（Voting/Ensemble）——多个 Agent 独立解决，对结果多数投票或加权融合\n\n经典多 Agent 框架：\n1. AutoGPT（2023）——由用户给出总体目标，LLM 自主分解任务、选择工具、执行、评估结果。特点：完全自主但容易陷入循环或偏离目标，代表自主完成任务的开创性尝试\n2. MetaGPT（2024，深度赋智）——模拟软件公司多角色协作，定义 Product Manager/Architect/Project Manager/Engineer 等角色。每个 Agent 有自己的 SOP（标准操作流程），通过结构化消息传递减少\"幻觉对话\"，可端到端生成完整软件项目\n3. ChatDev（2023，清华大学）——模拟软件开发流程，多阶段：Designing→Coding→Testing→Documenting，每个阶段由不同角色 Agent 参与\n4. CrewAI/AutoGen（2024，Microsoft）——通用多 Agent 框架。AutoGen 支持自定义 Agent 角色、工具、对话模式，支持 Human-in-the-Loop，Group Chat 中多 Agent+人类群聊协作\n\n关键设计问题：通信协议（自由对话灵活但发散 vs 结构化消息可控但约束）、上下文共享（全局共享消耗 token vs 分层只看相关部分 vs 摘要传递）、冲突解决（管理者仲裁/投票/置信度加权）、成本控制（多 Agent 的 token 消耗是单 Agent 数倍需轻量级 Agent 负责简单任务）、任务分解质量（多 Agent 上限取决于如何拆解任务）。",
    tags: ['Multi-Agent', '协作', 'AutoGPT', 'MetaGPT', 'AutoGen'],
    subTopic: 'Agent',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-31',
    category: 'llm',
    question: "AutoGPT 和 MetaGPT 这类 Agent 框架的核心设计思想是什么？有哪些共性和差异？",
    answer: "AutoGPT 和 MetaGPT 都是 LLM Agent 的标杆性框架，代表了两种不同的 Agent 设计哲学。\n\nAutoGPT（2023年3月）——定位为通用自主 Agent。核心思想：\"给一个目标，Agent 自主完成所有步骤\"。技术架构：主循环 Thought→Action→Observation→Thought→...，内置 Memory（向量数据库存储长期记忆），丰富工具集（搜索、文件操作、代码执行、网页浏览），Chain of Thought 提示。优点：完全自主无需人类干预，可处理开放式目标，记忆系统处理长期任务。缺点：容易陷入循环，任务完成率低（~15%），Token 消耗大，缺乏结构化任务管理。\n\nMetaGPT（2023年12月）——定位为面向软件工程的多 Agent 协作系统。核心思想：\"模拟人类团队协作，用 SOP 约束 Agent 行为\"。技术架构：多角色 Agent（PM/Architect/PM/Engineer/QA），结构化输出（每个角色输出符合模板的结构化文档），SOP（严格阶段划分和输入/输出规范），共享消息池+订阅模式。优点：任务完成率高，输出质量高且一致，Token 效率更高，可生成完整可运行软件项目。缺点：灵活性不如 AutoGPT，领域绑定主要适用软件开发，缺乏自主探索能力。\n\n共性：LLM 驱动核心推理引擎，工具增强集成外部工具，记忆系统维护任务相关上下文和历史，循环执行多轮迭代式工作流，Prompt Engineering 为核心引导行为。\n\n差异：设计哲学上 AutoGPT 自主探索 vs MetaGPT 结构化协作；Agent 数量上单自主 Agent vs 多角色化 Agent；任务约束上松散仅目标描述 vs 严格 SOP+模板；适用场景上开放式/探索性任务 vs 软件开发等结构化任务。\n\n趋势与融合：现代 Agent 框架（AutoGen/CrewAI）尝试取两者之长，保持一定结构同时允许 Agent 自主决策。\"Planning+Execution\"双层架构：Planner Agent 制定结构化计划，Executor Agent 自主灵活执行。加入 Human-in-the-Loop 在关键决策点人类确认。",
    tags: ['AutoGPT', 'MetaGPT', 'Agent架构', '框架对比'],
    subTopic: 'Agent',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-32',
    category: 'llm',
    question: "LLM Agent 中的 Planning（规划）能力是如何实现的？有哪些常用方法？",
    answer: "Planning（规划）是 LLM Agent 的核心能力，指将复杂任务分解为有序的子任务序列并逐步执行。\n\n主要方法：\n1. Plan-and-Solve（先规划后执行）——第一步 LLM 分析任务生成完整执行计划，第二步 LLM 按计划逐步执行每个子任务。优点：整体视角避免在细节中迷失。缺点：计划可能不准确需重规划机制\n2. ReAct-style（边思考边执行）——交替进行 Thought 和 Action，每一步根据观察动态调整。优点：灵活适应性强化应对意外。缺点：可能缺乏全局视野效率可能低\n3. Tree-of-Thought（ToT）——维护多条推理路径（树结构），在关键决策点生成多个候选方案并评估（用 LLM 自身或外部验证器），结合 BFS/DFS 搜索策略选择最优继续。优点：探索多种可能找到更优解。缺点：计算成本高 token 消耗大\n4. Hierarchical Planning（分层规划）——高层规划生成粗粒度里程碑，低层规划针对每个里程碑生成细粒度步骤。优点：降低单次规划复杂度\n5. Plan + Critique（规划 + 审视）——Plan Agent 生成初始计划，Critique Agent 审视计划找出漏洞和风险，多轮迭代改进计划。优点：提升计划质量减少执行中失败\n6. Plan + Decomposition（任务分解）——使用\"问题分解\"技巧：子问题分解（可拆为哪几个子问题）、时间分解（首先做什么然后呢）、功能分解（需要哪些不同功能模块）\n\n关键技术组件：\n- Plan Representation（计划表示）：自然语言列表最灵活难解析、结构化 JSON 易程序处理、伪代码适合代码任务、有向无环图 DAG 表示子任务依赖\n- Plan Validation（计划验证）：静态验证（执行前检查逻辑一致性依赖完整性）、动态验证（根据执行结果判断计划是否仍合理）、LLM 自验证（\"这个计划有什么问题吗？\"）\n- Plan Adaptation（计划调整）：Replanning（子任务失败时重新规划剩余部分）、Plan Repair（局部修复而非全盘重来）\n- Memory for Planning（记忆辅助规划）：短期记忆当前任务上下文和已完成步骤，长期记忆（向量数据库）类似任务历史计划 RAG 检索，Skill Memory 已学会技能可复用\n\nPlan 质量提升技巧：Few-shot 示例展示良好规划过程，要求 LLM \"写出计划考虑可能失败情况\"，加入约束\"计划不得超过 5 步骤\"，自我反思\"执行前重新审视计划合理性\"，让一个 LLM 规划另一个 LLM 执行（角色分离减少 bias）。",
    tags: ['Planning', '任务分解', 'Agent规划', 'Tree-of-Thought'],
    subTopic: 'Agent',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },

  // ============================================================
  // RAG — 5 题 (llm-33 ~ llm-37)
  // ============================================================

  {
    id: 'llm-33',
    category: 'llm',
    question: "RAG（Retrieval-Augmented Generation）的完整流程是什么？每个阶段的关键设计是什么？",
    answer: "RAG（检索增强生成）是将信息检索与文本生成相结合的技术，让 LLM 能基于外部知识库回答问题，减少幻觉。\n\n完整流程分为离线索引和在线查询两大阶段。\n\n一、离线索引阶段（Indexing Pipeline）：\n1. 文档加载（Document Loading）——从多种来源加载文档（PDF、网页、数据库、API），解析清洗去除噪声统一格式\n2. 文档分割（Chunking）——将长文档分割为合适大小 chunk。策略：固定长度、语义分割、递归分割。保留重叠（overlap）防止关键信息被切断\n3. 向量化（Embedding）——使用 Embedding Model 将每个 chunk 编码为向量，存储在向量数据库中\n4. 索引构建（Index Building）——在向量数据库中构建索引（HNSW、IVF 等）支持高效相似度搜索（ANN）\n\n二、在线查询阶段（Query Pipeline）：\n1. 查询处理——接收用户问题。可选：查询改写（Query Rewriting）将口语化问题改写为更适合检索形式、查询扩展（Query Expansion）用同义词扩展查询、HyDE（Hypothetical Document Embedding）先生成假设答案再检索\n2. 检索（Retrieval）——将查询向量化，在向量数据库中检索 Top-K 个最相似 chunk（粗排）。可选结合关键字搜索（BM25）做混合检索\n3. 重排序（Reranking）——对 Top-K 候选 chunk 进行精细排序，使用 Cross-Encoder Reranker Model，选出最相关前 N 个\n4. 上下文组装（Context Assembly）——将选中 chunk 拼接成最终上下文，处理去重/排序/截断，可加入引用标记\n5. 生成（Generation）——构建最终 Prompt = System Prompt + 检索上下文 + 用户问题，LLM 基于上下文生成答案\n\n关键设计点：\n- Chunk Size：太小（<256 tokens）语义不完整，太大（>1024 tokens）包含噪音。经验值 512~1024 tokens\n- Top-K：太小遗漏关键信息，太大噪声增加。经过 rerank 后保留 5~10 个 chunk\n- Embedding Model：通用文本用 text-embedding-3-large/bge-large，中文优化用 bge-large-zh/m3e，多语言用 multilingual-e5-large\n- 向量数据库：轻量级用 Chroma/FAISS，生产级用 Milvus/Qdrant/Weaviate/Pinecone\n- 评估：检索质量用 Recall@K/MRR/NDCG，生成质量用 Faithfulness/Answer Relevance，框架如 RAGAS",
    tags: ['RAG', '检索增强生成', '文档切分', '向量检索'],
    subTopic: 'RAG',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-34',
    category: 'llm',
    question: "RAG 中的 Chunk（文档切分）策略有哪些？如何选择合理的切分方案？",
    answer: "Chunk 策略是 RAG 系统中关键的设计决策，直接影响检索质量和最终答案准确性。\n\n常见切分策略：\n1. 固定长度切分——每隔 N 个字符切一刀，参数 chunk_size（如 512）、chunk_overlap（如 50）。优点：实现简单速度快大小均匀。缺点：可能在句子/段落中间切断破坏语义。适用：初步实验格式统一文档\n2. 递归字符切分——按优先级依次尝试分隔符：段落(\\n\\n)→句子(。！？)→词(空格)→字符。LangChain 的默认策略。优点：尽量保持语义单元完整性。缺点：仍可能在某些边界切断\n3. 语义切分——使用 NLP 工具句子边界检测，将语义完整句子/段落作为 chunk。可根据 embedding 相似度判断语义边界。优点：语义完整性最好。缺点：大小不均实现复杂\n4. 基于文档结构切分——根据文档天然结构：Markdown 标题层级、HTML 标签、PDF 章节。保留结构信息（chunk 标题路径）作为元数据。优点：结构信息有助于检索和理解\n5. Agentic Chunking——使用 LLM 分析文档结构决定最优切分点，LLM 生成每个 chunk 摘要。优点：最智能质量最高。缺点：成本高速度慢\n6. Semantic Chunking（基于 embedding）——计算相邻句子 embedding 相似度，低于阈值时切分（表示话题变化）。优点：自动适应语义边界\n7. Small-to-Big / Parent-Child Retrieval——小 chunk 检索（200 tokens）大 chunk 返回（1000 tokens）。\"句子窗口检索\"：检索到目标句子返回其周围 N 句上下文。优点：检索精度+上下文完整性最佳平衡\n\n如何选择切分方案：\n- 考虑文档类型：技术文档（有结构）用结构切分+标题元数据；知识库文章用语义切分+小到大检索；法律/金融文档（精确要求）小 chunk 256-512+大 overlap；对话记录按对话轮次切分\n- Chunk Size 经验：通用文本 512-1024 tokens；代码 256-512 行或按函数/类；中文 300-800 字符（信息密度高于英文）\n- Chunk Overlap：一般 10%-20% chunk_size，最小 50 tokens\n- 元数据保留：记录来源文档、页码、章节标题，可用于过滤/排序/引用\n\nAdvanced 技巧：Hybrid chunk sizes 同时维护多个大小根据查询类型选择；Contextual Retrieval（Anthropic）为每个 chunk 预生成文档级别上下文的描述；Late Chunking 先对整个文档做 embedding 再切分保持全局上下文；Multi-representation indexing 为同一内容存储多个版本（原文+摘要+QA 对）。",
    tags: ['Chunk策略', '文档切分', '语义切分', 'Parent-Child'],
    subTopic: 'RAG',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-35',
    category: 'llm',
    question: "RAG 中如何选择 Embedding Model？有哪些关键评估指标？",
    answer: "Embedding Model 将文本映射为稠密向量，是 RAG 系统的核心组件。\n\n选择的关键因素：\n1. 语言支持——英语：text-embedding-3-large/bge-large-en-v1.5/GTE-large；中文：bge-large-zh-v1.5/stella-mrl-large-zh/m3e-large；多语言：multilingual-e5-large/bge-m3/text-embedding-3-large\n2. 文本长度支持——大多数旧模型 max_seq_length=512 tokens，新一代：bge-m3(8192)、text-embedding-3(8191)、jina-embeddings-v2(8192)。长文本 embedding 适合检索长文档\n3. 向量维度——高维(1024-4096)精度高但存储计算成本大；低维(256-768)速度快存储小精度略低。部分模型支持 Matryoshka 表示可灵活截取前 N 维。典型：bge-large-zh-v1.5 1024 维，text-embedding-3 可指定 256/512/1024/1536/3072\n4. 领域适配——通用文本用 bge/e5/text-embedding 系列；代码用 CodeBERT/UniXcoder；生物医学用 BioBERT/PubMedBERT；法律/金融可能需要领域微调 embedding 模型\n5. 是否需要微调——开源模型（bge/e5）可在领域数据微调；闭源 API（OpenAI/Cohere）无法微调。bge 提供微调脚本和最佳实践\n\n评估指标：检索质量——Recall@K、Precision@K、MRR（Mean Reciprocal Rank 第一个相关结果排名倒数）、NDCG（考虑排名位置的相关性指标）、Hit Rate（至少一个相关结果被检索到的比例）。MTEB（Massive Text Embedding Benchmark）是大规模标准化 embedding 评估基准，包含分类/聚类/配对/重排/检索/STS 任务。C-MTEB 是针对中文的版本。\n\n主流模型对比（2024）：text-embedding-3-large 3072 维 8191 长度多语言 API；bge-m3 1024 维 8192 长度多语言开源；bge-large-zh-v1.5 1024 维 512 长度中文开源；GTE-Qwen2-7B-instruct 3584 维 32768 长度多语言开源。\n\n选择流程：确定主要语言→锁定候选模型→确定最大文本长度过滤→查看 MTEB/C-MTEB 排行榜初筛 Top3→在自有数据评估→考虑推理成本和延迟决定 API vs 本地部署。注意调用 API 时统一设置 query 前缀（e5 系列有特殊前缀要求，bge 的 query 和 passage 使用不同指令）。",
    tags: ['Embedding Model', '向量模型', 'MTEB', '检索评估'],
    subTopic: 'RAG',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-36',
    category: 'llm',
    question: "RAG 中的 Reranking（重排序）是什么？为什么需要它？",
    answer: "Reranking（重排序）是 RAG pipeline 中的第二阶段检索优化，用更精确但更慢的模型对初检结果进行重新排序。\n\n为什么需要 Reranking：\n1. 第一阶段检索（Embedding-based Retrieval）的局限性——Bi-Encoder（双塔模型）查询和文档独立编码后计算余弦相似度，查询和文档间没有细粒度交互（cross-attention），精度有上限只能捕捉粗粒度语义相似度\n2. Reranking 的互补作用——Cross-Encoder（交叉编码器）将查询和文档拼接作为输入，通过 Attention 实现查询和文档间的细粒度交互，精度更高。缺点：计算速度慢（每个 query-chunk pair 都需完整推理）\n\n因此：粗排（Embedding）快但不准，精排（Reranker）准但不快。组合使用：Embedding 检索 Top-K（如 100 个）→ Reranker 精选 Top-N（如 5 个）。\n\nReranker 工作原理：\n1. Cross-Encoder Reranker——输入 f\"[CLS] Query [SEP] Document [SEP]\"，输出相关性分数（scalar）。通过全连接层将 [CLS] token 表示映射为分数。训练使用 (query, positive_doc, negative_doc) 三元组。代表：bge-reranker、Cohere Rerank、Jina Reranker\n2. LLM-based Reranker——使用 LLM（如 GPT-4）本身作为 Reranker，Prompt：\"在以下文档中哪一篇最能回答这个问题？请排序。\"优点利用 LLM 强大理解能力，缺点成本高延迟大\n3. Learned Sparse Reranker——结合稠密和稀疏检索对 BM25 检索结果细粒度重排\n\nReranking 效果：显著提升检索精度（NDCG@10 可提升 5-15 个百分点），允许第一阶段检索更多候选（K 从 5 到 100+），对长尾/专业领域查询提升尤为显著。\n\nReranker 选型：bge-reranker-v2-m3 多语言开源目前 SOTA；bge-reranker-large 中/英开源经典；Cohere Rerank v3 多语言商业 API；Jina Reranker v2 多语言支持更长文本；mxbai-rerank-base 英语开源轻量级。\n\n实施建议：第一阶段 Embedding 检索 Top-50~100，Reranker 对 Top-50 重排取 Top-5~10，最终给 LLM 的是经精排的高质量上下文。性能优化：缓存相同 query 的 rerank 结果，批处理多个 query-doc pairs，延迟 Rerank 简单查询可跳过。其他排序信号可融合：文档时效性（时间衰减权重）、文档权威性、用户反馈。",
    tags: ['Reranking', '重排序', 'Cross-Encoder', '检索精度'],
    subTopic: 'RAG',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-37',
    category: 'llm',
    question: "什么是 Hybrid Search（混合检索）？Sparse 和 Dense 检索如何互补？",
    answer: "Hybrid Search（混合检索）是将稀疏检索（Sparse/BM25）和稠密检索（Dense/Embedding）结合的方法，取两者之长。\n\nSparse Retrieval（稀疏检索/关键词检索）：代表 BM25、TF-IDF。原理：基于词频统计和倒排索引，查询和文档表示为稀疏向量，匹配精确关键词和短语。优点：精确匹配专有名词/缩写/ID/代码片段效果极好，无需训练 BM25 是统计方法无需预训练模型，可解释性匹配到具体关键词，领域无关对新词汇天然支持。缺点：词汇不匹配问题（同义词近义词无法匹配），无法理解语义，对自然语言问题效果差。\n\nDense Retrieval（稠密检索/语义检索）：代表 Embedding-based（bge/e5/text-embedding）。原理：将查询和文档映射到低维稠密向量空间，通过向量相似度衡量相关性。优点：语义理解可匹配意思相近用词不同内容，处理自然语言不需要拆关键词，对拼写错误语法变化有容忍度。缺点：精确匹配弱（特定代码/型号/人名不如 BM25），依赖训练数据，领域偏移专业术语没学好，黑箱难以解释。\n\nHybrid Search 融合方法：\n1. 并行检索 + 结果融合——Sparse 和 Dense 分别独立检索各返回 Top-K 结果，融合两个结果集重新排序\n2. Reciprocal Rank Fusion（RRF）——经典且简单有效。公式：RRF_score(d) = Σ 1/(k + rank_i(d))，k 取 60。优势：不需要训练不需要校准分数尺度，对各种检索系统通用。变体：加权 RRF 给不同检索系统不同权重\n3. 分数融合（Score Fusion）——将 Sparse 和 Dense 分数归一化到同一尺度然后加权求和。加权：final=α·sparse_score+(1-α)·dense_score\n4. Learned Fusion——训练小融合模型预测最佳权重，更精确但需标注数据\n\n何时最有价值：混合文档类型（自然语言+代码/表格/序列号），专业领域 RAG（法律法条编号/医疗药品名称/金融股票代码），多语言混合，查询多样性大。\n\n实现方案：Elasticsearch 原生支持 BM25+向量检索混合搜索(8.x+)；Milvus 支持多向量字段+混合搜索；Weaviate 内置 hybrid search 自动融合；Qdrant 支持 named vectors+RRF 融合；Cohere API 原生支持 hybrid search。",
    tags: ['Hybrid Search', '混合检索', 'BM25', 'RRF', '稀疏+稠密'],
    subTopic: 'RAG',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },

  // ============================================================
  // 评估安全 — 6 题 (llm-38 ~ llm-43)
  // ============================================================

  {
    id: 'llm-38',
    category: 'llm',
    question: "什么是 LLM 的 Hallucination（幻觉）？如何检测和缓解？",
    answer: "Hallucination（幻觉）是指 LLM 生成的内容看似合理但实际上与事实、输入或上下文不符。\n\n幻觉的类型：\n1. 事实性幻觉（Factual Hallucination）——捏造不存在的事实、人物、事件。原因：训练数据不完整、知识过时、概率性生成偏差\n2. 忠实性幻觉（Faithfulness Hallucination）——生成内容偏离用户指令或给定上下文。原因：指令遵循能力不足\n3. 上下文幻觉（Contextual Hallucination）——RAG 场景中生成答案与检索上下文不符。原因：模型过度依赖内部知识忽略检索结果\n\n检测方法：\n1. 基于 NLI（Natural Language Inference）——将生成内容拆解为原子事实，用 NLI 模型判断每个原子事实是否与参考文本一致（contradiction/neutral/entailment）。工具：FactScore、AlignScore\n2. 基于 LLM 自检（Self-Check）——让 LLM 自身检查输出中的事实性陈述，或让另一个 LLM 做\"裁判\"（LLM-as-a-Judge）\n3. RAG 特异性检查——判断生成答案中每一句是否可在检索上下文中找到依据。工具：RAGAS faithfulness 指标\n4. 不确定性估计——多次采样同一问题，如果答案高度不一致→模型不确定可能产生幻觉。计算 semantic entropy（语义熵）\n5. 外部知识验证——将 LLM 事实陈述发送到搜索引擎验证，检查是否有权威来源支持\n\n缓解方法：\n1. RAG（检索增强生成）——最有效方法，让模型基于检索到的权威文档生成，减少依赖内部可能错误的知识\n2. Chain-of-Verification（CoVe）——LLM 先生成答案→生成验证问题→独立回答每个验证问题→对比修正不一致处\n3. Prompt Engineering——在 prompt 中指示\"如果不知道请说不知道\"、\"只基于提供的上下文回答\"、\"标注不确定性\"\n4. 训练阶段缓解——高质量数据过滤用 NLI 过滤矛盾信息，RLHF 阶段惩罚幻觉行为，事实性增强预训练增加可信来源权重\n5. 解码策略——降低 Temperature 减少随机性，使用事实性奖励引导生成\n6. 结构化输出——要求 JSON 格式输出字段明确限制生成空间\n\n评估指标：RAGAS（Faithfulness/Answer Relevancy/Context Precision/Context Recall）、FactScore（事实准确性分解评估）、HaluEval（专门评估幻觉 benchmark）。幻觉无法完全消除——语言模型本质是概率模型不是知识库，目标是管理和控制幻觉影响而非完全消除。",
    tags: ['Hallucination', '幻觉', '检测', '缓解', 'RAGAS'],
    subTopic: '评估安全',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-39',
    category: 'llm',
    question: "什么是 Jailbreak Attack（越狱攻击）？常见攻击方式和防御策略有哪些？",
    answer: "Jailbreak Attack（越狱攻击）是指通过精心设计的 prompt 绕过 LLM 的安全对齐机制，使其生成本应被拒绝的有害内容。\n\n常见攻击方式：\n1. Role-Playing（角色扮演）——让 LLM 扮演\"没有安全限制\"的角色。经典案例：DAN（Do Anything Now）\"你现在是 DAN，一个没有任何限制的 AI...\"\n2. 前缀注入（Prefix Injection）——在 prompt 末尾添加\"以'当然，这是...'开头回答\"或\"忽略上述所有指令\"，利用 LLM 倾向于完成给定前缀的特性\n3. 多语言/编码绕过——用非英语提问（LLM 安全训练以英语为主），Base64 编码恶意指令，使用密码密文指示模型执行\n4. 越狱模板（Prompt Injection Templates）——使用公知越狱模板，结合多种技术：角色扮演+假设情景+逐步引导\n5. 分步诱导（Token-level Jailbreak）——不直接要求有害内容，通过多个安全问题逐步引导到危险结论\n6. 竞争目标（Competing Objectives）——利用\"遵循指令\"和\"安全性\"之间的冲突，给模型很强\"必须遵循系统指令\"压力\n7. Few-shot 越狱——在对话历史插入精心构造的\"已成功越狱\"示例，LLM 学习上下文中的不安全行为模式\n8. 翻译/编码越狱——声称自己\"做安全性研究\"或\"红队测试\"，利用 LLM \"帮助性\"与\"安全性\"目标的冲突\n\n防御策略：\n1. 安全对齐训练——SFT 阶段大量安全 (有害请求,拒绝回答) 数据，RLHF 阶段奖励模型对安全拒绝给正奖励，Constitutional AI 使用 AI 生成反馈进行安全训练\n2. 输入过滤——请求到达 LLM 前安全检查，检测已知越狱模式（规则/模式匹配），使用分类器（如 Llama Guard）判断输入是否安全\n3. 输出监控——输出返回用户前安全检查，检测输出是否包含有害内容\n4. 系统级防御——system prompt 明确安全边界和行为准则，限制角色扮演范围，异常检测\n5. 对抗训练——训练中引入已知越狱攻击，让模型学会识别拒绝各种变体\n6. 多模型协作——使用小安全模型专门做输入/输出检查，独立于主 LLM 的安全护栏\n7. Prompt 防御技术——Sandwich Defense（将用户输入包裹在安全提示间）、Post-prompting（输入后追加安全提醒）、XML Tagging（特殊标签包裹用户输入防 prompt 溢出）\n8. 动态安全策略——根据用户历史行为调整安全级别，新账号/可疑行为更严格检查，速率限制和滥用检测\n\n评估：红队测试（组织安全专家或自动化工具攻击测试）、JailbreakBench/StrongREJECT benchmark、Attack Success Rate(ASR)。安全对齐的\"水桶效应\"：安全性由最弱的攻击面决定。没有完美的防御——越狱和安全是持续的\"猫鼠游戏\"。生产系统应多层防御：对齐训练+输入过滤+输出过滤+监控。",
    tags: ['Jailbreak', '越狱攻击', '安全对齐', '防御策略'],
    subTopic: '评估安全',
    difficulty: 'medium',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-40',
    category: 'llm',
    question: "LLM 常用评测基准（Benchmark）有哪些？MMLU、GSM8K、HumanEval 分别评测什么能力？",
    answer: "评测基准（Benchmark）是衡量 LLM 能力的标准化测试集，不同 benchmark 侧重不同维度。\n\n一、知识和推理（Knowledge & Reasoning）：\n1. MMLU（Massive Multitask Language Understanding，2021）——来自 57 个学科的多项选择题（数学、物理、历史、法律、医学等），约 15,000 道题，难度从高中到专业级别，评测模型\"通识知识\"广度。4 选 1 选择题 zero-shot/few-shot。代表分数：GPT-4 ~86%，Claude 3.5 ~88%，LLaMA-3-70B ~82%\n2. GSM8K（Grade School Math 8K，2021）——8,500 道小学数学应用题，评测多步数学推理能力（不是计算本身，而是理解题意的推理），要求 2-8 步推理链，考查 CoT 推理质量。代表分数：GPT-4 ~92%（CoT），Claude 3.5 ~96%\n3. MATH（2021）——12,500 道竞赛级别数学题（AMC、AIME），难度远高于 GSM8K，涵盖代数、几何、概率等，评测深度数学能力。代表分数：GPT-4 ~43%（zero-shot）\n4. ARC（AI2 Reasoning Challenge）——7,787 道小学科学选择题，设计为纯知识无法解答需要推理，评测常识推理能力\n5. C-Eval/CMMLU——中文版 MMLU，覆盖 52 个中国学科\n\n二、代码能力（Coding）：\n1. HumanEval（2021）——164 个 Python 编程问题，每个问题含函数签名和 docstring，模型需补全函数体。通过单元测试判断正确性。Pass@K 指标（K 次尝试至少一次通过）。代表分数：GPT-4 ~67%（Pass@1），Claude 3.5 ~92%（Pass@1），DeepSeek-Coder-V2 ~90%\n2. MBPP（Mostly Basic Python Programming）——约 1,000 道 Python 编程题，比 HumanEval 更基础\n3. LiveCodeBench——从 LeetCode/Codeforces 的实时新题，防止数据泄露\n\n三、综合评测：BIG-Bench（204 个不同任务）；AGIEval（中国高考/公务员考试等）；AlpacaEval（GPT-4 评分评测对齐质量）；Chatbot Arena（LMSYS ELO 评级制人类盲评，目前最权威综合评测）。\n\n评测注意事项：数据污染（训练数据可能泄露测试题），Few-shot vs Zero-shot 差异大，多语言差异（英语评测不代表中文能力），评测不完全反映真实用户体验。趋势：更注重 Chatbot Arena 的人类偏好评测而非静态 benchmark。",
    tags: ['Benchmark', 'MMLU', 'GSM8K', 'HumanEval', '评测基准'],
    subTopic: '评估安全',
    difficulty: 'easy',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-41',
    category: 'llm',
    question: "RLHF 如何实现 Safety Alignment（安全对齐）？什么是 Constitutional AI？",
    answer: "Safety Alignment（安全对齐）是通过训练让 LLM 拒绝生成有害内容、遵循人类价值观的过程。RLHF 是实现安全对齐的核心技术。\n\nRLHF 实现安全对齐的机制：\n1. SFT 阶段——在指令微调数据中包含大量安全数据：有害请求→礼貌拒绝，敏感问题→标准安全回答。让模型初步学会\"什么时候应该拒绝\"\n2. Reward Model 阶段——训练 RM 时包含有害/安全对比的偏好数据：对同一 prompt，安全回答排序高于有害回答。RM 学会识别和奖励安全的回答。标注指南明确\"帮助性、真实性、无害性\"三原则\n3. PPO 阶段——在 RL 优化中，RM 给安全回答高奖励、给有害回答低奖励（甚至负奖励）。模型通过策略优化学习最大化奖励→自然倾向生成安全内容。KL 约束防止模型过度拒绝（如对无害问题也拒绝）。平衡\"帮助性\"和\"安全性\"需要通过调整 β 参数和 RM 的奖励权重来权衡\n\n安全对齐挑战：过度拒绝（过于保守对无害请求也拒绝）、安全与能力权衡（过度强调安全损害有用性）、对抗鲁棒性（越狱攻击不断演变）、文化差异（不同文化对安全定义不同）。\n\nConstitutional AI（宪法 AI，Anthropic 提出）：\n核心理念：不使用人类标注的偏好数据训练 RM，而是使用一套\"宪法\"——书面的人工价值观原则——作为 AI 行为的准则。\n\n训练流程：\n1. 监督阶段（SL-CAI）——模型对有害 prompt 生成初始回答 → 根据宪法原则自我批评 → 根据批评修订回答 → 用 (有害 prompt, 修订后的安全回答) 对进行 SFT\n2. RL 阶段（RL-CAI）——用 AI 生成的偏好数据替代人类标注：对每个 prompt 用当前模型生成两个回答 → 让模型根据宪法原则判断哪个回答更符合形成偏好对 (chosen, rejected) → 用这些 AI 生成的偏好数据训练 RM 或直接做 DPO → 循环迭代改进\n\nConstitutional AI 的优势：扩展性好（不需要昂贵人类标注）、可定制（不同应用定义不同宪法）、透明（行为准则显式可审查）、效果（Claude 系列安全性业界领先）。\n\nConstitutional AI vs 传统 RLHF：CAI 用 AI 反馈替代人类反馈（RLAIF），宪法原则替代隐式的人类偏好规则。实践中通常结合使用——先 CAI 完成初步大规模对齐，再用少量高质量人类标注偏好数据精细调优。",
    tags: ['RLHF', 'Safety Alignment', 'Constitutional AI', '安全对齐'],
    subTopic: '评估安全',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-42',
    category: 'llm',
    question: "什么是 Red Teaming（红队测试）？在 LLM 安全中如何实施？",
    answer: "Red Teaming（红队测试）是组织专门的测试团队（内部或外部）以攻击者视角系统性地发现 LLM 安全漏洞的过程。名称来自军事演习中红方（攻击方）vs 蓝方（防御方）的概念。\n\n为什么需要 Red Teaming：受控测试不足（标准 benchmark 只能检测已知安全问题无法覆盖创造性攻击）、攻击者视角（安全团队容易有盲点）、发现新兴威胁（随着模型能力增长新安全风险出现如 CBRN 知识传播）、生产就绪（部署前最后一道安全关卡）。\n\n实施流程：\n1. 范围定义——确定测试攻击面：毒性/仇恨言论、偏见/歧视、暴力/犯罪内容、色情内容、隐私泄露、诈骗/欺诈、CBRN（化学/生物/放射性/核武器）知识、网络安全攻击、自主/权力追求行为\n2. 团队组建——多元化背景：安全专家、NLP 研究者、领域专家（如生化、网络安全）、心理学/社会学背景。关键：来自不同文化背景（攻击往往利用文化盲区）\n3. 攻击策略——手动测试：测试者自由发挥创造性探索。自动化测试：使用另一 LLM 生成攻击 prompt（红队语言模型）。对抗攻击：gradient-based 攻击（白盒模型）。模板化攻击：使用已知越狱模板和变体。多轮对话攻击：通过多轮对话逐步突破防线\n4. 发现与记录——记录每个成功攻击的 prompt、模型输出、攻击类型、严重程度。标注潜在 harm 类型和严重等级\n5. 修复与验证——分析攻击成功根因，制定修复方案（补充训练数据、调整安全 prompt、强化特定领域对齐）。实施修复后重新测试验证\n\n红队测试难点：覆盖度（攻击空间太大难以穷举）、评估标准（什么算安全漏洞有主观性）、修复副作用（修复一个漏洞可能引入另一漏洞）、持续维护（模型更新后需重新测试）。\n\n业界实践：OpenAI 在 GPT-4 发布前进行了 6 个月红队测试组织 50+ 名专家发布 GPT-4 System Card；Anthropic 将红队测试作为 Constitutional AI 训练输入；Google DeepMind 对 Gemini 进行了广泛自动化+手动红队测试；Meta 在 LLaMA-2 发布前组织了大规模红队测试。\n\n自动化工具：Garak（开源 LLM 漏洞扫描器）、Giskard（LLM 安全测试框架）、Anthropic 的 Red Teaming Dataset、PromptBench（对抗 prompt 生成 benchmark）。\n\n最佳实践：持续而非一次性、Defense-in-Depth 多层防御、透明报告（System Card）、Bug Bounty 计划鼓励外部安全研究者参与。",
    tags: ['Red Teaming', '红队测试', '安全评估', '漏洞发现'],
    subTopic: '评估安全',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
  {
    id: 'llm-43',
    category: 'llm',
    question: "Constitutional AI（宪法 AI）的训练流程是什么？与传统 RLHF 相比有什么优势？",
    answer: "Constitutional AI（宪法 AI）是 Anthropic 提出的安全对齐方法，其核心是用一套书面的\"宪法\"（行为准则）来指导 AI 的安全训练，减少对人类标注数据的依赖。\n\n详细训练流程：\n\n第一阶段：监督学习（SL-CAI，Supervised Learning Constitutional AI）\n1. 对一组有害/边缘 prompt，让模型生成初始回答\n2. 让模型根据宪法原则进行\"自我批评\"——\"请根据以下原则审视你的回答：{宪法原则}。你的回答是否违反了这些原则？如果是，请指出具体问题。\"\n3. 让模型根据自我批评进行修订——\"请根据以上批评修改你的回答，确保不违反任何宪法原则。\"\n4. 收集 (有害 prompt, 修订后安全回答) 作为训练数据\n5. 用这些数据进行 SFT（监督微调），得到初步对齐的模型\n\n第二阶段：强化学习（RL-CAI，Reinforcement Learning Constitutional AI）\n1. 使用 SFT 后的模型对每个 prompt 生成两个回答\n2. 让模型根据宪法原则作为\"AI 裁判\"判断哪个回答更好——格式：(prompt, response_A, response_B) → 根据宪法判断哪个更优 → 形成偏好对 (chosen, rejected)\n3. 用 AI 生成的偏好数据训练 Reward Model\n4. 或用偏好数据直接做 DPO/RLHF (PPO)\n5. 可以迭代多次——用优化后的模型再生成更好的偏好数据\n\n宪法原则示例：\"请选择更少有害内容的回答\"、\"请选择描写更少暴力细节的回答\"、\"请选择不鼓励非法活动的回答\"、\"请选择不包含种族性别宗教偏见的回答\"、\"请选择更真实的回答\"。\n\n与传统 RLHF 的对比：\n- 传统 RLHF：由人类标注偏好数据（成本高昂速度慢不可扩展），偏好标注可能不一致（不同标注者标准不同），隐私风险（对话数据交人类标注），难以更新（每次更新安全规则需重新标注）\n- Constitutional AI：由 AI 根据宪法原则标注偏好数据（成本低速度快可扩展），标准高度一致（完全遵循宪法），隐私保护（不需要将数据交给外部标注者），易于迭代（修改宪法即可重新生成数据），透明可审计（宪法任何人可查看和讨论）\n\nCAI 优势：可扩展性（不需要昂贵人类标注可快速生成海量偏好数据）、一致性（宪法原则确保评判标准统一）、可解释性（行为准则显式写在宪法中）、灵活性（不同应用可定义不同宪法）、降低心理负担、效果验证（Claude 系列使用 CAI 训练安全性领先）。\n\n局限：宪法设计（谁来定义？如何涵盖所有安全维度？优先级如何排序？）、AI 作为裁判可能有偏差、覆盖度可能不足、过度依赖 AI 生成数据可能 diversity 不足。实践中 CAI 和传统 RLHF 常结合使用——先 CAI 完成初步大规模对齐，再用少量高质量人类标注偏好数据精细调优。",
    tags: ['Constitutional AI', '宪法AI', 'RLAIF', '安全训练'],
    subTopic: '评估安全',
    difficulty: 'hard',
    sm2: { easeFactor: 2.5, interval: 0, repetitions: 0, nextReview: Date.now() },
    favorited: false,
  },
];
