# 后端运行端口
PORT=3001

# SQLite 数据库路径（开发环境）
DATABASE_URL="file:../data/flashcards.db"

# 生产环境 PostgreSQL 示例：
# DATABASE_URL="postgresql://user:password@host:5432/flashcards"

# CORS 允许的来源（生产环境改为前端域名）
CORS_ORIGIN=http://localhost:5173
